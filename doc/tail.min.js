function checkOpenPopupWindow() {
    for (var count = 0, i = 0; index > i; i++) isset(stack[i]) && !stack[i].closed && count++;
    return count
}

function isset(variable) {
    return "undefined" != typeof variable && null !== variable
}

function createCookie(name, value, days) {
    if (days) {
        var date = new Date;
        date.setTime(date.getTime() + 24 * days * 60 * 60 * 1e3);
        var expires = "; expires=" + date.toGMTString()
    } else var expires = "";
    document.cookie = name + "=" + value + expires + "; path=/"
}

function eraseCookie(name) {
    createCookie(name, "", -1)
}

function getCookie(Name) {
    var search = Name + "=";
    if (document.cookie.length > 0) {
        var offset = document.cookie.indexOf(search);
        if (-1 != offset) {
            offset += search.length;
            var end = document.cookie.indexOf(";", offset);
            return -1 == end && (end = document.cookie.length), unescape(document.cookie.substring(offset, end))
        }
    }
}

function ControlVersion() {
    var e, t;
    try {
        t = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.7"), e = t.GetVariable("$version")
    } catch (n) {}
    if (!e) try {
        t = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.6"), e = "WIN 6,0,21,0", t.AllowScriptAccess = "always", e = t.GetVariable("$version")
    } catch (n) {}
    if (!e) try {
        t = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.3"), e = t.GetVariable("$version")
    } catch (n) {}
    if (!e) try {
        t = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.3"), e = "WIN 3,0,18,0"
    } catch (n) {}
    if (!e) try {
        t = new ActiveXObject("ShockwaveFlash.ShockwaveFlash"), e = "WIN 2,0,0,11"
    } catch (n) {
        e = -1
    }
    return e
}

function GetSwfVer() {
    var e = -1;
    if (null != navigator.plugins && navigator.plugins.length > 0) {
        if (navigator.plugins["Shockwave Flash 2.0"] || navigator.plugins["Shockwave Flash"]) {
            var t = navigator.plugins["Shockwave Flash 2.0"] ? " 2.0" : "",
                n = navigator.plugins["Shockwave Flash" + t].description,
                r = n.split(" "),
                i = r[2].split("."),
                s = i[0],
                o = i[1],
                u = r[3];
            "" == u && (u = r[4]), "d" == u[0] ? u = u.substring(1) : "r" == u[0] && (u = u.substring(1), u.indexOf("d") > 0 && (u = u.substring(0, u.indexOf("d"))));
            var e = s + "." + o + "." + u
        }
    } else navigator.userAgent.toLowerCase().indexOf("webtv/2.6") != -1 ? e = 4 : navigator.userAgent.toLowerCase().indexOf("webtv/2.5") != -1 ? e = 3 : navigator.userAgent.toLowerCase().indexOf("webtv") != -1 ? e = 2 : isIE && isWin && !isOpera && (e = ControlVersion());
    return e
}

function DetectFlashVer(e, t, n) {
    if (versionStr = GetSwfVer(), versionStr == -1) return !1;
    if (0 != versionStr) {
        isIE && isWin && !isOpera ? (tempArray = versionStr.split(" "), tempString = tempArray[1], versionArray = tempString.split(",")) : versionArray = versionStr.split(".");
        var r = versionArray[0],
            i = versionArray[1],
            s = versionArray[2];
        if (r > parseFloat(e)) return !0;
        if (r == parseFloat(e)) {
            if (i > parseFloat(t)) return !0;
            if (i == parseFloat(t) && s >= parseFloat(n)) return !0
        }
        return !1
    }
}

function AC_AddExtension(e, t) {
    return e.indexOf("?") != -1 ? e.replace(/\?/, t + "?") : e + t
}

function AC_Generateobj(e, t, n) {
    var r = "";
    if (isIE && isWin && !isOpera) {
        r += "<object ";
        for (var i in e) r += i + '="' + e[i] + '" ';
        r += ">";
        for (var i in t) r += '<param name="' + i + '" value="' + t[i] + '" /> ';
        r += "</object>"
    } else {
        r += "<embed ";
        for (var i in n) r += i + '="' + n[i] + '" ';
        r += "> </embed>"
    }
    document.write(r)
}

function AC_FL_RunContent() {
    var e = AC_GetArgs(arguments, ".swf", "movie", "clsid:d27cdb6e-ae6d-11cf-96b8-444553540000", "application/x-shockwave-flash");
    AC_Generateobj(e.objAttrs, e.params, e.embedAttrs)
}

function AC_SW_RunContent() {
    var e = AC_GetArgs(arguments, ".dcr", "src", "clsid:166B1BCA-3F9C-11CF-8075-444553540000", null);
    AC_Generateobj(e.objAttrs, e.params, e.embedAttrs)
}

function AC_GetArgs(e, t, n, r, i) {
    var s = new Object;
    s.embedAttrs = new Object, s.params = new Object, s.objAttrs = new Object;
    for (var o = 0; o < e.length; o += 2) {
        var u = e[o].toLowerCase();
        switch (u) {
            case "classid":
                break;
            case "pluginspage":
                s.embedAttrs[e[o]] = e[o + 1];
                break;
            case "src":
            case "movie":
                e[o + 1] = AC_AddExtension(e[o + 1], t), s.embedAttrs.src = e[o + 1], s.params[n] = e[o + 1];
                break;
            case "onafterupdate":
            case "onbeforeupdate":
            case "onblur":
            case "oncellchange":
            case "onclick":
            case "ondblclick":
            case "ondrag":
            case "ondragend":
            case "ondragenter":
            case "ondragleave":
            case "ondragover":
            case "ondrop":
            case "onfinish":
            case "onfocus":
            case "onhelp":
            case "onmousedown":
            case "onmouseup":
            case "onmouseover":
            case "onmousemove":
            case "onmouseout":
            case "onkeypress":
            case "onkeydown":
            case "onkeyup":
            case "onload":
            case "onlosecapture":
            case "onpropertychange":
            case "onreadystatechange":
            case "onrowsdelete":
            case "onrowenter":
            case "onrowexit":
            case "onrowsinserted":
            case "onstart":
            case "onscroll":
            case "onbeforeeditfocus":
            case "onactivate":
            case "onbeforedeactivate":
            case "ondeactivate":
            case "type":
            case "codebase":
            case "id":
                s.objAttrs[e[o]] = e[o + 1];
                break;
            case "width":
            case "height":
            case "align":
            case "vspace":
            case "hspace":
            case "class":
            case "title":
            case "accesskey":
            case "name":
            case "tabindex":
                s.embedAttrs[e[o]] = s.objAttrs[e[o]] = e[o + 1];
                break;
            default:
                s.embedAttrs[e[o]] = s.params[e[o]] = e[o + 1]
        }
    }
    return s.objAttrs.classid = r, i && (s.embedAttrs.type = i), s
}

function getCookie(Name) {
    var search = Name + "=";
    if (document.cookie.length > 0) {
        var offset = document.cookie.indexOf(search);
        if (offset != -1) {
            offset += search.length;
            var end = document.cookie.indexOf(";", offset);
            return end == -1 && (end = document.cookie.length), unescape(document.cookie.substring(offset, end))
        }
    }
}

function fimReady() {
    var fim = getFim(fimName),
        gameHash = window.location.hash.substr(1),
        gameCode = gameHash.replace("code/", ""),
        subGameCode = getCookie("subgamecode"),
        gameTableId = getCookie("gametableid"),
        limitMin = getCookie("limitmin"),
        limitMax = getCookie("limitmax"),
        liveRoom = getCookie("liveroom"),
        networkId = getCookie("networkid");
    fim.setCallout("playforreal", "launchClientLogin"), "live-games" == secondLevelLocation ? fim.initClient("casino", casinoSwf + "?language=" + language + "&subgame=" + subgame + "&gametableid=" + gametableid + "&limitmin=" + limitmin + "&limitmax=" + limitmax + "&liveroom=" + liveroom + "-" + networkid) : fim.initClient("casino", casinoSwf + "?language=" + language), "" != userKey && fim.initUser(userKey), "live-games" == secondLevelLocation ? gameHash && gameCode.length != gameHash.length && gameHash.indexOf("overlay") == -1 && User.authenticated && launchFimCasino(gameCode, !1, subGameCode, gameTableId, limitMin, limitMax, liveRoom, networkId) : gameHash && gameCode.length != gameHash.length && gameHash.indexOf("overlay") == -1 && User.authenticated && launchFimCasino(gameCode)
}

function openCashier() {
    window.open(cashierUrl, "realoffline", "width=640,height=480,toolbar=no", "menubar=no,scrollbars=no,resizable=yes")
}

function focusCashierWebPage() {
    window.focus()
}

function getFim(swfId) {
    return window[swfId] ? window[swfId] : document[swfId]
}

function launchFimCasino(module, offline, subgame, gametableid, limitmin, limitmax, liveroom, networkid, cmsgametype) {
    var fim = getFim(fimName),
        offline = "undefined" != typeof offline && offline,
        subgamecode = "undefined" != typeof subgame && subgamecode,
        gametableid = "undefined" != typeof gametableid && gametableid,
        limitmin = "undefined" != typeof limitmin && limitmin,
        limitmax = "undefined" != typeof limitmax && limitmax,
        liveroom = "undefined" != typeof liveroom && liveroom,
        networkid = "undefined" != typeof networkid && networkid,
        cmsgametype = "undefined" != typeof cmsgametype && cmsgametype;
    if ("live-games" == cmsgametype ? 0 == networkid.length ? fim.initClient("casino", casinoSwf + "?language=" + language + "&subgame=" + subgame + "&gametableid=" + gametableid + "&limitmin=" + limitmin + "&limitmax=" + limitmax + "&liveroom=" + liveroom) : fim.initClient("casino", casinoSwf + "?language=" + language + "&subgame=" + subgame + "&gametableid=" + gametableid + "&limitmin=" + limitmin + "&limitmax=" + limitmax + "&liveroom=" + liveroom + "-" + networkid) : fim.initClient("casino", casinoSwf + "?language=" + language), subgame) var subgame = subgame;
    if (subgamecode) var subgame = subgamecode;
    "" != userKey && 0 == offline ? fim.launchClient("casino", module) : fim.launchClient("casino", module, "offline")
}

function launchClientLogin(type, module) {
    getFim(fimName).close(type), jQuery("." + module).click()
}

function closeCasino() {
    getFim(fimName).close("casino")
}

function queryToObject() {
    for (var retObj = {}, pair = null, sPageURL = window.location.search.substring(1), qArr = sPageURL.split("&"), i = 0; i < qArr.length; i++) pair = qArr[i].split("="), retObj[pair[0]] = pair[1];
    return retObj
}

function ImageFlow() {
    this.defaults = {
        animationSpeed: 50,
        aspectRatio: 1.964,
        buttons: !0,
        captions: !0,
        circular: !1,
        imageCursor: "default",
        ImageFlowID: "imageflow",
        imageFocusM: 1,
        imageFocusMax: 3,
        imagePath: "",
        imageScaling: !0,
        imagesHeight: .67,
        imagesM: 1,
        opacity: !1,
        opacityArray: [10, 8, 7, 6, 5],
        percentLandscape: 118,
        percentOther: 100,
        preloadImages: !1,
        reflections: !0,
        reflectionGET: "",
        reflectionP: .5,
        reflectionPNG: !1,
        reflectPath: "/media/gts/js/",
        scrollbarP: .6,
        slider: !0,
        sliderCursor: "pointer",
        sliderWidth: 14,
        slideshow: !1,
        slideshowSpeed: 1500,
        slideshowAutoplay: !1,
        startID: 1,
        glideToStartID: !0,
        startAnimation: !1,
        xStep: 150
    };
    var my = this;
    this.init = function(options) {
        for (var name in my.defaults) this[name] = void 0 !== options && void 0 !== options[name] ? options[name] : my.defaults[name];
        var ImageFlowDiv = document.getElementById(my.ImageFlowID);
        if (ImageFlowDiv && (ImageFlowDiv.style.visibility = "visible", this.ImageFlowDiv = ImageFlowDiv, this.createStructure())) {
            this.imagesDiv = document.getElementById(my.ImageFlowID + "_images"), this.captionDiv = document.getElementById(my.ImageFlowID + "_caption"), this.navigationDiv = document.getElementById(my.ImageFlowID + "_navigation"), this.scrollbarDiv = document.getElementById(my.ImageFlowID + "_scrollbar"), this.sliderDiv = document.getElementById(my.ImageFlowID + "_slider"), this.buttonNextDiv = document.getElementById(my.ImageFlowID + "_next"), this.buttonPreviousDiv = document.getElementById(my.ImageFlowID + "_previous"), this.buttonSlideshow = document.getElementById(my.ImageFlowID + "_slideshow"), this.indexArray = [], this.current = 0, this.imageID = 0, this.target = 0, this.memTarget = 0, this.firstRefresh = !0, this.firstCheck = !0, this.busy = !1;
            var width = this.ImageFlowDiv.offsetWidth,
                height = Math.round(width / my.aspectRatio);
            document.getElementById(my.ImageFlowID + "_loading_txt").style.paddingTop = .5 * height - 22 + "px", ImageFlowDiv.style.height = height + "px", this.loadingProgress()
        }
    }, this.createStructure = function() {
        for (var node, version, src, imageNode, imagesDiv = my.Helper.createDocumentElement("div", "images"), max = my.ImageFlowDiv.childNodes.length, index = 0; index < max; index++) node = my.ImageFlowDiv.childNodes[index], node && 1 == node.nodeType && "IMG" == node.nodeName && (my.reflections === !0 && (version = my.reflectionPNG ? "3" : "2", src = my.imagePath + node.getAttribute("src", 2), src += my.reflectionGET, node.setAttribute("src", src)), imageNode = node.cloneNode(!0), imagesDiv.appendChild(imageNode));
        if (my.circular) {
            var first = my.Helper.createDocumentElement("div", "images"),
                last = my.Helper.createDocumentElement("div", "images");
            if (max = imagesDiv.childNodes.length, max < my.imageFocusMax && (my.imageFocusMax = max), max > 1) {
                var i;
                for (i = 0; i < max; i++) node = imagesDiv.childNodes[i], i < my.imageFocusMax && (imageNode = node.cloneNode(!0), first.appendChild(imageNode)), max - i < my.imageFocusMax + 1 && (imageNode = node.cloneNode(!0), last.appendChild(imageNode));
                for (i = 0; i < max; i++) node = imagesDiv.childNodes[i], imageNode = node.cloneNode(!0), last.appendChild(imageNode);
                for (i = 0; i < my.imageFocusMax; i++) node = first.childNodes[i], imageNode = node.cloneNode(!0), last.appendChild(imageNode);
                imagesDiv = last
            }
        }
        if (my.slideshow) {
            var slideshowButton = my.Helper.createDocumentElement("div", "slideshow");
            imagesDiv.appendChild(slideshowButton)
        }
        var loadingP = my.Helper.createDocumentElement("p", "loading_txt"),
            loadingText = document.createTextNode(" ");
        loadingP.appendChild(loadingText);
        var loadingDiv = my.Helper.createDocumentElement("div", "loading"),
            loadingBarDiv = my.Helper.createDocumentElement("div", "loading_bar");
        loadingDiv.appendChild(loadingBarDiv);
        var captionDiv = my.Helper.createDocumentElement("div", "caption"),
            scrollbarDiv = my.Helper.createDocumentElement("div", "scrollbar"),
            sliderDiv = my.Helper.createDocumentElement("div", "slider");
        if (scrollbarDiv.appendChild(sliderDiv), my.buttons) {
            var buttonPreviousDiv = my.Helper.createDocumentElement("div", "previous", "button"),
                buttonNextDiv = my.Helper.createDocumentElement("div", "next", "button");
            scrollbarDiv.appendChild(buttonPreviousDiv), scrollbarDiv.appendChild(buttonNextDiv)
        }
        var navigationDiv = my.Helper.createDocumentElement("div", "navigation");
        navigationDiv.appendChild(captionDiv), navigationDiv.appendChild(scrollbarDiv);
        var success = !1;
        if (my.ImageFlowDiv.appendChild(imagesDiv) && my.ImageFlowDiv.appendChild(loadingP) && my.ImageFlowDiv.appendChild(loadingDiv) && my.ImageFlowDiv.appendChild(navigationDiv)) {
            for (max = my.ImageFlowDiv.childNodes.length, index = 0; index < max; index++) node = my.ImageFlowDiv.childNodes[index], node && 1 == node.nodeType && "IMG" == node.nodeName && my.ImageFlowDiv.removeChild(node);
            success = !0
        }
        return success
    }, this.loadingProgress = function() {
        var p = my.loadingStatus();
        (p < 100 || my.firstCheck) && my.preloadImages ? my.firstCheck && 100 == p ? (my.firstCheck = !1, window.setTimeout(my.loadingProgress, 100)) : window.setTimeout(my.loadingProgress, 40) : (document.getElementById(my.ImageFlowID + "_loading_txt").style.display = "none", document.getElementById(my.ImageFlowID + "_loading").style.display = "none", window.setTimeout(my.Helper.addResizeEvent, 1e3), my.refresh(), my.max > 1 && (my.MouseWheel.init(), my.MouseDrag.init(), my.Touch.init(), my.Key.init(), my.slideshow && my.Slideshow.init(), my.slider && (my.scrollbarDiv.style.visibility = "visible")))
    }, this.loadingStatus = function() {
        for (var max = my.imagesDiv.childNodes.length, i = 0, completed = 0, image = null, index = 0; index < max; index++) image = my.imagesDiv.childNodes[index], image && 1 == image.nodeType && "IMG" == image.nodeName && (image.complete && completed++, i++);
        var finished = Math.round(completed / i * 100),
            loadingBar = document.getElementById(my.ImageFlowID + "_loading_bar");
        loadingBar.style.width = finished + "%", my.circular && (i -= 2 * my.imageFocusMax, completed = finished < 1 ? 0 : Math.round(i / 100 * finished));
        var loadingP = document.getElementById(my.ImageFlowID + "_loading_txt"),
            loadingTxt = document.createTextNode("loading images " + completed + "/" + i);
        return loadingP.replaceChild(loadingTxt, loadingP.firstChild), finished
    }, this.refresh = function() {
        this.imagesDivWidth = my.imagesDiv.offsetWidth + my.imagesDiv.offsetLeft, this.maxHeight = Math.round(my.imagesDivWidth / my.aspectRatio), this.maxFocus = my.imageFocusMax * my.xStep, this.size = .5 * my.imagesDivWidth, this.sliderWidth = .5 * my.sliderWidth, this.scrollbarWidth = (my.imagesDivWidth - 2 * Math.round(my.sliderWidth)) * my.scrollbarP, this.imagesDivHeight = Math.round(my.maxHeight * my.imagesHeight), my.ImageFlowDiv.style.height = my.maxHeight + "px", my.imagesDiv.style.height = my.imagesDivHeight + "px", my.navigationDiv.style.height = my.maxHeight - my.imagesDivHeight + "px", my.captionDiv.style.width = my.imagesDivWidth + "px", my.captionDiv.style.paddingTop = Math.round(.02 * my.imagesDivWidth) + "px", my.scrollbarDiv.style.width = my.scrollbarWidth + 66 + "px", my.scrollbarDiv.style.marginTop = Math.round(.02 * my.imagesDivWidth) + "px", my.scrollbarDiv.style.marginLeft = Math.round(my.sliderWidth + (my.imagesDivWidth - my.scrollbarWidth) / 2 - 45) + "px", my.sliderDiv.style.cursor = my.sliderCursor, my.sliderDiv.onmousedown = function() {
            return my.MouseDrag.start(this), !1
        }, my.buttons && (my.buttonPreviousDiv.onclick = function() {
            my.MouseWheel.handle(1)
        }, my.buttonNextDiv.onclick = function() {
            my.MouseWheel.handle(-1)
        });
        for (var multi = my.reflections === !0 ? my.reflectionP + 1 : 1, max = my.imagesDiv.childNodes.length, i = 0, image = null, index = 0; index < max; index++) image = my.imagesDiv.childNodes[index], null !== image && 1 == image.nodeType && "IMG" == image.nodeName && (this.indexArray[i] = index, image.url = image.getAttribute("longdesc"), image.xPosition = -i * my.xStep, image.i = i, my.firstRefresh && (null !== image.getAttribute("width") && null !== image.getAttribute("height") ? (image.w = image.getAttribute("width"), image.h = image.getAttribute("height") * multi) : (image.w = image.width, image.h = image.height)), image.w > image.h / (my.reflectionP + 1) ? (image.pc = my.percentLandscape, image.pcMem = my.percentLandscape) : (image.pc = my.percentOther, image.pcMem = my.percentOther), my.imageScaling === !1 && (image.style.position = "relative", image.style.display = "inline"), image.style.cursor = my.imageCursor, i++);
        this.max = my.indexArray.length, my.imageScaling === !1 && (image = my.imagesDiv.childNodes[my.indexArray[0]], this.totalImagesWidth = image.w * my.max, image.style.paddingLeft = my.imagesDivWidth / 2 + image.w / 2 + "px", my.imagesDiv.style.height = image.h + "px", my.navigationDiv.style.height = my.maxHeight - image.h + "px"), my.firstRefresh && (my.firstRefresh = !1, my.imageID = my.startID - 1, my.imageID < 0 && (my.imageID = 0), my.circular && (my.imageID = my.imageID + my.imageFocusMax), maxId = my.circular ? my.max - my.imageFocusMax - 1 : my.max - 1, my.imageID > maxId && (my.imageID = maxId), my.glideToStartID === !1 && my.moveTo(-my.imageID * my.xStep), my.startAnimation && my.moveTo(5e3)), my.max > 1 && my.glideTo(my.imageID, my.startID), my.moveTo(my.current)
    }, this.moveTo = function(x) {
        this.current = x, this.zIndex = my.max;
        for (var index = 0; index < my.max; index++) {
            var image = my.imagesDiv.childNodes[my.indexArray[index]],
                currentImage = index * -my.xStep;
            if (my.imageScaling)
                if (currentImage + my.maxFocus < my.memTarget || currentImage - my.maxFocus > my.memTarget) image.style.visibility = "hidden", image.style.display = "none";
                else {
                    var z = (Math.sqrt(1e4 + x * x) + 100) * my.imagesM,
                        xs = x / z * my.size + my.size;
                    image.style.display = "block";
                    var newImageH = image.h / image.w * image.pc / z * my.size,
                        newImageW = 0;
                    switch (newImageH > my.maxHeight) {
                        case !1:
                            newImageW = image.pc / z * my.size;
                            break;
                        default:
                            newImageH = my.maxHeight, newImageW = image.w * newImageH / image.h
                    }
                    var newImageTop = my.imagesDivHeight - newImageH + newImageH / (my.reflectionP + 1) * my.reflectionP;
                    switch (image.style.left = xs - image.pc / 2 / z * my.size + "px", newImageW && newImageH && (image.style.height = newImageH + "px", image.style.width = newImageW + "px", image.style.top = newImageTop + "px"), image.style.visibility = "visible", x < 0) {
                        case !0:
                            this.zIndex++;
                            break;
                        default:
                            this.zIndex = my.zIndex - 1
                    }
                    switch (image.i == my.imageID) {
                        case !1:
                            image.onclick = function() {
                                my.glideTo(this.i)
                            };
                            break;
                        default:
                            this.zIndex = my.zIndex + 1, "" !== image.url && void 0 !== my.onClick && (image.onclick = my.onClick)
                    }
                    image.style.zIndex = my.zIndex
                } else {
                if (currentImage + my.maxFocus < my.memTarget || currentImage - my.maxFocus > my.memTarget) image.style.visibility = "hidden";
                else switch (image.style.visibility = "visible", image.i == my.imageID) {
                    case !1:
                        image.onclick = function() {
                            my.glideTo(this.i)
                        };
                        break;
                    default:
                        "" !== image.url && (image.onclick = my.onClick)
                }
                my.imagesDiv.style.marginLeft = x - my.totalImagesWidth + "px"
            }
            x += my.xStep
        }
    }, this.glideTo = function(imageID) {
        var jumpTarget, clonedImageID;
        my.circular && (imageID + 1 === my.imageFocusMax && (clonedImageID = my.max - my.imageFocusMax, jumpTarget = -clonedImageID * my.xStep, imageID = clonedImageID - 1), imageID === my.max - my.imageFocusMax && (clonedImageID = my.imageFocusMax - 1, jumpTarget = -clonedImageID * my.xStep, imageID = clonedImageID + 1));
        var x = -imageID * my.xStep;
        this.target = x, this.memTarget = x, this.imageID = imageID;
        var caption = my.imagesDiv.childNodes[imageID].getAttribute("alt");
        "" !== caption && my.captions !== !1 || (caption = "&nbsp;");
        var local_image_id = this.imageID + 1;
        if (document.getElementById("contentloyaltyclub") ? loyaltycontent(local_image_id) : document.getElementById("contentslotsclub") ? slotscontent(local_image_id) : cashbackcontent(local_image_id), my.captionDiv.innerHTML = caption, my.MouseDrag.busy === !1 && (my.circular ? this.newSliderX = (imageID - my.imageFocusMax) * my.scrollbarWidth / (my.max - 2 * my.imageFocusMax - 1) - my.MouseDrag.newX : this.newSliderX = imageID * my.scrollbarWidth / (my.max - 1) - my.MouseDrag.newX, my.sliderDiv.style.marginLeft = my.newSliderX - my.sliderWidth + "px"), my.opacity === !0 || my.imageFocusM !== my.defaults.imageFocusM) {
            my.Helper.setOpacity(my.imagesDiv.childNodes[imageID], my.opacityArray[0]), my.imagesDiv.childNodes[imageID].pc = my.imagesDiv.childNodes[imageID].pc * my.imageFocusM;
            for (var opacityValue = 0, rightID = 0, leftID = 0, last = my.opacityArray.length, i = 1; i < my.imageFocusMax + 1; i++) opacityValue = i + 1 > last ? my.opacityArray[last - 1] : my.opacityArray[i], rightID = imageID + i, leftID = imageID - i, rightID < my.max && (my.Helper.setOpacity(my.imagesDiv.childNodes[rightID], opacityValue), my.imagesDiv.childNodes[rightID].pc = my.imagesDiv.childNodes[rightID].pcMem), leftID >= 0 && (my.Helper.setOpacity(my.imagesDiv.childNodes[leftID], opacityValue), my.imagesDiv.childNodes[leftID].pc = my.imagesDiv.childNodes[leftID].pcMem)
        }
        jumpTarget && my.moveTo(jumpTarget), my.busy === !1 && (my.busy = !0, my.animate())
    }, this.animate = function() {
        switch (my.target < my.current - 1 || my.target > my.current + 1) {
            case !0:
                my.moveTo(my.current + (my.target - my.current) / 3), window.setTimeout(my.animate, my.animationSpeed), my.busy = !0;
                break;
            default:
                my.busy = !1
        }
    }, this.glideOnEvent = function(imageID) {
        my.slideshow && my.Slideshow.interrupt(), my.glideTo(imageID)
    }, this.Slideshow = {
        direction: 1,
        init: function() {
            my.slideshowAutoplay ? my.Slideshow.start() : my.Slideshow.stop()
        },
        interrupt: function() {
            my.Helper.removeEvent(my.ImageFlowDiv, "click", my.Slideshow.interrupt), my.Slideshow.stop()
        },
        addInterruptEvent: function() {
            my.Helper.addEvent(my.ImageFlowDiv, "click", my.Slideshow.interrupt)
        },
        start: function() {
            my.Helper.setClassName(my.buttonSlideshow, "slideshow pause"), my.buttonSlideshow.onclick = function() {
                my.Slideshow.stop()
            }, my.Slideshow.action = window.setInterval(my.Slideshow.slide, my.slideshowSpeed), window.setTimeout(my.Slideshow.addInterruptEvent, 100)
        },
        stop: function() {
            my.Helper.setClassName(my.buttonSlideshow, "slideshow play"), my.buttonSlideshow.onclick = function() {
                my.Slideshow.start()
            }, window.clearInterval(my.Slideshow.action)
        },
        slide: function() {
            var newImageID = my.imageID + my.Slideshow.direction,
                reverseDirection = !1;
            newImageID === my.max && (my.Slideshow.direction = -1, reverseDirection = !0), newImageID < 0 && (my.Slideshow.direction = 1, reverseDirection = !0), reverseDirection ? my.Slideshow.slide() : my.glideTo(newImageID)
        }
    }, this.MouseWheel = {
        init: function() {
            window.addEventListener && my.ImageFlowDiv.addEventListener("DOMMouseScroll", my.MouseWheel.get, !1), my.Helper.addEvent(my.ImageFlowDiv, "mousewheel", my.MouseWheel.get)
        },
        get: function(event) {
            var delta = 0;
            event || (event = window.event), event.wheelDelta ? delta = event.wheelDelta / 120 : event.detail && (delta = -event.detail / 3), delta && my.MouseWheel.handle(delta), my.Helper.suppressBrowserDefault(event)
        },
        handle: function(delta) {
            var change = !1,
                newImageID = 0;
            delta > 0 ? my.imageID >= 1 && (newImageID = my.imageID - 1, change = !0) : my.imageID < my.max - 1 && (newImageID = my.imageID + 1, change = !0), change && my.glideOnEvent(newImageID)
        }
    }, this.MouseDrag = {
        object: null,
        objectX: 0,
        mouseX: 0,
        newX: 0,
        busy: !1,
        init: function() {
            my.Helper.addEvent(my.ImageFlowDiv, "mousemove", my.MouseDrag.drag), my.Helper.addEvent(my.ImageFlowDiv, "mouseup", my.MouseDrag.stop), my.Helper.addEvent(document, "mouseup", my.MouseDrag.stop), my.ImageFlowDiv.onselectstart = function() {
                var selection = !0;
                return my.MouseDrag.busy && (selection = !1), selection
            }
        },
        start: function(o) {
            my.MouseDrag.object = o, my.MouseDrag.objectX = my.MouseDrag.mouseX - o.offsetLeft + my.newSliderX
        },
        stop: function() {
            my.MouseDrag.object = null, my.MouseDrag.busy = !1
        },
        drag: function(e) {
            var posx = 0;
            if (e || (e = window.event), e.pageX ? posx = e.pageX : e.clientX && (posx = e.clientX + document.body.scrollLeft + document.documentElement.scrollLeft), my.MouseDrag.mouseX = posx, null !== my.MouseDrag.object) {
                var newX = my.MouseDrag.mouseX - my.MouseDrag.objectX + my.sliderWidth;
                newX < -my.newSliderX && (newX = -my.newSliderX), newX > my.scrollbarWidth - my.newSliderX && (newX = my.scrollbarWidth - my.newSliderX);
                var step, imageID;
                my.circular ? (step = (newX + my.newSliderX) / (my.scrollbarWidth / (my.max - 2 * my.imageFocusMax - 1)), imageID = Math.round(step) + my.imageFocusMax) : (step = (newX + my.newSliderX) / (my.scrollbarWidth / (my.max - 1)), imageID = Math.round(step)), my.MouseDrag.newX = newX, my.MouseDrag.object.style.left = newX + "px", my.imageID !== imageID && my.glideOnEvent(imageID), my.MouseDrag.busy = !0
            }
        }
    }, this.Touch = {
        x: 0,
        startX: 0,
        stopX: 0,
        busy: !1,
        first: !0,
        init: function() {
            my.Helper.addEvent(my.navigationDiv, "touchstart", my.Touch.start), my.Helper.addEvent(document, "touchmove", my.Touch.handle), my.Helper.addEvent(document, "touchend", my.Touch.stop)
        },
        isOnNavigationDiv: function(e) {
            var state = !1;
            if (e.touches) {
                var target = e.touches[0].target;
                target !== my.navigationDiv && target !== my.sliderDiv && target !== my.scrollbarDiv || (state = !0)
            }
            return state
        },
        getX: function(e) {
            var x = 0;
            return e.touches && (x = e.touches[0].pageX), x
        },
        start: function(e) {
            my.Touch.startX = my.Touch.getX(e), my.Touch.busy = !0, my.Helper.suppressBrowserDefault(e)
        },
        isBusy: function() {
            var busy = !1;
            return my.Touch.busy && (busy = !0), busy
        },
        handle: function(e) {
            if (my.Touch.isBusy && my.Touch.isOnNavigationDiv(e)) {
                var max = my.circular ? my.max - 2 * my.imageFocusMax - 1 : my.max - 1;
                my.Touch.first && (my.Touch.stopX = (max - my.imageID) * (my.imagesDivWidth / max), my.Touch.first = !1);
                var newX = -(my.Touch.getX(e) - my.Touch.startX - my.Touch.stopX);
                newX < 0 && (newX = 0), newX > my.imagesDivWidth && (newX = my.imagesDivWidth), my.Touch.x = newX;
                var imageID = Math.round(newX / (my.imagesDivWidth / max));
                imageID = max - imageID, my.imageID !== imageID && (my.circular && (imageID += my.imageFocusMax), my.glideOnEvent(imageID)), my.Helper.suppressBrowserDefault(e)
            }
        },
        stop: function() {
            my.Touch.stopX = my.Touch.x, my.Touch.busy = !1
        }
    }, this.Key = {
        init: function() {
            document.onkeydown = function(event) {
                my.Key.handle(event)
            }
        },
        handle: function(event) {
            var charCode = my.Key.get(event);
            switch (charCode) {
                case 39:
                    my.MouseWheel.handle(-1);
                    break;
                case 37:
                    my.MouseWheel.handle(1)
            }
        },
        get: function(event) {
            return event = event || window.event, event.keyCode
        }
    }, this.Helper = {
        addEvent: function(obj, type, fn) {
            obj.addEventListener ? obj.addEventListener(type, fn, !1) : obj.attachEvent && (obj["e" + type + fn] = fn, obj[type + fn] = function() {
                obj["e" + type + fn](window.event)
            }, obj.attachEvent("on" + type, obj[type + fn]))
        },
        removeEvent: function(obj, type, fn) {
            obj.removeEventListener ? obj.removeEventListener(type, fn, !1) : obj.detachEvent && (void 0 === obj[type + fn] && alert("Helper.removeEvent » Pointer to detach event is undefined - perhaps you are trying to detach an unattached event?"), obj.detachEvent("on" + type, obj[type + fn]), obj[type + fn] = null, obj["e" + type + fn] = null)
        },
        setOpacity: function(object, value) {
            my.opacity === !0 && (object.style.opacity = value / 10, object.style.filter = "alpha(opacity=" + 10 * value + ")")
        },
        createDocumentElement: function(type, id, optionalClass) {
            var element = document.createElement(type);
            return element.setAttribute("id", my.ImageFlowID + "_" + id), void 0 !== optionalClass && (id += " " + optionalClass), my.Helper.setClassName(element, id), element
        },
        setClassName: function(element, className) {
            element && (element.setAttribute("class", className), element.setAttribute("className", className))
        },
        suppressBrowserDefault: function(e) {
            return e.preventDefault ? e.preventDefault() : e.returnValue = !1, !1
        },
        addResizeEvent: function() {
            var otherFunctions = window.onresize;
            "function" != typeof window.onresize ? window.onresize = function() {
                my.refresh()
            } : window.onresize = function() {
                otherFunctions && otherFunctions(), my.refresh()
            }
        }
    }
}

function getUrlVars() {
    for (var hash, vars = [], hashes = window.location.href.slice(window.location.href.indexOf("?") + 1).split("&"), i = 0; i < hashes.length; i++) hash = hashes[i].split("="), vars.push(hash[0]), vars[hash[0]] = hash[1];
    return vars
}

function cashbackcontent(imageID) {
    var content = null;
    switch (parseInt(imageID)) {
        case 1:
            content = document.getElementById("cash-back-premiere-club-content").innerHTML;
            break;
        case 2:
            content = document.getElementById("cash-back-elite-club-content").innerHTML;
            break;
        case 3:
            content = document.getElementById("cash-back-millionaires-club-content").innerHTML
    }
    return document.getElementById("contentcashbackclub").innerHTML = content, !1
}

function slotscontent(imageID) {
    var content = null;
    if (imageID > 3) return !1;
    switch (parseInt(imageID)) {
        case 1:
            content = document.getElementById("slots-prestige-club-content").innerHTML;
            break;
        case 2:
            content = document.getElementById("slots-premium-club-content").innerHTML;
            break;
        case 3:
            content = document.getElementById("slots-masters-club-content").innerHTML
    }
    return null != content && (document.getElementById("contentslotsclub").innerHTML = content, !0)
}

function loyaltycontent(imageID) {
        var content = null;
        if (imageID > 5) return !1;
        switch (parseInt(imageID)) {
            case 1:
                content = document.getElementById("loyalty-bronze-club-content").innerHTML;
                break;
            case 2:
                content = document.getElementById("loyalty-silver-club-content").innerHTML;
                break;
            case 3:
                content = document.getElementById("loyalty-gold-club-content").innerHTML;
                break;
            case 4:
                content = document.getElementById("loyalty-platinum-club-content").innerHTML;
                break;
            case 5:
                content = document.getElementById("loyalty-diamond-club-content").innerHTML
        }
        return null != content && (document.getElementById("contentloyaltyclub").innerHTML = content, !0)
    }! function(t, e) {
        function i(t, e, i) {
            return [parseFloat(t[0]) * (p.test(t[0]) ? e / 100 : 1), parseFloat(t[1]) * (p.test(t[1]) ? i / 100 : 1)]
        }

        function s(e, i) {
            return parseInt(t.css(e, i), 10) || 0
        }

        function n(e) {
            var i = e[0];
            return 9 === i.nodeType ? {
                width: e.width(),
                height: e.height(),
                offset: {
                    top: 0,
                    left: 0
                }
            } : t.isWindow(i) ? {
                width: e.width(),
                height: e.height(),
                offset: {
                    top: e.scrollTop(),
                    left: e.scrollLeft()
                }
            } : i.preventDefault ? {
                width: 0,
                height: 0,
                offset: {
                    top: i.pageY,
                    left: i.pageX
                }
            } : {
                width: e.outerWidth(),
                height: e.outerHeight(),
                offset: e.offset()
            }
        }
        t.ui = t.ui || {};
        var a, o = Math.max,
            r = Math.abs,
            h = Math.round,
            l = /left|center|right/,
            c = /top|center|bottom/,
            u = /[\+\-]\d+(\.[\d]+)?%?/,
            d = /^\w+/,
            p = /%$/,
            f = t.fn.position;
        t.position = {
                scrollbarWidth: function() {
                    if (a !== e) return a;
                    var i, s, n = t("<div style='display:block;width:50px;height:50px;overflow:hidden;'><div style='height:100px;width:auto;'></div></div>"),
                        o = n.children()[0];
                    return t("body").append(n), i = o.offsetWidth, n.css("overflow", "scroll"), s = o.offsetWidth, i === s && (s = n[0].clientWidth), n.remove(), a = i - s
                },
                getScrollInfo: function(e) {
                    var i = e.isWindow ? "" : e.element.css("overflow-x"),
                        s = e.isWindow ? "" : e.element.css("overflow-y"),
                        n = "scroll" === i || "auto" === i && e.width < e.element[0].scrollWidth,
                        a = "scroll" === s || "auto" === s && e.height < e.element[0].scrollHeight;
                    return {
                        width: a ? t.position.scrollbarWidth() : 0,
                        height: n ? t.position.scrollbarWidth() : 0
                    }
                },
                getWithinInfo: function(e) {
                    var i = t(e || window),
                        s = t.isWindow(i[0]);
                    return {
                        element: i,
                        isWindow: s,
                        offset: i.offset() || {
                            left: 0,
                            top: 0
                        },
                        scrollLeft: i.scrollLeft(),
                        scrollTop: i.scrollTop(),
                        width: s ? i.width() : i.outerWidth(),
                        height: s ? i.height() : i.outerHeight()
                    }
                }
            }, t.fn.position = function(e) {
                if (!e || !e.of) return f.apply(this, arguments);
                e = t.extend({}, e);
                var a, p, m, g, v, _, b = t(e.of),
                    y = t.position.getWithinInfo(e.within),
                    w = t.position.getScrollInfo(y),
                    x = (e.collision || "flip").split(" "),
                    k = {};
                return _ = n(b), b[0].preventDefault && (e.at = "left top"), p = _.width, m = _.height, g = _.offset, v = t.extend({}, g), t.each(["my", "at"], function() {
                    var t, i, s = (e[this] || "").split(" ");
                    1 === s.length && (s = l.test(s[0]) ? s.concat(["center"]) : c.test(s[0]) ? ["center"].concat(s) : ["center", "center"]), s[0] = l.test(s[0]) ? s[0] : "center", s[1] = c.test(s[1]) ? s[1] : "center", t = u.exec(s[0]), i = u.exec(s[1]), k[this] = [t ? t[0] : 0, i ? i[0] : 0], e[this] = [d.exec(s[0])[0], d.exec(s[1])[0]]
                }), 1 === x.length && (x[1] = x[0]), "right" === e.at[0] ? v.left += p : "center" === e.at[0] && (v.left += p / 2), "bottom" === e.at[1] ? v.top += m : "center" === e.at[1] && (v.top += m / 2), a = i(k.at, p, m), v.left += a[0], v.top += a[1], this.each(function() {
                    var n, l, c = t(this),
                        u = c.outerWidth(),
                        d = c.outerHeight(),
                        f = s(this, "marginLeft"),
                        _ = s(this, "marginTop"),
                        D = u + f + s(this, "marginRight") + w.width,
                        T = d + _ + s(this, "marginBottom") + w.height,
                        C = t.extend({}, v),
                        M = i(k.my, c.outerWidth(), c.outerHeight());
                    "right" === e.my[0] ? C.left -= u : "center" === e.my[0] && (C.left -= u / 2), "bottom" === e.my[1] ? C.top -= d : "center" === e.my[1] && (C.top -= d / 2), C.left += M[0], C.top += M[1], t.support.offsetFractions || (C.left = h(C.left), C.top = h(C.top)), n = {
                        marginLeft: f,
                        marginTop: _
                    }, t.each(["left", "top"], function(i, s) {
                        t.ui.position[x[i]] && t.ui.position[x[i]][s](C, {
                            targetWidth: p,
                            targetHeight: m,
                            elemWidth: u,
                            elemHeight: d,
                            collisionPosition: n,
                            collisionWidth: D,
                            collisionHeight: T,
                            offset: [a[0] + M[0], a[1] + M[1]],
                            my: e.my,
                            at: e.at,
                            within: y,
                            elem: c
                        })
                    }), e.using && (l = function(t) {
                        var i = g.left - C.left,
                            s = i + p - u,
                            n = g.top - C.top,
                            a = n + m - d,
                            h = {
                                target: {
                                    element: b,
                                    left: g.left,
                                    top: g.top,
                                    width: p,
                                    height: m
                                },
                                element: {
                                    element: c,
                                    left: C.left,
                                    top: C.top,
                                    width: u,
                                    height: d
                                },
                                horizontal: 0 > s ? "left" : i > 0 ? "right" : "center",
                                vertical: 0 > a ? "top" : n > 0 ? "bottom" : "middle"
                            };
                        u > p && p > r(i + s) && (h.horizontal = "center"), d > m && m > r(n + a) && (h.vertical = "middle"), h.important = o(r(i), r(s)) > o(r(n), r(a)) ? "horizontal" : "vertical", e.using.call(this, t, h)
                    }), c.offset(t.extend(C, {
                        using: l
                    }))
                })
            }, t.ui.position = {
                fit: {
                    left: function(t, e) {
                        var i, s = e.within,
                            n = s.isWindow ? s.scrollLeft : s.offset.left,
                            a = s.width,
                            r = t.left - e.collisionPosition.marginLeft,
                            h = n - r,
                            l = r + e.collisionWidth - a - n;
                        e.collisionWidth > a ? h > 0 && 0 >= l ? (i = t.left + h + e.collisionWidth - a - n, t.left += h - i) : t.left = l > 0 && 0 >= h ? n : h > l ? n + a - e.collisionWidth : n : h > 0 ? t.left += h : l > 0 ? t.left -= l : t.left = o(t.left - r, t.left)
                    },
                    top: function(t, e) {
                        var i, s = e.within,
                            n = s.isWindow ? s.scrollTop : s.offset.top,
                            a = e.within.height,
                            r = t.top - e.collisionPosition.marginTop,
                            h = n - r,
                            l = r + e.collisionHeight - a - n;
                        e.collisionHeight > a ? h > 0 && 0 >= l ? (i = t.top + h + e.collisionHeight - a - n, t.top += h - i) : t.top = l > 0 && 0 >= h ? n : h > l ? n + a - e.collisionHeight : n : h > 0 ? t.top += h : l > 0 ? t.top -= l : t.top = o(t.top - r, t.top)
                    }
                },
                flip: {
                    left: function(t, e) {
                        var i, s, n = e.within,
                            a = n.offset.left + n.scrollLeft,
                            o = n.width,
                            h = n.isWindow ? n.scrollLeft : n.offset.left,
                            l = t.left - e.collisionPosition.marginLeft,
                            c = l - h,
                            u = l + e.collisionWidth - o - h,
                            d = "left" === e.my[0] ? -e.elemWidth : "right" === e.my[0] ? e.elemWidth : 0,
                            p = "left" === e.at[0] ? e.targetWidth : "right" === e.at[0] ? -e.targetWidth : 0,
                            f = -2 * e.offset[0];
                        0 > c ? (i = t.left + d + p + f + e.collisionWidth - o - a, (0 > i || r(c) > i) && (t.left += d + p + f)) : u > 0 && (s = t.left - e.collisionPosition.marginLeft + d + p + f - h, (s > 0 || u > r(s)) && (t.left += d + p + f))
                    },
                    top: function(t, e) {
                        var i, s, n = e.within,
                            a = n.offset.top + n.scrollTop,
                            o = n.height,
                            h = n.isWindow ? n.scrollTop : n.offset.top,
                            l = t.top - e.collisionPosition.marginTop,
                            c = l - h,
                            u = l + e.collisionHeight - o - h,
                            d = "top" === e.my[1],
                            p = d ? -e.elemHeight : "bottom" === e.my[1] ? e.elemHeight : 0,
                            f = "top" === e.at[1] ? e.targetHeight : "bottom" === e.at[1] ? -e.targetHeight : 0,
                            m = -2 * e.offset[1];
                        0 > c ? (s = t.top + p + f + m + e.collisionHeight - o - a, t.top + p + f + m > c && (0 > s || r(c) > s) && (t.top += p + f + m)) : u > 0 && (i = t.top - e.collisionPosition.marginTop + p + f + m - h, t.top + p + f + m > u && (i > 0 || u > r(i)) && (t.top += p + f + m))
                    }
                },
                flipfit: {
                    left: function() {
                        t.ui.position.flip.left.apply(this, arguments), t.ui.position.fit.left.apply(this, arguments)
                    },
                    top: function() {
                        t.ui.position.flip.top.apply(this, arguments), t.ui.position.fit.top.apply(this, arguments)
                    }
                }
            },
            function() {
                var e, i, s, n, a, o = document.getElementsByTagName("body")[0],
                    r = document.createElement("div");
                e = document.createElement(o ? "div" : "body"), s = {
                    visibility: "hidden",
                    width: 0,
                    height: 0,
                    border: 0,
                    margin: 0,
                    background: "none"
                }, o && t.extend(s, {
                    position: "absolute",
                    left: "-1000px",
                    top: "-1000px"
                });
                for (a in s) e.style[a] = s[a];
                e.appendChild(r), i = o || document.documentElement, i.insertBefore(e, i.firstChild), r.style.cssText = "position: absolute; left: 10.7432222px;", n = t(r).offset().left, t.support.offsetFractions = n > 10 && 11 > n, e.innerHTML = "", i.removeChild(e)
            }()
    }(jQuery),
    function(t) {
        function e(e, i) {
            var s = (e.attr("aria-describedby") || "").split(/\s+/);
            s.push(i), e.data("ui-tooltip-id", i).attr("aria-describedby", t.trim(s.join(" ")))
        }

        function i(e) {
            var i = e.data("ui-tooltip-id"),
                s = (e.attr("aria-describedby") || "").split(/\s+/),
                n = t.inArray(i, s); - 1 !== n && s.splice(n, 1), e.removeData("ui-tooltip-id"), s = t.trim(s.join(" ")), s ? e.attr("aria-describedby", s) : e.removeAttr("aria-describedby")
        }
        var s = 0;
        t.widget("ui.tooltip", {
            version: "1.10.2",
            options: {
                content: function() {
                    var e = t(this).attr("title") || "";
                    return t("<a>").text(e).html()
                },
                hide: !0,
                items: "[title]:not([disabled])",
                position: {
                    my: "left top+15",
                    at: "left bottom",
                    collision: "flipfit flip"
                },
                show: !0,
                tooltipClass: null,
                track: !1,
                close: null,
                open: null
            },
            _create: function() {
                this._on({
                    mouseover: "open",
                    focusin: "open"
                }), this.tooltips = {}, this.parents = {}, this.options.disabled && this._disable()
            },
            _setOption: function(e, i) {
                var s = this;
                return "disabled" === e ? (this[i ? "_disable" : "_enable"](), void(this.options[e] = i)) : (this._super(e, i), void("content" === e && t.each(this.tooltips, function(t, e) {
                    s._updateContent(e)
                })))
            },
            _disable: function() {
                var e = this;
                t.each(this.tooltips, function(i, s) {
                    var n = t.Event("blur");
                    n.target = n.currentTarget = s[0], e.close(n, !0)
                }), this.element.find(this.options.items).addBack().each(function() {
                    var e = t(this);
                    e.is("[title]") && e.data("ui-tooltip-title", e.attr("title")).attr("title", "")
                })
            },
            _enable: function() {
                this.element.find(this.options.items).addBack().each(function() {
                    var e = t(this);
                    e.data("ui-tooltip-title") && e.attr("title", e.data("ui-tooltip-title"))
                })
            },
            open: function(e) {
                var i = this,
                    s = t(e ? e.target : this.element).closest(this.options.items);
                s.length && !s.data("ui-tooltip-id") && (s.attr("title") && s.data("ui-tooltip-title", s.attr("title")), s.data("ui-tooltip-open", !0), e && "mouseover" === e.type && s.parents().each(function() {
                    var e, s = t(this);
                    s.data("ui-tooltip-open") && (e = t.Event("blur"), e.target = e.currentTarget = this, i.close(e, !0)), s.attr("title") && (s.uniqueId(), i.parents[this.id] = {
                        element: this,
                        title: s.attr("title")
                    }, s.attr("title", ""))
                }), this._updateContent(s, e))
            },
            _updateContent: function(t, e) {
                var i, s = this.options.content,
                    n = this,
                    a = e ? e.type : null;
                return "string" == typeof s ? this._open(e, t, s) : (i = s.call(t[0], function(i) {
                    t.data("ui-tooltip-open") && n._delay(function() {
                        e && (e.type = a), this._open(e, t, i)
                    })
                }), void(i && this._open(e, t, i)))
            },
            _open: function(i, s, n) {
                function a(t) {
                    l.of = t, o.is(":hidden") || o.position(l)
                }
                var o, r, h, l = t.extend({}, this.options.position);
                if (n) {
                    if (o = this._find(s), o.length) return void o.find(".ui-tooltip-content").html(n);
                    s.is("[title]") && (i && "mouseover" === i.type ? s.attr("title", "") : s.removeAttr("title")), o = this._tooltip(s), e(s, o.attr("id")), o.find(".ui-tooltip-content").html(n), this.options.track && i && /^mouse/.test(i.type) ? (this._on(this.document, {
                        mousemove: a
                    }), a(i)) : o.position(t.extend({
                        of: s
                    }, this.options.position)), o.hide(), this._show(o, this.options.show), this.options.show && this.options.show.delay && (h = this.delayedShow = setInterval(function() {
                        o.is(":visible") && (a(l.of), clearInterval(h))
                    }, t.fx.interval)), this._trigger("open", i, {
                        tooltip: o
                    }), r = {
                        keyup: function(e) {
                            if (e.keyCode === t.ui.keyCode.ESCAPE) {
                                var i = t.Event(e);
                                i.currentTarget = s[0], this.close(i, !0)
                            }
                        },
                        remove: function() {
                            this._removeTooltip(o)
                        }
                    }, i && "mouseover" !== i.type || (r.mouseleave = "close"), i && "focusin" !== i.type || (r.focusout = "close"), this._on(!0, s, r)
                }
            },
            close: function(e) {
                var s = this,
                    n = t(e ? e.currentTarget : this.element),
                    a = this._find(n);
                this.closing || (clearInterval(this.delayedShow), n.data("ui-tooltip-title") && n.attr("title", n.data("ui-tooltip-title")), i(n), a.stop(!0), this._hide(a, this.options.hide, function() {
                    s._removeTooltip(t(this))
                }), n.removeData("ui-tooltip-open"), this._off(n, "mouseleave focusout keyup"), n[0] !== this.element[0] && this._off(n, "remove"), this._off(this.document, "mousemove"), e && "mouseleave" === e.type && t.each(this.parents, function(e, i) {
                    t(i.element).attr("title", i.title), delete s.parents[e]
                }), this.closing = !0, this._trigger("close", e, {
                    tooltip: a
                }), this.closing = !1)
            },
            _tooltip: function(e) {
                var i = "ui-tooltip-" + s++,
                    n = t("<div>").attr({
                        id: i,
                        role: "tooltip"
                    }).addClass("ui-tooltip ui-widget ui-corner-all ui-widget-content " + (this.options.tooltipClass || ""));
                return t("<div>").addClass("ui-tooltip-content").appendTo(n), n.appendTo(this.document[0].body), this.tooltips[i] = e, n
            },
            _find: function(e) {
                var i = e.data("ui-tooltip-id");
                return i ? t("#" + i) : t()
            },
            _removeTooltip: function(t) {
                t.remove(), delete this.tooltips[t.attr("id")]
            },
            _destroy: function() {
                var e = this;
                t.each(this.tooltips, function(i, s) {
                    var n = t.Event("blur");
                    n.target = n.currentTarget = s[0], e.close(n, !0), t("#" + i).remove(), s.data("ui-tooltip-title") && (s.attr("title", s.data("ui-tooltip-title")), s.removeData("ui-tooltip-title"))
                })
            }
        })
    }(jQuery), ! function() {
        function h(p) {
            console.log("$f.fireEvent", [].slice.call(p))
        }

        function l(r) {
            if (!r || "object" != typeof r) return r;
            var p = new r.constructor;
            for (var q in r) r.hasOwnProperty(q) && (p[q] = l(r[q]));
            return p
        }

        function n(u, r) {
            if (u) {
                var p, q = 0,
                    s = u.length;
                if (void 0 === s) {
                    for (p in u)
                        if (r.call(u[p], p, u[p]) === !1) break
                } else
                    for (var t = u[0]; q < s && r.call(t, q, t) !== !1; t = u[++q]);
                return u
            }
        }

        function c(p) {
            return document.getElementById(p)
        }

        function j(r, q, p) {
            return "object" != typeof q ? r : (r && q && n(q, function(s, t) {
                p && "function" == typeof t || (r[s] = t)
            }), r)
        }

        function o(t) {
            var r = t.indexOf(".");
            if (r != -1) {
                var q = t.slice(0, r) || "*",
                    p = t.slice(r + 1, t.length),
                    s = [];
                return n(document.getElementsByTagName(q), function() {
                    this.className && this.className.indexOf(p) != -1 && s.push(this)
                }), s
            }
        }

        function g(p) {
            return p = p || window.event, p.preventDefault ? (p.stopPropagation(), p.preventDefault()) : (p.returnValue = !1, p.cancelBubble = !0), !1
        }

        function k(r, p, q) {
            r[p] = r[p] || [], r[p].push(q)
        }

        function e(p) {
            return p.replace(/&amp;/g, "%26").replace(/&/g, "%26").replace(/=/g, "%3D")
        }

        function f() {
            return "_" + ("" + Math.random()).slice(2, 10)
        }

        function b(r, H, u) {
            function C() {
                function L(M) {
                    return !(!/iPad|iPhone|iPod/i.test(navigator.userAgent) || /.flv$/i.test(G[0].url) || K()) || (x.isLoaded() || x._fireEvent("onBeforeClick") === !1 || x.load(), g(M))
                }

                function K() {
                    return x.hasiPadSupport && x.hasiPadSupport()
                }

                function I() {
                    "" !== v.replace(/\s/g, "") ? r.addEventListener ? r.addEventListener("click", L, !1) : r.attachEvent && r.attachEvent("onclick", L) : (r.addEventListener && !K() && r.addEventListener("click", g, !1), x.load())
                }
                $f(r) ? ($f(r).getParent().innerHTML = "", q = $f(r).getIndex(), a[q] = x) : (a.push(x), q = a.length - 1), B = parseInt(r.style.height, 10) || r.clientHeight, F = r.id || "fp" + f(), s = H.id || F + "_api", H.id = s, v = r.innerHTML, "string" == typeof u && (u = {
                    clip: {
                        url: u
                    }
                }), u.playerId = F, u.clip = u.clip || {}, r.getAttribute("href", 2) && !u.clip.url && (u.clip.url = r.getAttribute("href", 2)), u.clip.url && (u.clip.url = e(u.clip.url)), t = new i(u.clip, (-1), x), u.playlist = u.playlist || [u.clip];
                var J = 0;
                n(u.playlist, function() {
                    var M = this;
                    "object" == typeof M && M.length && (M = {
                        url: "" + M
                    }), M.url && (M.url = e(M.url)), n(u.clip, function(N, O) {
                        void 0 !== O && void 0 === M[N] && "function" != typeof O && (M[N] = O)
                    }), u.playlist[J] = M, M = new i(M, J, x), G.push(M), J++
                }), n(u, function(M, N) {
                    "function" == typeof N && (t[M] ? t[M](N) : k(y, M, N), delete u[M])
                }), n(u.plugins, function(M, N) {
                    N && (z[M] = new m(M, N, x))
                }), u.plugins && void 0 !== u.plugins.controls || (z.controls = new m("controls", null, x)), z.canvas = new m("canvas", null, x), v = r.innerHTML, setTimeout(I, 0)
            }
            var v, t, F, s, q, D, p, B, x = this,
                w = null,
                E = !1,
                G = [],
                z = {},
                y = {};
            if (j(x, {
                    id: function() {
                        return F
                    },
                    isLoaded: function() {
                        return null !== w && void 0 !== w.fp_play && !E
                    },
                    getParent: function() {
                        return r
                    },
                    hide: function(I) {
                        return I && (r.style.height = "0px"), x.isLoaded() && (w.style.height = "0px"), x
                    },
                    show: function() {
                        return r.style.height = B + "px", x.isLoaded() && (w.style.height = p + "px"), x
                    },
                    isHidden: function() {
                        return x.isLoaded() && 0 === parseInt(w.style.height, 10)
                    },
                    load: function(K) {
                        if (!x.isLoaded() && x._fireEvent("onBeforeLoad") !== !1) {
                            var I = function() {
                                    v && !flashembed.isSupported(H.version) && (r.innerHTML = ""), K && (K.cached = !0, k(y, "onLoad", K)), flashembed(r, H, {
                                        config: u
                                    })
                                },
                                J = 0;
                            n(a, function() {
                                this.unload(function(L) {
                                    ++J == a.length && I()
                                })
                            })
                        }
                        return x
                    },
                    unload: function(K) {
                        if ("" !== v.replace(/\s/g, "")) {
                            if (x._fireEvent("onBeforeUnload") === !1) return K && K(!1), x;
                            E = !0;
                            try {
                                w && (w.fp_isFullscreen() && w.fp_toggleFullscreen(), w.fp_close(), x._fireEvent("onUnload"))
                            } catch (I) {}
                            var J = function() {
                                w = null, r.innerHTML = v, E = !1, K && K(!0)
                            };
                            /WebKit/i.test(navigator.userAgent) && !/Chrome/i.test(navigator.userAgent) ? setTimeout(J, 0) : J()
                        } else K && K(!1);
                        return x
                    },
                    getClip: function(I) {
                        return void 0 === I && (I = D), G[I]
                    },
                    getCommonClip: function() {
                        return t
                    },
                    getPlaylist: function() {
                        return G
                    },
                    getPlugin: function(I) {
                        var K = z[I];
                        if (!K && x.isLoaded()) {
                            var J = x._api().fp_getPlugin(I);
                            J && (K = new m(I, J, x), z[I] = K)
                        }
                        return K
                    },
                    getScreen: function() {
                        return x.getPlugin("screen")
                    },
                    getControls: function() {
                        return x.getPlugin("controls")._fireEvent("onUpdate")
                    },
                    getLogo: function() {
                        try {
                            return x.getPlugin("logo")._fireEvent("onUpdate")
                        } catch (I) {}
                    },
                    getPlay: function() {
                        return x.getPlugin("play")._fireEvent("onUpdate")
                    },
                    getConfig: function(I) {
                        return I ? l(u) : u
                    },
                    getFlashParams: function() {
                        return H
                    },
                    loadPlugin: function(L, K, N, M) {
                        "function" == typeof N && (M = N, N = {});
                        var J = M ? f() : "_";
                        x._api().fp_loadPlugin(L, K, N, J);
                        var I = {};
                        I[J] = M;
                        var O = new m(L, null, x, I);
                        return z[L] = O, O
                    },
                    getState: function() {
                        return x.isLoaded() ? w.fp_getState() : -1
                    },
                    play: function(J, I) {
                        var K = function() {
                            void 0 !== J ? x._api().fp_play(J, I) : x._api().fp_play()
                        };
                        return x.isLoaded() ? K() : E ? setTimeout(function() {
                            x.play(J, I)
                        }, 50) : x.load(function() {
                            K()
                        }), x
                    },
                    getVersion: function() {
                        var J = "flowplayer.js 3.2.12";
                        if (x.isLoaded()) {
                            var I = w.fp_getVersion();
                            return I.push(J), I
                        }
                        return J
                    },
                    _api: function() {
                        if (!x.isLoaded()) throw "Flowplayer " + x.id() + " not loaded when calling an API method";
                        return w
                    },
                    setClip: function(I) {
                        return n(I, function(J, K) {
                            "function" == typeof K ? (k(y, J, K), delete I[J]) : "onCuepoint" == J && $f(r).getCommonClip().onCuepoint(I[J][0], I[J][1])
                        }), x.setPlaylist([I]), x
                    },
                    getIndex: function() {
                        return q
                    },
                    bufferAnimate: function(I) {
                        return w.fp_bufferAnimate(void 0 === I || I), x
                    },
                    _swfHeight: function() {
                        return w.clientHeight
                    }
                }), n("Click*,Load*,Unload*,Keypress*,Volume*,Mute*,Unmute*,PlaylistReplace,ClipAdd,Fullscreen*,FullscreenExit,Error,MouseOver,MouseOut".split(","), function() {
                    var I = "on" + this;
                    if (I.indexOf("*") != -1) {
                        I = I.slice(0, I.length - 1);
                        var J = "onBefore" + I.slice(2);
                        x[J] = function(K) {
                            return k(y, J, K), x
                        }
                    }
                    x[I] = function(K) {
                        return k(y, I, K), x
                    }
                }), n("pause,resume,mute,unmute,stop,toggle,seek,getStatus,getVolume,setVolume,getTime,isPaused,isPlaying,startBuffering,stopBuffering,isFullscreen,toggleFullscreen,reset,close,setPlaylist,addClip,playFeed,setKeyboardShortcutsEnabled,isKeyboardShortcutsEnabled".split(","), function() {
                    var I = this;
                    x[I] = function(K, J) {
                        if (!x.isLoaded()) return x;
                        var L = null;
                        return L = void 0 !== K && void 0 !== J ? w["fp_" + I](K, J) : void 0 === K ? w["fp_" + I]() : w["fp_" + I](K), "undefined" === L || void 0 === L ? x : L
                    }
                }), x._fireEvent = function(R) {
                    "string" == typeof R && (R = [R]);
                    var S = R[0],
                        P = R[1],
                        N = R[2],
                        M = R[3],
                        L = 0;
                    if (u.debug && h(R), x.isLoaded() || "onLoad" != S || "player" != P || (w = w || c(s), p = x._swfHeight(), n(G, function() {
                            this._fireEvent("onLoad")
                        }), n(z, function(T, U) {
                            U._fireEvent("onUpdate")
                        }), t._fireEvent("onLoad")), "onLoad" != S || "player" == P) {
                        if ("onError" == S && ("string" == typeof P || "number" == typeof P && "number" == typeof N) && (P = N, N = M), "onContextMenu" == S) return void n(u.contextMenu[P], function(T, U) {
                            U.call(x)
                        });
                        if ("onPluginEvent" != S && "onBeforePluginEvent" != S) {
                            if ("onPlaylistReplace" == S) {
                                G = [];
                                var O = 0;
                                n(P, function() {
                                    G.push(new i(this, (O++), x))
                                })
                            }
                            if ("onClipAdd" == S) {
                                if (P.isInStream) return;
                                for (P = new i(P, N, x), G.splice(N, 0, P), L = N + 1; L < G.length; L++) G[L].index++
                            }
                            var Q = !0;
                            if ("number" == typeof P && P < G.length) {
                                D = P;
                                var K = G[P];
                                K && (Q = K._fireEvent(S, N, M)), K && Q === !1 || (Q = t._fireEvent(S, N, M, K))
                            }
                            return n(y[S], function() {
                                return Q = this.call(x, P, N), this.cached && y[S].splice(L, 1), Q !== !1 && void L++
                            }), Q
                        }
                        var I = P.name || P,
                            J = z[I];
                        if (J) return J._fireEvent("onUpdate", P), J._fireEvent(N, R.slice(3))
                    }
                }, "string" == typeof r) {
                var A = c(r);
                if (!A) throw "Flowplayer cannot access element: " + r;
                r = A, C()
            } else C()
        }

        function d(p) {
            this.length = p.length, this.each = function(r) {
                n(p, r)
            }, this.size = function() {
                return p.length
            };
            var q = this;
            for (name in b.prototype) q[name] = function() {
                var r = arguments;
                q.each(function() {
                    this[name].apply(this, r)
                })
            }
        }
        var i = function(u, s, t) {
                var r = this,
                    q = {},
                    v = {};
                if (r.index = s, "string" == typeof u && (u = {
                        url: u
                    }), j(this, u, !0), n("Begin*,Start,Pause*,Resume*,Seek*,Stop*,Finish*,LastSecond,Update,BufferFull,BufferEmpty,BufferStop".split(","), function() {
                        var w = "on" + this;
                        if (w.indexOf("*") != -1) {
                            w = w.slice(0, w.length - 1);
                            var x = "onBefore" + w.slice(2);
                            r[x] = function(y) {
                                return k(v, x, y), r
                            }
                        }
                        r[w] = function(y) {
                            return k(v, w, y), r
                        }, s == -1 && (r[x] && (t[x] = r[x]), r[w] && (t[w] = r[w]))
                    }), j(this, {
                        onCuepoint: function(y, x) {
                            if (1 == arguments.length) return q.embedded = [null, y], r;
                            "number" == typeof y && (y = [y]);
                            var w = f();
                            return q[w] = [y, x], t.isLoaded() && t._api().fp_addCuepoints(y, s, w), r
                        },
                        update: function(x) {
                            j(r, x), t.isLoaded() && t._api().fp_updateClip(x, s);
                            var w = t.getConfig(),
                                y = s == -1 ? w.clip : w.playlist[s];
                            j(y, x, !0)
                        },
                        _fireEvent: function(w, z, x, B) {
                            if ("onLoad" == w) return n(q, function(C, D) {
                                D[0] && t._api().fp_addCuepoints(D[0], s, C)
                            }), !1;
                            if (B = B || r, "onCuepoint" == w) {
                                var A = q[z];
                                if (A) return A[1].call(t, B, x)
                            }
                            z && "onBeforeBegin,onMetaData,onStart,onUpdate,onResume".indexOf(w) != -1 && (j(B, z), z.metaData && (B.duration ? B.fullDuration = z.metaData.duration : B.duration = z.metaData.duration));
                            var y = !0;
                            return n(v[w], function() {
                                y = this.call(t, B, z, x)
                            }), y
                        }
                    }), u.onCuepoint) {
                    var p = u.onCuepoint;
                    r.onCuepoint.apply(r, "function" == typeof p ? [p] : p), delete u.onCuepoint
                }
                n(u, function(w, x) {
                    "function" == typeof x && (k(v, w, x), delete u[w])
                }), s == -1 && (t.onCuepoint = this.onCuepoint)
            },
            m = function(q, s, r, u) {
                var p = this,
                    t = {},
                    v = !1;
                u && j(t, u), n(s, function(w, x) {
                    "function" == typeof x && (t[w] = x, delete s[w])
                }), j(this, {
                    animate: function(z, A, y) {
                        if (!z) return p;
                        if ("function" == typeof A && (y = A, A = 500), "string" == typeof z) {
                            var x = z;
                            z = {}, z[x] = A, A = 500
                        }
                        if (y) {
                            var w = f();
                            t[w] = y
                        }
                        return void 0 === A && (A = 500), s = r._api().fp_animate(q, z, A, w), p
                    },
                    css: function(x, y) {
                        if (void 0 !== y) {
                            var w = {};
                            w[x] = y, x = w
                        }
                        return s = r._api().fp_css(q, x), j(p, s), p
                    },
                    show: function() {
                        return this.display = "block", r._api().fp_showPlugin(q), p
                    },
                    hide: function() {
                        return this.display = "none", r._api().fp_hidePlugin(q), p
                    },
                    toggle: function() {
                        return this.display = r._api().fp_togglePlugin(q), p
                    },
                    fadeTo: function(z, y, x) {
                        if ("function" == typeof y && (x = y, y = 500), x) {
                            var w = f();
                            t[w] = x
                        }
                        return this.display = r._api().fp_fadeTo(q, z, y, w), this.opacity = z, p
                    },
                    fadeIn: function(x, w) {
                        return p.fadeTo(1, x, w)
                    },
                    fadeOut: function(x, w) {
                        return p.fadeTo(0, x, w)
                    },
                    getName: function() {
                        return q
                    },
                    getPlayer: function() {
                        return r
                    },
                    _fireEvent: function(x, w, y) {
                        if ("onUpdate" == x) {
                            var A = r._api().fp_getPlugin(q);
                            if (!A) return;
                            j(p, A), delete p.methods, v || (n(A.methods, function() {
                                var C = "" + this;
                                p[C] = function() {
                                    var D = [].slice.call(arguments),
                                        E = r._api().fp_invoke(q, C, D);
                                    return "undefined" === E || void 0 === E ? p : E
                                }
                            }), v = !0)
                        }
                        var B = t[x];
                        if (B) {
                            var z = B.apply(p, w);
                            return "_" == x.slice(0, 1) && delete t[x], z
                        }
                        return p
                    }
                })
            },
            a = [];
        window.flowplayer = window.$f = function() {
            var q = null,
                p = arguments[0];
            if (!arguments.length) return n(a, function() {
                if (this.isLoaded()) return q = this, !1
            }), q || a[0];
            if (1 == arguments.length) return "number" == typeof p ? a[p] : "*" == p ? new d(a) : (n(a, function() {
                if (this.id() == p.id || this.id() == p || this.getParent() == p) return q = this, !1
            }), q);
            if (arguments.length > 1) {
                var u = arguments[1],
                    r = 3 == arguments.length ? arguments[2] : {};
                if ("string" == typeof u && (u = {
                        src: u
                    }), u = j({
                        bgcolor: "#000000",
                        version: [10, 1],
                        expressInstall: "http://releases.flowplayer.org/swf/expressinstall.swf",
                        cachebusting: !1
                    }, u), "string" == typeof p) {
                    if (p.indexOf(".") != -1) {
                        var t = [];
                        return n(o(p), function() {
                            t.push(new b(this, l(u), l(r)))
                        }), new d(t)
                    }
                    var s = c(p);
                    return new b(null !== s ? s : l(p), l(u), l(r))
                }
                if (p) return new b(p, l(u), l(r))
            }
            return null
        }, j(window.$f, {
            fireEvent: function() {
                var q = [].slice.call(arguments),
                    r = $f(q[0]);
                return r ? r._fireEvent(q.slice(1)) : null
            },
            addPlugin: function(p, q) {
                return b.prototype[p] = q, $f
            },
            each: n,
            extend: j
        }), "function" == typeof jQuery && (jQuery.fn.flowplayer = function(r, q) {
            if (!arguments.length || "number" == typeof arguments[0]) {
                var p = [];
                return this.each(function() {
                    var s = $f(this);
                    s && p.push(s)
                }), arguments.length ? p[arguments[0]] : new d(p)
            }
            return this.each(function() {
                $f(this, l(r), q ? l(q) : {})
            })
        })
    }(), ! function() {
        function i(m, l) {
            if (l)
                for (var f in l) l.hasOwnProperty(f) && (m[f] = l[f]);
            return m
        }

        function a(f, n) {
            var m = [];
            for (var l in f) f.hasOwnProperty(l) && (m[l] = n(f[l]));
            return m
        }

        function d(f, n, m) {
            if (g.isSupported(n.version)) f.innerHTML = g.getHTML(n, m);
            else if (n.expressInstall && g.isSupported([6, 65])) f.innerHTML = g.getHTML(i(n, {
                src: n.expressInstall
            }), {
                MMredirectURL: encodeURIComponent(location.href),
                MMplayerType: "PlugIn",
                MMdoctitle: document.title
            });
            else if (f.innerHTML.replace(/\s/g, "") || (f.innerHTML = "<h2>Flash version " + n.version + " or greater is required</h2><h3>" + (k[0] > 0 ? "Your version is " + k : "You have no flash plugin installed") + "</h3>" + ("A" == f.tagName ? "<p>Click here to download latest version</p>" : "<p>Download latest version from <a href='" + j + "'>here</a></p>"), "A" != f.tagName && "DIV" != f.tagName || (f.onclick = function() {
                    location.href = j
                })), n.onFail) {
                var l = n.onFail.call(this);
                "string" == typeof l && (f.innerHTML = l)
            }
            h && (window[n.id] = document.getElementById(n.id)), i(this, {
                getRoot: function() {
                    return f
                },
                getOptions: function() {
                    return n
                },
                getConf: function() {
                    return m
                },
                getApi: function() {
                    return f.firstChild
                }
            })
        }
        var h = document.all,
            j = "http://get.adobe.com/flashplayer",
            c = "function" == typeof jQuery,
            e = /(\d+)[^\d]+(\d+)[^\d]*(\d*)/,
            b = {
                width: "100%",
                height: "100%",
                id: "_" + ("" + Math.random()).slice(9),
                allowfullscreen: !0,
                allowscriptaccess: "always",
                quality: "high",
                version: [3, 0],
                onFail: null,
                expressInstall: null,
                w3c: !1,
                cachebusting: !1
            };
        window.attachEvent && window.attachEvent("onbeforeunload", function() {
            __flash_unloadHandler = function() {}, __flash_savedUnloadHandler = function() {}
        }), window.flashembed = function(f, m, l) {
            if ("string" == typeof f && (f = document.getElementById(f.replace("#", ""))), f) return "string" == typeof m && (m = {
                src: m
            }), new d(f, i(i({}, b), m), l)
        };
        var g = i(window.flashembed, {
                conf: b,
                getVersion: function() {
                    var m, f;
                    try {
                        f = navigator.plugins["Shockwave Flash"].description.slice(16)
                    } catch (o) {
                        try {
                            m = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.7"), f = m && m.GetVariable("$version")
                        } catch (n) {
                            try {
                                m = new ActiveXObject("ShockwaveFlash.ShockwaveFlash.6"), f = m && m.GetVariable("$version")
                            } catch (l) {}
                        }
                    }
                    return f = e.exec(f), f ? [1 * f[1], 1 * f[1 * f[1] > 9 ? 2 : 3] * 1] : [0, 0]
                },
                asString: function(l) {
                    if (null === l || void 0 === l) return null;
                    var f = typeof l;
                    switch ("object" == f && l.push && (f = "array"), f) {
                        case "string":
                            return l = l.replace(new RegExp('(["\\\\])', "g"), "\\$1"), l = l.replace(/^\s?(\d+\.?\d*)%/, "$1pct"), '"' + l + '"';
                        case "array":
                            return "[" + a(l, function(o) {
                                return g.asString(o)
                            }).join(",") + "]";
                        case "function":
                            return '"function()"';
                        case "object":
                            var m = [];
                            for (var n in l) l.hasOwnProperty(n) && m.push('"' + n + '":' + g.asString(l[n]));
                            return "{" + m.join(",") + "}"
                    }
                    return String(l).replace(/\s/g, " ").replace(/\'/g, '"')
                },
                getHTML: function(o, l) {
                    o = i({}, o);
                    var n = '<object width="' + o.width + '" height="' + o.height + '" id="' + o.id + '" name="' + o.id + '"';
                    o.cachebusting && (o.src += (o.src.indexOf("?") != -1 ? "&" : "?") + Math.random()), n += o.w3c || !h ? ' data="' + o.src + '" type="application/x-shockwave-flash"' : ' classid="clsid:D27CDB6E-AE6D-11cf-96B8-444553540000"', n += ">", (o.w3c || h) && (n += '<param name="movie" value="' + o.src + '" />'), o.width = o.height = o.id = o.w3c = o.src = null, o.onFail = o.version = o.expressInstall = null;
                    for (var m in o) o[m] && (n += '<param name="' + m + '" value="' + o[m] + '" />');
                    var p = "";
                    if (l) {
                        for (var f in l)
                            if (l[f]) {
                                var q = l[f];
                                p += f + "=" + (/function|object/.test(typeof q) ? g.asString(q) : q) + "&"
                            }
                        p = p.slice(0, -1), n += '<param name="flashvars" value=\'' + p + "' />"
                    }
                    return n += "</object>"
                },
                isSupported: function(f) {
                    return k[0] > f[0] || k[0] == f[0] && k[1] >= f[1]
                }
            }),
            k = g.getVersion();
        c && (jQuery.tools = jQuery.tools || {
            version: "3.2.12"
        }, jQuery.tools.flashembed = {
            conf: b
        }, jQuery.fn.flashembed = function(l, f) {
            return this.each(function() {
                $(this).data("flashembed", flashembed(this, l, f))
            })
        })
    }(), ! function($) {
        var plugin = {},
            defaults = {
                mode: "horizontal",
                slideSelector: "",
                infiniteLoop: !0,
                hideControlOnEnd: !1,
                speed: 500,
                easing: null,
                slideMargin: 0,
                startSlide: 0,
                randomStart: !1,
                captions: !1,
                ticker: !1,
                tickerHover: !1,
                adaptiveHeight: !1,
                adaptiveHeightSpeed: 500,
                video: !1,
                useCSS: !0,
                preloadImages: "visible",
                responsive: !0,
                slideZIndex: 50,
                wrapperClass: "bx-wrapper",
                touchEnabled: !0,
                swipeThreshold: 50,
                oneToOneTouch: !0,
                preventDefaultSwipeX: !0,
                preventDefaultSwipeY: !1,
                pager: !0,
                pagerType: "full",
                pagerShortSeparator: " / ",
                pagerSelector: null,
                buildPager: null,
                pagerCustom: null,
                controls: !0,
                nextText: "Next",
                prevText: "Prev",
                nextSelector: null,
                prevSelector: null,
                autoControls: !1,
                startText: "Start",
                stopText: "Stop",
                autoControlsCombine: !1,
                autoControlsSelector: null,
                auto: !1,
                pause: 4e3,
                autoStart: !0,
                autoDirection: "next",
                autoHover: !1,
                autoDelay: 0,
                autoSlideForOnePage: !1,
                stopAuto: !0,
                isCarouselPager: !1,
                carouselPager: {
                    Instance: null,
                    Options: null
                },
                pagerGroupings: [],
                minSlides: 1,
                maxSlides: 1,
                moveSlides: 0,
                slideWidth: 0,
                onSliderLoad: function() {},
                onSlideBefore: function() {},
                onSlideAfter: function() {},
                onSlideNext: function() {},
                onSlidePrev: function() {},
                onSliderResize: function() {}
            };
        $.fn.bxSlider = function(options) {
            if (0 == this.length) return this;
            if (this.length > 1) return this.each(function() {
                $(this).bxSlider(options)
            }), this;
            var slider = {},
                el = this;
            plugin.el = this;
            var windowWidth = $(window).width(),
                windowHeight = $(window).height(),
                init = function() {
                    slider.settings = $.extend({}, defaults, options), slider.settings.slideWidth = parseInt(slider.settings.slideWidth), slider.children = el.children(slider.settings.slideSelector), slider.children.length < slider.settings.minSlides && (slider.settings.minSlides = slider.children.length), slider.children.length < slider.settings.maxSlides && (slider.settings.maxSlides = slider.children.length), slider.settings.randomStart && (slider.settings.startSlide = Math.floor(Math.random() * slider.children.length)), slider.active = {
                        index: slider.settings.startSlide
                    }, slider.carousel = slider.settings.minSlides > 1 || slider.settings.maxSlides > 1, slider.carousel && (slider.settings.preloadImages = "all"), slider.minThreshold = slider.settings.minSlides * slider.settings.slideWidth + (slider.settings.minSlides - 1) * slider.settings.slideMargin, slider.maxThreshold = slider.settings.maxSlides * slider.settings.slideWidth + (slider.settings.maxSlides - 1) * slider.settings.slideMargin, slider.working = !1, slider.controls = {}, slider.interval = null, slider.animProp = "vertical" == slider.settings.mode ? "top" : "left", slider.usingCSS = slider.settings.useCSS && "fade" != slider.settings.mode && function() {
                        var div = document.createElement("div"),
                            props = ["WebkitPerspective", "MozPerspective", "OPerspective", "msPerspective"];
                        for (var i in props)
                            if (void 0 !== div.style[props[i]]) return slider.cssPrefix = props[i].replace("Perspective", "").toLowerCase(), slider.animProp = "-" + slider.cssPrefix + "-transform", !0;
                        return !1
                    }(), "vertical" == slider.settings.mode && (slider.settings.maxSlides = slider.settings.minSlides), el.data("origStyle", el.attr("style")), el.children(slider.settings.slideSelector).each(function() {
                        $(this).data("origStyle", $(this).attr("style"))
                    }), setup()
                },
                setup = function() {
                    el.wrap('<div class="' + slider.settings.wrapperClass + '"><div class="bx-viewport"></div></div>'), slider.viewport = el.parent(), slider.loader = $('<div class="bx-loading" />'), slider.viewport.prepend(slider.loader), el.css({
                        width: "horizontal" == slider.settings.mode ? 100 * slider.children.length + 215 + "%" : "auto",
                        position: "relative"
                    }), slider.usingCSS && slider.settings.easing ? el.css("-" + slider.cssPrefix + "-transition-timing-function", slider.settings.easing) : slider.settings.easing || (slider.settings.easing = "swing"), getNumberSlidesShowing(), slider.viewport.css({
                        width: "100%",
                        overflow: "hidden",
                        position: "relative"
                    }), slider.viewport.parent().css({
                        maxWidth: getViewportMaxWidth()
                    }), slider.settings.pager || slider.viewport.parent().css({
                        margin: "0 auto 0px"
                    }), slider.children.css({
                        "float": "horizontal" == slider.settings.mode ? "left" : "none",
                        listStyle: "none",
                        position: "relative"
                    }), slider.children.css("width", getSlideWidth()), "horizontal" == slider.settings.mode && slider.settings.slideMargin > 0 && slider.children.css("marginRight", slider.settings.slideMargin), "vertical" == slider.settings.mode && slider.settings.slideMargin > 0 && slider.children.css("marginBottom", slider.settings.slideMargin), "fade" == slider.settings.mode && (slider.children.css({
                        position: "absolute",
                        zIndex: 0,
                        display: "none"
                    }), slider.children.eq(slider.settings.startSlide).css({
                        zIndex: slider.settings.slideZIndex,
                        display: "block"
                    })), slider.controls.el = $('<div class="bx-controls" />'), slider.settings.captions && appendCaptions(), slider.active.last = slider.settings.startSlide == getPagerQty() - 1, slider.settings.video && el.fitVids();
                    var preloadSelector = slider.children.eq(slider.settings.startSlide);
                    "all" == slider.settings.preloadImages && (preloadSelector = slider.children), slider.settings.ticker ? slider.settings.pager = !1 : (slider.settings.pager && appendPager(), slider.settings.controls && appendControls(), slider.settings.auto && slider.settings.autoControls && appendControlsAuto(), (slider.settings.controls || slider.settings.autoControls || slider.settings.pager) && slider.viewport.after(slider.controls.el)), slider.settings.isCarouselPager && el.children("li").each(function(i, e) {
                        $("a", e).attr("data-slide-index", i)
                    }), loadElements(preloadSelector, start)
                },
                loadElements = function(selector, callback) {
                    var total = selector.find("img, iframe").length;
                    if (0 == total) return void callback();
                    var count = 0;
                    selector.find("img, iframe").each(function() {
                        $(this).one("load", function() {
                            ++count == total && callback()
                        }).each(function() {
                            this.complete && $(this).load()
                        })
                    })
                },
                start = function() {
                    if (slider.settings.infiniteLoop && "fade" != slider.settings.mode && !slider.settings.ticker) {
                        var slice = "vertical" == slider.settings.mode ? slider.settings.minSlides : slider.settings.maxSlides,
                            sliceAppend = slider.children.slice(0, slice).clone().addClass("bx-clone"),
                            slicePrepend = slider.children.slice(-slice).clone().addClass("bx-clone");
                        el.append(sliceAppend).prepend(slicePrepend)
                    }
                    if (slider.loader.remove(), setSlidePosition(), "vertical" == slider.settings.mode && (slider.settings.adaptiveHeight = !0), slider.viewport.height(getViewportHeight()), el.redrawSlider(), 0 == slider.settings.isCarouselPager && null !== slider.settings.carouselPager.Instance)
                        for (var totalSlides = slider.settings.carouselPager.Instance.getSlideCount(), pageCount = 0, pagerIndex = 0, i = 0; totalSlides > i; i++) pageCount == slider.settings.carouselPager.Options.maxSlides && (pagerIndex++, pageCount = 0), slider.settings.pagerGroupings[i] = pagerIndex, pageCount++;
                    slider.settings.onSliderLoad(slider.active.index), slider.initialized = !0, slider.settings.responsive && $(window).bind("resize", resizeWindow), slider.settings.auto && slider.settings.autoStart && (getPagerQty() > 1 || slider.settings.autoSlideForOnePage) && initAuto(), slider.settings.ticker && initTicker(), slider.settings.pager && updatePagerActive(slider.settings.startSlide), slider.settings.controls && updateDirectionControls(), slider.settings.touchEnabled && !slider.settings.ticker && initTouch()
                },
                getViewportHeight = function() {
                    var height = 0,
                        children = $();
                    if ("vertical" == slider.settings.mode || slider.settings.adaptiveHeight)
                        if (slider.carousel) {
                            var currentIndex = 1 == slider.settings.moveSlides ? slider.active.index : slider.active.index * getMoveBy();
                            for (children = slider.children.eq(currentIndex), i = 1; i <= slider.settings.maxSlides - 1; i++) children = currentIndex + i >= slider.children.length ? children.add(slider.children.eq(i - 1)) : children.add(slider.children.eq(currentIndex + i))
                        } else children = slider.children.eq(slider.active.index);
                    else children = slider.children;
                    return "vertical" == slider.settings.mode ? (children.each(function(index) {
                        height += $(this).outerHeight()
                    }), slider.settings.slideMargin > 0 && (height += slider.settings.slideMargin * (slider.settings.minSlides - 1))) : height = Math.max.apply(Math, children.map(function() {
                        return $(this).outerHeight(!1)
                    }).get()), "border-box" == slider.viewport.css("box-sizing") ? height += parseFloat(slider.viewport.css("padding-top")) + parseFloat(slider.viewport.css("padding-bottom")) + parseFloat(slider.viewport.css("border-top-width")) + parseFloat(slider.viewport.css("border-bottom-width")) : "padding-box" == slider.viewport.css("box-sizing") && (height += parseFloat(slider.viewport.css("padding-top")) + parseFloat(slider.viewport.css("padding-bottom"))), height
                },
                getViewportMaxWidth = function() {
                    var width = "100%";
                    return slider.settings.slideWidth > 0 && (width = "horizontal" == slider.settings.mode ? slider.settings.maxSlides * slider.settings.slideWidth + (slider.settings.maxSlides - 1) * slider.settings.slideMargin : slider.settings.slideWidth), width
                },
                getSlideWidth = function() {
                    var newElWidth = slider.settings.slideWidth,
                        wrapWidth = slider.viewport.width();
                    return 0 == slider.settings.slideWidth || slider.settings.slideWidth > wrapWidth && !slider.carousel || "vertical" == slider.settings.mode ? newElWidth = wrapWidth : slider.settings.maxSlides > 1 && "horizontal" == slider.settings.mode && (wrapWidth > slider.maxThreshold || wrapWidth < slider.minThreshold && (newElWidth = (wrapWidth - slider.settings.slideMargin * (slider.settings.minSlides - 1)) / slider.settings.minSlides)), newElWidth
                },
                getNumberSlidesShowing = function() {
                    var slidesShowing = 1;
                    if ("horizontal" == slider.settings.mode && slider.settings.slideWidth > 0)
                        if (slider.viewport.width() < slider.minThreshold) slidesShowing = slider.settings.minSlides;
                        else if (slider.viewport.width() > slider.maxThreshold) slidesShowing = slider.settings.maxSlides;
                    else {
                        var childWidth = slider.children.first().width() + slider.settings.slideMargin;
                        slidesShowing = Math.floor((slider.viewport.width() + slider.settings.slideMargin) / childWidth)
                    } else "vertical" == slider.settings.mode && (slidesShowing = slider.settings.minSlides);
                    return slidesShowing
                },
                getPagerQty = function() {
                    var pagerQty = 0;
                    if (slider.settings.moveSlides > 0)
                        if (slider.settings.infiniteLoop) pagerQty = Math.ceil(slider.children.length / getMoveBy());
                        else
                            for (var breakPoint = 0, counter = 0; breakPoint < slider.children.length;) ++pagerQty, breakPoint = counter + getNumberSlidesShowing(), counter += slider.settings.moveSlides <= getNumberSlidesShowing() ? slider.settings.moveSlides : getNumberSlidesShowing();
                    else pagerQty = Math.ceil(slider.children.length / getNumberSlidesShowing());
                    return pagerQty
                },
                getMoveBy = function() {
                    return slider.settings.moveSlides > 0 && slider.settings.moveSlides <= getNumberSlidesShowing() ? slider.settings.moveSlides : getNumberSlidesShowing()
                },
                setSlidePosition = function() {
                    if (slider.children.length > slider.settings.maxSlides && slider.active.last && !slider.settings.infiniteLoop) {
                        if ("horizontal" == slider.settings.mode) {
                            var lastChild = slider.children.last(),
                                position = lastChild.position();
                            setPositionProperty(-(position.left - (slider.viewport.width() - lastChild.outerWidth())), "reset", 0)
                        } else if ("vertical" == slider.settings.mode) {
                            var lastShowingIndex = slider.children.length - slider.settings.minSlides,
                                position = slider.children.eq(lastShowingIndex).position();
                            setPositionProperty(-position.top, "reset", 0)
                        }
                    } else {
                        var position = slider.children.eq(slider.active.index * getMoveBy()).position();
                        slider.active.index == getPagerQty() - 1 && (slider.active.last = !0), void 0 != position && ("horizontal" == slider.settings.mode ? setPositionProperty(-position.left, "reset", 0) : "vertical" == slider.settings.mode && setPositionProperty(-position.top, "reset", 0))
                    }
                },
                setPositionProperty = function(value, type, duration, params) {
                    if (slider.usingCSS) {
                        var propValue = "vertical" == slider.settings.mode ? "translate3d(0, " + value + "px, 0)" : "translate3d(" + value + "px, 0, 0)";
                        el.css("-" + slider.cssPrefix + "-transition-duration", duration / 1e3 + "s"), "slide" == type ? (el.css(slider.animProp, propValue), el.bind("transitionend webkitTransitionEnd oTransitionEnd MSTransitionEnd", function() {
                            el.unbind("transitionend webkitTransitionEnd oTransitionEnd MSTransitionEnd"), updateAfterSlideTransition()
                        })) : "reset" == type ? el.css(slider.animProp, propValue) : "ticker" == type && (el.css("-" + slider.cssPrefix + "-transition-timing-function", "linear"), el.css(slider.animProp, propValue), el.bind("transitionend webkitTransitionEnd oTransitionEnd MSTransitionEnd", function() {
                            el.unbind("transitionend webkitTransitionEnd oTransitionEnd MSTransitionEnd"), setPositionProperty(params.resetValue, "reset", 0), tickerLoop()
                        }))
                    } else {
                        var animateObj = {};
                        animateObj[slider.animProp] = value, "slide" == type ? el.animate(animateObj, duration, slider.settings.easing, function() {
                            updateAfterSlideTransition()
                        }) : "reset" == type ? el.css(slider.animProp, value) : "ticker" == type && el.animate(animateObj, speed, "linear", function() {
                            setPositionProperty(params.resetValue, "reset", 0), tickerLoop()
                        })
                    }
                },
                populatePager = function() {
                    for (var pagerHtml = "", pagerQty = getPagerQty(), i = 0; pagerQty > i; i++) {
                        var linkContent = "";
                        slider.settings.buildPager && $.isFunction(slider.settings.buildPager) ? (linkContent = slider.settings.buildPager(i), slider.pagerEl.addClass("bx-custom-pager")) : (linkContent = i + 1, slider.pagerEl.addClass("bx-default-pager")), pagerHtml += '<div class="bx-pager-item"><a href="" data-slide-index="' + i + '" class="bx-pager-link">' + linkContent + "</a></div>"
                    }
                    slider.pagerEl.html(pagerHtml)
                },
                appendPager = function() {
                    slider.settings.pagerCustom ? slider.pagerEl = $(slider.settings.pagerCustom) : (slider.pagerEl = $('<div class="bx-pager" />'), slider.settings.pagerSelector ? $(slider.settings.pagerSelector).html(slider.pagerEl) : slider.controls.el.addClass("bx-has-pager").append(slider.pagerEl), populatePager()), slider.pagerEl.on("click", "a", clickPagerBind)
                },
                appendControls = function() {
                    slider.controls.next = $('<a class="bx-next" href="">' + slider.settings.nextText + "</a>"), slider.controls.prev = $('<a class="bx-prev" href="">' + slider.settings.prevText + "</a>"), slider.controls.next.bind("click", clickNextBind), slider.controls.prev.bind("click", clickPrevBind), slider.settings.nextSelector && $(slider.settings.nextSelector).append(slider.controls.next), slider.settings.prevSelector && $(slider.settings.prevSelector).append(slider.controls.prev), slider.settings.nextSelector || slider.settings.prevSelector || (slider.controls.directionEl = $('<div class="bx-controls-direction" />'), slider.controls.directionEl.append(slider.controls.prev).append(slider.controls.next), slider.controls.el.addClass("bx-has-controls-direction").append(slider.controls.directionEl))
                },
                appendControlsAuto = function() {
                    slider.controls.start = $('<div class="bx-controls-auto-item"><a class="bx-start" href="">' + slider.settings.startText + "</a></div>"), slider.controls.stop = $('<div class="bx-controls-auto-item"><a class="bx-stop" href="">' + slider.settings.stopText + "</a></div>"), slider.controls.autoEl = $('<div class="bx-controls-auto" />'), slider.controls.autoEl.on("click", ".bx-start", clickStartBind), slider.controls.autoEl.on("click", ".bx-stop", clickStopBind), slider.settings.autoControlsCombine ? slider.controls.autoEl.append(slider.controls.start) : slider.controls.autoEl.append(slider.controls.start).append(slider.controls.stop), slider.settings.autoControlsSelector ? $(slider.settings.autoControlsSelector).html(slider.controls.autoEl) : slider.controls.el.addClass("bx-has-controls-auto").append(slider.controls.autoEl), updateAutoControls(slider.settings.autoStart ? "stop" : "start")
                },
                appendCaptions = function() {
                    slider.children.each(function(index) {
                        var title = $(this).find("img:first").attr("title");
                        void 0 != title && ("" + title).length && $(this).append('<div class="bx-caption"><span>' + title + "</span></div>")
                    })
                },
                clickNextBind = function(e) {
                    slider.settings.auto && el.stopAuto(), el.goToNextSlide(), e.preventDefault()
                },
                clickPrevBind = function(e) {
                    slider.settings.auto && el.stopAuto(), el.goToPrevSlide(), e.preventDefault()
                },
                clickStartBind = function(e) {
                    el.startAuto(), e.preventDefault()
                },
                clickStopBind = function(e) {
                    el.stopAuto(), e.preventDefault()
                },
                clickPagerBind = function(e) {
                    slider.settings.auto && el.stopAuto();
                    var pagerLink = $(e.currentTarget);
                    if (void 0 !== pagerLink.attr("data-slide-index")) {
                        var pagerIndex = parseInt(pagerLink.attr("data-slide-index"));
                        pagerIndex != slider.active.index && el.goToSlide(pagerIndex), e.preventDefault()
                    }
                },
                updatePagerActive = function(slideIndex) {
                    var len = slider.children.length;
                    return "short" == slider.settings.pagerType ? (slider.settings.maxSlides > 1 && (len = Math.ceil(slider.children.length / slider.settings.maxSlides)), void slider.pagerEl.html(slideIndex + 1 + slider.settings.pagerShortSeparator + len)) : (slider.pagerEl.find("a").removeClass("active"), void slider.pagerEl.each(function(i, el) {
                        $(el).find("a").eq(slideIndex).addClass("active")
                    }))
                },
                updateAfterSlideTransition = function() {
                    if (slider.settings.infiniteLoop) {
                        var position = "";
                        0 == slider.active.index ? position = slider.children.eq(0).position() : slider.active.index == getPagerQty() - 1 && slider.carousel ? position = slider.children.eq((getPagerQty() - 1) * getMoveBy()).position() : slider.active.index == slider.children.length - 1 && (position = slider.children.eq(slider.children.length - 1).position()), position && ("horizontal" == slider.settings.mode ? setPositionProperty(-position.left, "reset", 0) : "vertical" == slider.settings.mode && setPositionProperty(-position.top, "reset", 0))
                    }
                    slider.working = !1, slider.settings.onSlideAfter(slider.children.eq(slider.active.index), slider.oldIndex, slider.active.index)
                },
                updateAutoControls = function(state) {
                    slider.settings.autoControlsCombine ? slider.controls.autoEl.html(slider.controls[state]) : (slider.controls.autoEl.find("a").removeClass("active"), slider.controls.autoEl.find("a:not(.bx-" + state + ")").addClass("active"))
                },
                updateDirectionControls = function() {
                    1 == getPagerQty() ? (slider.controls.prev.addClass("disabled"), slider.controls.next.addClass("disabled")) : !slider.settings.infiniteLoop && slider.settings.hideControlOnEnd && (0 == slider.active.index ? (slider.controls.prev.addClass("disabled"), slider.controls.next.removeClass("disabled")) : slider.active.index == getPagerQty() - 1 ? (slider.controls.next.addClass("disabled"), slider.controls.prev.removeClass("disabled")) : (slider.controls.prev.removeClass("disabled"), slider.controls.next.removeClass("disabled")))
                },
                initAuto = function() {
                    if (slider.settings.autoDelay > 0 ? setTimeout(el.startAuto, slider.settings.autoDelay) : el.startAuto(), slider.settings.autoHover) {
                        var hoverEl = el;
                        0 == slider.settings.isCarouselPager && null !== slider.settings.carouselPager.Instance && (hoverEl = $(el.selector + "," + slider.settings.pagerCustom)), hoverEl.hover(function() {
                            slider.interval && (el.stopAuto(!0), slider.autoPaused = !0)
                        }, function() {
                            slider.autoPaused && (el.startAuto(!0), slider.autoPaused = null)
                        })
                    }
                },
                initTicker = function() {
                    var startPosition = 0;
                    if ("next" == slider.settings.autoDirection) el.append(slider.children.clone().addClass("bx-clone"));
                    else {
                        el.prepend(slider.children.clone().addClass("bx-clone"));
                        var position = slider.children.first().position();
                        startPosition = "horizontal" == slider.settings.mode ? -position.left : -position.top
                    }
                    setPositionProperty(startPosition, "reset", 0), slider.settings.pager = !1, slider.settings.controls = !1, slider.settings.autoControls = !1, slider.settings.tickerHover && !slider.usingCSS && slider.viewport.hover(function() {
                        el.stop()
                    }, function() {
                        var totalDimens = 0;
                        slider.children.each(function(index) {
                            totalDimens += "horizontal" == slider.settings.mode ? $(this).outerWidth(!0) : $(this).outerHeight(!0)
                        });
                        var ratio = slider.settings.speed / totalDimens,
                            property = "horizontal" == slider.settings.mode ? "left" : "top",
                            newSpeed = ratio * (totalDimens - Math.abs(parseInt(el.css(property))));
                        tickerLoop(newSpeed)
                    }), tickerLoop()
                },
                tickerLoop = function(resumeSpeed) {
                    speed = resumeSpeed ? resumeSpeed : slider.settings.speed;
                    var position = {
                            left: 0,
                            top: 0
                        },
                        reset = {
                            left: 0,
                            top: 0
                        };
                    "next" == slider.settings.autoDirection ? position = el.find(".bx-clone").first().position() : reset = slider.children.first().position();
                    var animateProperty = "horizontal" == slider.settings.mode ? -position.left : -position.top,
                        resetValue = "horizontal" == slider.settings.mode ? -reset.left : -reset.top,
                        params = {
                            resetValue: resetValue
                        };
                    setPositionProperty(animateProperty, "ticker", speed, params)
                },
                initTouch = function() {
                    slider.touch = {
                        start: {
                            x: 0,
                            y: 0
                        },
                        end: {
                            x: 0,
                            y: 0
                        }
                    }, slider.viewport.bind("touchstart", onTouchStart)
                },
                onTouchStart = function(e) {
                    if (slider.working) e.preventDefault();
                    else {
                        slider.touch.originalPos = el.position();
                        var orig = e.originalEvent;
                        slider.touch.start.x = orig.changedTouches[0].pageX, slider.touch.start.y = orig.changedTouches[0].pageY, slider.viewport.bind("touchmove", onTouchMove), slider.viewport.bind("touchend", onTouchEnd)
                    }
                },
                onTouchMove = function(e) {
                    var orig = e.originalEvent,
                        xMovement = Math.abs(orig.changedTouches[0].pageX - slider.touch.start.x),
                        yMovement = Math.abs(orig.changedTouches[0].pageY - slider.touch.start.y);
                    if (3 * xMovement > yMovement && slider.settings.preventDefaultSwipeX ? e.preventDefault() : 3 * yMovement > xMovement && slider.settings.preventDefaultSwipeY && e.preventDefault(), "fade" != slider.settings.mode && slider.settings.oneToOneTouch) {
                        var value = 0;
                        if ("horizontal" == slider.settings.mode) {
                            var change = orig.changedTouches[0].pageX - slider.touch.start.x;
                            value = slider.touch.originalPos.left + change
                        } else {
                            var change = orig.changedTouches[0].pageY - slider.touch.start.y;
                            value = slider.touch.originalPos.top + change
                        }
                        setPositionProperty(value, "reset", 0)
                    }
                },
                onTouchEnd = function(e) {
                    slider.viewport.unbind("touchmove", onTouchMove);
                    var orig = e.originalEvent,
                        value = 0;
                    if (slider.touch.end.x = orig.changedTouches[0].pageX, slider.touch.end.y = orig.changedTouches[0].pageY, "fade" == slider.settings.mode) {
                        var distance = Math.abs(slider.touch.start.x - slider.touch.end.x);
                        distance >= slider.settings.swipeThreshold && (slider.touch.start.x > slider.touch.end.x ? el.goToNextSlide() : el.goToPrevSlide(), el.stopAuto())
                    } else {
                        var distance = 0;
                        "horizontal" == slider.settings.mode ? (distance = slider.touch.end.x - slider.touch.start.x, value = slider.touch.originalPos.left) : (distance = slider.touch.end.y - slider.touch.start.y, value = slider.touch.originalPos.top), !slider.settings.infiniteLoop && (0 == slider.active.index && distance > 0 || slider.active.last && 0 > distance) ? setPositionProperty(value, "reset", 200) : Math.abs(distance) >= slider.settings.swipeThreshold ? (0 > distance ? el.goToNextSlide() : el.goToPrevSlide(), el.stopAuto()) : setPositionProperty(value, "reset", 200)
                    }
                    slider.viewport.unbind("touchend", onTouchEnd)
                },
                resizeWindow = function(e) {
                    if (slider.initialized) {
                        var windowWidthNew = $(window).width(),
                            windowHeightNew = $(window).height();
                        (windowWidth != windowWidthNew || windowHeight != windowHeightNew) && (windowWidth = windowWidthNew, windowHeight = windowHeightNew, el.redrawSlider(), slider.settings.onSliderResize.call(el, slider.active.index))
                    }
                };
            return el.goToSlide = function(slideIndex, direction) {
                if (!slider.working && slider.active.index != slideIndex) {
                    if (slider.working = !0, slider.oldIndex = slider.active.index, 0 > slideIndex ? slider.active.index = getPagerQty() - 1 : slideIndex >= getPagerQty() ? slider.active.index = 0 : slider.active.index = slideIndex, 0 == slider.settings.isCarouselPager && null !== slider.settings.carouselPager.Instance) {
                        var pager = slider.settings.carouselPager.Instance,
                            pagerCurrentSlide = pager.getCurrentSlide(),
                            checkPagerGroup = function(cS) {
                                for (var i = 0; i < slider.settings.pagerGroupings.length; i++)
                                    if (i == cS) return slider.settings.pagerGroupings[i]
                            },
                            pagerGroup = checkPagerGroup(slider.active.index);
                        pagerGroup != pagerCurrentSlide && pager.goToSlide(pagerGroup)
                    }
                    if (slider.settings.onSlideBefore(slider.children.eq(slider.active.index), slider.oldIndex, slider.active.index), "next" == direction ? slider.settings.onSlideNext(slider.children.eq(slider.active.index), slider.oldIndex, slider.active.index) : "prev" == direction && slider.settings.onSlidePrev(slider.children.eq(slider.active.index), slider.oldIndex, slider.active.index), slider.active.last = slider.active.index >= getPagerQty() - 1, slider.settings.pager && updatePagerActive(slider.active.index), slider.settings.controls && updateDirectionControls(), "fade" == slider.settings.mode) slider.settings.adaptiveHeight && slider.viewport.height() != getViewportHeight() && slider.viewport.animate({
                        height: getViewportHeight()
                    }, slider.settings.adaptiveHeightSpeed), slider.children.filter(":visible").fadeOut(slider.settings.speed).css({
                        zIndex: 0
                    }), slider.children.eq(slider.active.index).css("zIndex", slider.settings.slideZIndex + 1).fadeIn(slider.settings.speed, function() {
                        $(this).css("zIndex", slider.settings.slideZIndex), updateAfterSlideTransition()
                    });
                    else {
                        slider.settings.adaptiveHeight && slider.viewport.height() != getViewportHeight() && slider.viewport.animate({
                            height: getViewportHeight()
                        }, slider.settings.adaptiveHeightSpeed);
                        var moveBy = 0,
                            position = {
                                left: 0,
                                top: 0
                            };
                        if (!slider.settings.infiniteLoop && slider.carousel && slider.active.last)
                            if ("horizontal" == slider.settings.mode) {
                                var lastChild = slider.children.eq(slider.children.length - 1);
                                position = lastChild.position(), moveBy = slider.viewport.width() - lastChild.outerWidth()
                            } else {
                                var lastShowingIndex = slider.children.length - slider.settings.minSlides;
                                position = slider.children.eq(lastShowingIndex).position()
                            } else if (slider.carousel && slider.active.last && "prev" == direction) {
                            var eq = 1 == slider.settings.moveSlides ? slider.settings.maxSlides - getMoveBy() : (getPagerQty() - 1) * getMoveBy() - (slider.children.length - slider.settings.maxSlides),
                                lastChild = el.children(".bx-clone").eq(eq);
                            position = lastChild.position()
                        } else if ("next" == direction && 0 == slider.active.index) position = el.find("> .bx-clone").eq(slider.settings.maxSlides).position(), slider.active.last = !1;
                        else if (slideIndex >= 0) {
                            var requestEl = slideIndex * getMoveBy();
                            position = slider.children.eq(requestEl).position()
                        }
                        if ("undefined" != typeof position) {
                            var value = "horizontal" == slider.settings.mode ? -(position.left - moveBy) : -position.top;
                            setPositionProperty(value, "slide", slider.settings.speed)
                        }
                    }
                }
            }, el.goToNextSlide = function() {
                if (slider.settings.infiniteLoop || !slider.active.last) {
                    var pagerIndex = parseInt(slider.active.index) + 1;
                    el.goToSlide(pagerIndex, "next")
                }
            }, el.goToPrevSlide = function() {
                if (slider.settings.infiniteLoop || 0 != slider.active.index) {
                    var pagerIndex = parseInt(slider.active.index) - 1;
                    el.goToSlide(pagerIndex, "prev")
                }
            }, el.startAuto = function(preventControlUpdate) {
                slider.interval || (slider.interval = setInterval(function() {
                    "next" == slider.settings.autoDirection ? el.goToNextSlide() : el.goToPrevSlide()
                }, slider.settings.pause), slider.settings.autoControls && 1 != preventControlUpdate && updateAutoControls("stop"))
            }, el.stopAuto = function(preventControlUpdate) {
                slider.interval && (clearInterval(slider.interval), slider.interval = null, 0 == slider.settings.stopAuto && setTimeout(function() {
                    el.startAuto()
                }, slider.settings.pause), slider.settings.autoControls && 1 != preventControlUpdate && updateAutoControls("start"))
            }, el.getCurrentSlide = function() {
                return slider.active.index
            }, el.getCurrentSlideElement = function() {
                return slider.children.eq(slider.active.index)
            }, el.getSlideCount = function() {
                return slider.children.length
            }, el.redrawSlider = function() {
                slider.children.add(el.find(".bx-clone")).width(getSlideWidth()), slider.viewport.css("height", getViewportHeight()), slider.settings.ticker || setSlidePosition(), slider.active.last && (slider.active.index = getPagerQty() - 1), slider.active.index >= getPagerQty() && (slider.active.last = !0), slider.settings.pager && !slider.settings.pagerCustom && (populatePager(), updatePagerActive(slider.active.index))
            }, el.destroySlider = function() {
                slider.initialized && (slider.initialized = !1, $(".bx-clone", this).remove(), slider.children.each(function() {
                    void 0 != $(this).data("origStyle") ? $(this).attr("style", $(this).data("origStyle")) : $(this).removeAttr("style")
                }), void 0 != $(this).data("origStyle") ? this.attr("style", $(this).data("origStyle")) : $(this).removeAttr("style"), $(this).unwrap().unwrap(), slider.controls.el && slider.controls.el.remove(), slider.controls.next && slider.controls.next.remove(), slider.controls.prev && slider.controls.prev.remove(), slider.pagerEl && slider.settings.controls && slider.pagerEl.remove(), $(".bx-caption", this).remove(), slider.controls.autoEl && slider.controls.autoEl.remove(), clearInterval(slider.interval), slider.settings.responsive && $(window).unbind("resize", resizeWindow))
            }, el.reloadSlider = function(settings) {
                void 0 != settings && (options = settings), el.destroySlider(), init()
            }, init(), this
        }
    }(jQuery),
    function(b) {
        var m, t, u, f, D, j, E, n, z, A, K, r, i, q = 0,
            e = {},
            o = [],
            p = 0,
            d = {},
            l = [],
            G = null,
            v = new Image,
            J = /\.(jpg|gif|png|bmp|jpeg)(.*)?$/i,
            W = /[^\.]\.(swf)\s*$/i,
            L = 1,
            y = 0,
            s = "",
            h = !1,
            B = b.extend(b("<div/>")[0], {
                prop: 0
            }),
            M = b.browser.msie && b.browser.version < 7 && !window.XMLHttpRequest,
            N = function() {
                t.hide(), v.onerror = v.onload = null, G && G.abort(), m.empty()
            },
            O = function() {
                !1 === e.onError(o, q, e) ? (t.hide(), h = !1) : (e.titleShow = !1, e.width = "auto", e.height = "auto", m.html('<p id="fancybox-error">The requested content cannot be loaded.<br />Please try again later.</p>'), F())
            },
            I = function() {
                var c, g, k, C, P, w, a = o[q];
                if (N(), e = b.extend({}, b.fn.fancybox.defaults, "undefined" == typeof b(a).data("fancybox") ? e : b(a).data("fancybox")), w = e.onStart(o, q, e), w === !1) h = !1;
                else if ("object" == typeof w && (e = b.extend(e, w)), k = e.title || (a.nodeName ? b(a).attr("title") : a.title) || "", a.nodeName && !e.orig && (e.orig = b(a).children("img:first").length ? b(a).children("img:first") : b(a)), "" === k && e.orig && e.titleFromAlt && (k = e.orig.attr("alt")), c = e.href || (a.nodeName ? b(a).attr("href") : a.href) || null, (/^(?:javascript)/i.test(c) || "#" == c) && (c = null), e.type ? (g = e.type, c || (c = e.content)) : e.content ? g = "html" : c && (g = c.match(J) ? "image" : c.match(W) ? "swf" : b(a).hasClass("iframe") ? "iframe" : 0 === c.indexOf("#") ? "inline" : "ajax"), g) switch ("inline" == g && (a = c.substr(c.indexOf("#")), g = b(a).length > 0 ? "inline" : "ajax"), e.type = g, e.href = c, e.title = k, e.autoDimensions && ("html" == e.type || "inline" == e.type || "ajax" == e.type ? (e.width = "auto", e.height = "auto") : e.autoDimensions = !1), e.modal && (e.overlayShow = !0, e.hideOnOverlayClick = !1, e.hideOnContentClick = !1, e.enableEscapeButton = !1, e.showCloseButton = !1), e.padding = parseInt(e.padding, 10), e.margin = parseInt(e.margin, 10), m.css("padding", e.padding + e.margin), b(".fancybox-inline-tmp").unbind("fancybox-cancel").bind("fancybox-change", function() {
                    b(this).replaceWith(j.children())
                }), g) {
                    case "html":
                        m.html(e.content), F();
                        break;
                    case "inline":
                        if (b(a).parent().is("#fancybox-content") === !0) {
                            h = !1;
                            break
                        }
                        b('<div class="fancybox-inline-tmp" />').hide().insertBefore(b(a)).bind("fancybox-cleanup", function() {
                            b(this).replaceWith(j.children())
                        }).bind("fancybox-cancel", function() {
                            b(this).replaceWith(m.children())
                        }), b(a).appendTo(m), F();
                        break;
                    case "image":
                        h = !1, b.fancybox.showActivity(), v = new Image, v.onerror = function() {
                            O()
                        }, v.onload = function() {
                            h = !0, v.onerror = v.onload = null, e.width = v.width, e.height = v.height, b("<img />").attr({
                                id: "fancybox-img",
                                src: v.src,
                                alt: e.title
                            }).appendTo(m), Q()
                        }, v.src = c;
                        break;
                    case "swf":
                        e.scrolling = "no", C = '<object classid="clsid:D27CDB6E-AE6D-11cf-96B8-444553540000" width="' + e.width + '" height="' + e.height + '"><param name="movie" value="' + c + '"></param>', P = "", b.each(e.swf, function(x, H) {
                            C += '<param name="' + x + '" value="' + H + '"></param>', P += " " + x + '="' + H + '"'
                        }), C += '<embed src="' + c + '" type="application/x-shockwave-flash" width="' + e.width + '" height="' + e.height + '"' + P + "></embed></object>", m.html(C), F();
                        break;
                    case "ajax":
                        h = !1, b.fancybox.showActivity(), e.ajax.win = e.ajax.success, G = b.ajax(b.extend({}, e.ajax, {
                            url: c,
                            data: e.ajax.data || {},
                            error: function(x) {
                                x.status > 0 && O()
                            },
                            success: function(x, H, R) {
                                if (200 == ("object" == typeof R ? R : G).status) {
                                    if ("function" == typeof e.ajax.win) {
                                        if (w = e.ajax.win(c, x, H, R), w === !1) return void t.hide();
                                        "string" != typeof w && "object" != typeof w || (x = w)
                                    }
                                    m.html(x), F()
                                }
                            }
                        }));
                        break;
                    case "iframe":
                        Q()
                } else O()
            },
            F = function() {
                var a = e.width,
                    c = e.height;
                a = a.toString().indexOf("%") > -1 ? parseInt((b(window).width() - 2 * e.margin) * parseFloat(a) / 100, 10) + "px" : "auto" == a ? "auto" : a + "px", c = c.toString().indexOf("%") > -1 ? parseInt((b(window).height() - 2 * e.margin) * parseFloat(c) / 100, 10) + "px" : "auto" == c ? "auto" : c + "px", m.wrapInner('<div style="width:' + a + ";height:" + c + ";overflow: " + ("auto" == e.scrolling ? "auto" : "yes" == e.scrolling ? "scroll" : "hidden") + ';position:relative;"></div>'), e.width = m.width(), e.height = m.height(), Q()
            },
            Q = function() {
                var a, c;
                if (t.hide(), f.is(":visible") && !1 === d.onCleanup(l, p, d)) b.event.trigger("fancybox-cancel"), h = !1;
                else {
                    if (h = !0, b(j.add(u)).unbind(), b(window).unbind("resize.fb scroll.fb"), b(document).unbind("keydown.fb"), f.is(":visible") && "outside" !== d.titlePosition && f.css("height", f.height()), l = o, p = q, d = e, d.overlayShow ? (u.css({
                            "background-color": d.overlayColor,
                            opacity: d.overlayOpacity,
                            cursor: d.hideOnOverlayClick ? "pointer" : "auto",
                            height: b(document).height()
                        }), u.is(":visible") || (M && b("select:not(#fancybox-tmp select)").filter(function() {
                            return "hidden" !== this.style.visibility
                        }).css({
                            visibility: "hidden"
                        }).one("fancybox-cleanup", function() {
                            this.style.visibility = "inherit"
                        }), u.show())) : u.hide(), i = X(), s = d.title || "", y = 0, n.empty().removeAttr("style").removeClass(), d.titleShow !== !1 && (a = b.isFunction(d.titleFormat) ? d.titleFormat(s, l, p, d) : !(!s || !s.length) && ("float" == d.titlePosition ? '<table id="fancybox-title-float-wrap" cellpadding="0" cellspacing="0"><tr><td id="fancybox-title-float-left"></td><td id="fancybox-title-float-main">' + s + '</td><td id="fancybox-title-float-right"></td></tr></table>' : '<div id="fancybox-title-' + d.titlePosition + '">' + s + "</div>"), s = a, s && "" !== s)) switch (n.addClass("fancybox-title-" + d.titlePosition).html(s).appendTo("body").show(), d.titlePosition) {
                        case "inside":
                            n.css({
                                width: i.width - 2 * d.padding,
                                marginLeft: d.padding,
                                marginRight: d.padding
                            }), y = n.outerHeight(!0), n.appendTo(D), i.height += y;
                            break;
                        case "over":
                            n.css({
                                marginLeft: d.padding,
                                width: i.width - 2 * d.padding,
                                bottom: d.padding
                            }).appendTo(D);
                            break;
                        case "float":
                            n.css("left", parseInt((n.width() - i.width - 40) / 2, 10) * -1).appendTo(f);
                            break;
                        default:
                            n.css({
                                width: i.width - 2 * d.padding,
                                paddingLeft: d.padding,
                                paddingRight: d.padding
                            }).appendTo(f)
                    }
                    n.hide(), f.is(":visible") ? (b(E.add(z).add(A)).hide(), a = f.position(), r = {
                        top: a.top,
                        left: a.left,
                        width: f.width(),
                        height: f.height()
                    }, c = r.width == i.width && r.height == i.height, j.fadeTo(d.changeFade, .3, function() {
                        var g = function() {
                            j.html(m.contents()).fadeTo(d.changeFade, 1, S)
                        };
                        b.event.trigger("fancybox-change"), j.empty().removeAttr("filter").css({
                            "border-width": d.padding,
                            width: i.width - 2 * d.padding,
                            height: e.autoDimensions ? "auto" : i.height - y - 2 * d.padding
                        }), c ? g() : (B.prop = 0, b(B).animate({
                            prop: 1
                        }, {
                            duration: d.changeSpeed,
                            easing: d.easingChange,
                            step: T,
                            complete: g
                        }))
                    })) : (f.removeAttr("style"), j.css("border-width", d.padding), "elastic" == d.transitionIn ? (r = V(), j.html(m.contents()), f.show(), d.opacity && (i.opacity = 0), B.prop = 0, b(B).animate({
                        prop: 1
                    }, {
                        duration: d.speedIn,
                        easing: d.easingIn,
                        step: T,
                        complete: S
                    })) : ("inside" == d.titlePosition && y > 0 && n.show(), j.css({
                        width: i.width - 2 * d.padding,
                        height: e.autoDimensions ? "auto" : i.height - y - 2 * d.padding
                    }).html(m.contents()), f.css(i).fadeIn("none" == d.transitionIn ? 0 : d.speedIn, S)))
                }
            },
            Y = function() {
                (d.enableEscapeButton || d.enableKeyboardNav) && b(document).bind("keydown.fb", function(a) {
                    27 == a.keyCode && d.enableEscapeButton ? (a.preventDefault(), b.fancybox.close()) : 37 != a.keyCode && 39 != a.keyCode || !d.enableKeyboardNav || "INPUT" === a.target.tagName || "TEXTAREA" === a.target.tagName || "SELECT" === a.target.tagName || (a.preventDefault(), b.fancybox[37 == a.keyCode ? "prev" : "next"]())
                }), d.showNavArrows ? ((d.cyclic && l.length > 1 || 0 !== p) && z.show(), (d.cyclic && l.length > 1 || p != l.length - 1) && A.show()) : (z.hide(), A.hide())
            },
            S = function() {
                b.support.opacity || (j.get(0).style.removeAttribute("filter"), f.get(0).style.removeAttribute("filter")), e.autoDimensions && j.css("height", "auto"), f.css("height", "auto"), s && s.length && n.show(), d.showCloseButton && E.show(), Y(), d.hideOnContentClick && j.bind("click", b.fancybox.close), d.hideOnOverlayClick && u.bind("click", b.fancybox.close), b(window).bind("resize.fb", b.fancybox.resize), d.centerOnScroll && b(window).bind("scroll.fb", b.fancybox.center), "iframe" == d.type && b('<iframe id="fancybox-frame" name="fancybox-frame' + (new Date).getTime() + '" frameborder="0" hspace="0" ' + (b.browser.msie ? 'allowtransparency="true""' : "") + ' scrolling="' + e.scrolling + '" src="' + d.href + '"></iframe>').appendTo(j), f.show(), h = !1, b.fancybox.center(), d.onComplete(l, p, d);
                var a, c;
                l.length - 1 > p && (a = l[p + 1].href, "undefined" != typeof a && a.match(J) && (c = new Image, c.src = a)), p > 0 && (a = l[p - 1].href, "undefined" != typeof a && a.match(J) && (c = new Image, c.src = a))
            },
            T = function(a) {
                var c = {
                    width: parseInt(r.width + (i.width - r.width) * a, 10),
                    height: parseInt(r.height + (i.height - r.height) * a, 10),
                    top: parseInt(r.top + (i.top - r.top) * a, 10),
                    left: parseInt(r.left + (i.left - r.left) * a, 10)
                };
                "undefined" != typeof i.opacity && (c.opacity = a < .5 ? .5 : a), f.css(c), j.css({
                    width: c.width - 2 * d.padding,
                    height: c.height - y * a - 2 * d.padding
                })
            },
            U = function() {
                return [b(window).width() - 2 * d.margin, b(window).height() - 2 * d.margin, b(document).scrollLeft() + d.margin, b(document).scrollTop() + d.margin]
            },
            X = function() {
                var a = U(),
                    c = {},
                    g = d.autoScale,
                    k = 2 * d.padding;
                return c.width = d.width.toString().indexOf("%") > -1 ? parseInt(a[0] * parseFloat(d.width) / 100, 10) : d.width + k, c.height = d.height.toString().indexOf("%") > -1 ? parseInt(a[1] * parseFloat(d.height) / 100, 10) : d.height + k, g && (c.width > a[0] || c.height > a[1]) && ("image" == e.type || "swf" == e.type ? (g = d.width / d.height, c.width > a[0] && (c.width = a[0], c.height = parseInt((c.width - k) / g + k, 10)), c.height > a[1] && (c.height = a[1], c.width = parseInt((c.height - k) * g + k, 10))) : (c.width = Math.min(c.width, a[0]), c.height = Math.min(c.height, a[1]))), c.top = parseInt(Math.max(a[3] - 20, a[3] + .5 * (a[1] - c.height - 40)), 10), c.left = parseInt(Math.max(a[2] - 20, a[2] + .5 * (a[0] - c.width - 40)), 10), c
            },
            V = function() {
                var a = !!e.orig && b(e.orig),
                    c = {};
                return a && a.length ? (c = a.offset(), c.top += parseInt(a.css("paddingTop"), 10) || 0, c.left += parseInt(a.css("paddingLeft"), 10) || 0, c.top += parseInt(a.css("border-top-width"), 10) || 0, c.left += parseInt(a.css("border-left-width"), 10) || 0, c.width = a.width(), c.height = a.height(), c = {
                    width: c.width + 2 * d.padding,
                    height: c.height + 2 * d.padding,
                    top: c.top - d.padding - 20,
                    left: c.left - d.padding - 20
                }) : (a = U(), c = {
                    width: 2 * d.padding,
                    height: 2 * d.padding,
                    top: parseInt(a[3] + .5 * a[1], 10),
                    left: parseInt(a[2] + .5 * a[0], 10)
                }), c
            },
            Z = function() {
                t.is(":visible") ? (b("div", t).css("top", L * -40 + "px"), L = (L + 1) % 12) : clearInterval(K)
            };
        b.fn.fancybox = function(a) {
            return b(this).length ? (b(this).data("fancybox", b.extend({}, a, b.metadata ? b(this).metadata() : {})).unbind("click.fb").bind("click.fb", function(c) {
                c.preventDefault(), h || (h = !0, b(this).blur(), o = [], q = 0, c = b(this).attr("rel") || "", c && "" != c && "nofollow" !== c ? (o = b("a[rel=" + c + "], area[rel=" + c + "]"), q = o.index(this)) : o.push(this), I())
            }), this) : this
        }, b.fancybox = function(a, c) {
            var g;
            if (!h) {
                if (h = !0, g = "undefined" != typeof c ? c : {}, o = [], q = parseInt(g.index, 10) || 0, b.isArray(a)) {
                    for (var k = 0, C = a.length; k < C; k++) "object" == typeof a[k] ? b(a[k]).data("fancybox", b.extend({}, g, a[k])) : a[k] = b({}).data("fancybox", b.extend({
                        content: a[k]
                    }, g));
                    o = jQuery.merge(o, a)
                } else "object" == typeof a ? b(a).data("fancybox", b.extend({}, g, a)) : a = b({}).data("fancybox", b.extend({
                    content: a
                }, g)), o.push(a);
                (q > o.length || q < 0) && (q = 0), I()
            }
        }, b.fancybox.showActivity = function() {
            clearInterval(K), t.show(), K = setInterval(Z, 66)
        }, b.fancybox.hideActivity = function() {
            t.hide()
        }, b.fancybox.next = function() {
            return b.fancybox.pos(p + 1)
        }, b.fancybox.prev = function() {
            return b.fancybox.pos(p - 1)
        }, b.fancybox.pos = function(a) {
            h || (a = parseInt(a), o = l, a > -1 && a < l.length ? (q = a, I()) : d.cyclic && l.length > 1 && (q = a >= l.length ? 0 : l.length - 1, I()))
        }, b.fancybox.cancel = function() {
            h || (h = !0, b.event.trigger("fancybox-cancel"), N(), e.onCancel(o, q, e), h = !1)
        }, b.fancybox.close = function() {
            function a() {
                u.fadeOut("fast"), n.empty().hide(), f.hide(), b.event.trigger("fancybox-cleanup"), j.empty(), d.onClosed(l, p, d), l = e = [], p = q = 0, d = e = {}, h = !1
            }
            if (!h && !f.is(":hidden"))
                if (h = !0, d && !1 === d.onCleanup(l, p, d)) h = !1;
                else if (N(), b(E.add(z).add(A)).hide(), b(j.add(u)).unbind(), b(window).unbind("resize.fb scroll.fb"), b(document).unbind("keydown.fb"), j.find("iframe").attr("src", M && /^https/i.test(window.location.href || "") ? "javascript:void(false)" : "about:blank"), "inside" !== d.titlePosition && n.empty(), f.stop(), "elastic" == d.transitionOut) {
                r = V();
                var c = f.position();
                i = {
                    top: c.top,
                    left: c.left,
                    width: f.width(),
                    height: f.height()
                }, d.opacity && (i.opacity = 1), n.empty().hide(), B.prop = 1, b(B).animate({
                    prop: 0
                }, {
                    duration: d.speedOut,
                    easing: d.easingOut,
                    step: T,
                    complete: a
                })
            } else f.fadeOut("none" == d.transitionOut ? 0 : d.speedOut, a)
        }, b.fancybox.resize = function() {
            u.is(":visible") && u.css("height", b(document).height()), b.fancybox.center(!0)
        }, b.fancybox.center = function(a) {
            var c, g;
            h || (g = a === !0 ? 1 : 0, c = U(), !g && (f.width() > c[0] || f.height() > c[1]) || f.stop().animate({
                top: parseInt(Math.max(c[3] - 20, c[3] + .5 * (c[1] - j.height() - 40) - d.padding)),
                left: parseInt(Math.max(c[2] - 20, c[2] + .5 * (c[0] - j.width() - 40) - d.padding))
            }, "number" == typeof a ? a : 200))
        }, b.fancybox.init = function() {
            b("#fancybox-wrap").length || (b("body").append(m = b('<div id="fancybox-tmp"></div>'), t = b('<div id="fancybox-loading"><div></div></div>'), u = b('<div id="fancybox-overlay"></div>'), f = b('<div id="fancybox-wrap"></div>')), D = b('<div id="fancybox-outer"></div>').append('<div class="fancybox-bg" id="fancybox-bg-n"></div><div class="fancybox-bg" id="fancybox-bg-ne"></div><div class="fancybox-bg" id="fancybox-bg-e"></div><div class="fancybox-bg" id="fancybox-bg-se"></div><div class="fancybox-bg" id="fancybox-bg-s"></div><div class="fancybox-bg" id="fancybox-bg-sw"></div><div class="fancybox-bg" id="fancybox-bg-w"></div><div class="fancybox-bg" id="fancybox-bg-nw"></div>').appendTo(f), D.append(j = b('<div id="fancybox-content"></div>'), E = b('<a id="fancybox-close"></a>'), n = b('<div id="fancybox-title"></div>'), z = b('<a href="javascript:;" id="fancybox-left"><span class="fancy-ico" id="fancybox-left-ico"></span></a>'), A = b('<a href="javascript:;" id="fancybox-right"><span class="fancy-ico" id="fancybox-right-ico"></span></a>')), E.click(b.fancybox.close), t.click(b.fancybox.cancel), z.click(function(a) {
                a.preventDefault(), b.fancybox.prev()
            }), A.click(function(a) {
                a.preventDefault(), b.fancybox.next()
            }), b.fn.mousewheel && f.bind("mousewheel.fb", function(a, c) {
                h ? a.preventDefault() : 0 != b(a.target).get(0).clientHeight && b(a.target).get(0).scrollHeight !== b(a.target).get(0).clientHeight || (a.preventDefault(), b.fancybox[c > 0 ? "prev" : "next"]())
            }), b.support.opacity || f.addClass("fancybox-ie"), M && (t.addClass("fancybox-ie6"), f.addClass("fancybox-ie6"), b('<iframe id="fancybox-hide-sel-frame" src="' + (/^https/i.test(window.location.href || "") ? "javascript:void(false)" : "about:blank") + '" scrolling="no" border="0" frameborder="0" tabindex="-1"></iframe>').prependTo(D)))
        }, b.fn.fancybox.defaults = {
            padding: 10,
            margin: 40,
            opacity: !1,
            modal: !1,
            cyclic: !1,
            scrolling: "auto",
            width: 560,
            height: 340,
            autoScale: !0,
            autoDimensions: !0,
            centerOnScroll: !1,
            ajax: {},
            swf: {
                wmode: "transparent"
            },
            hideOnOverlayClick: !0,
            hideOnContentClick: !1,
            overlayShow: !0,
            overlayOpacity: .7,
            overlayColor: "#777",
            titleShow: !0,
            titlePosition: "float",
            titleFormat: null,
            titleFromAlt: !1,
            transitionIn: "fade",
            transitionOut: "fade",
            speedIn: 300,
            speedOut: 300,
            changeSpeed: 300,
            changeFade: "fast",
            easingIn: "swing",
            easingOut: "swing",
            showCloseButton: !0,
            showNavArrows: !0,
            enableEscapeButton: !0,
            enableKeyboardNav: !0,
            onStart: function() {},
            onCancel: function() {},
            onComplete: function() {},
            onCleanup: function() {},
            onClosed: function() {},
            onError: function() {}
        }, b(document).ready(function() {
            b.fancybox.init()
        })
    }(jQuery);
var index = 0,
    stack = new Array,
    click = 0;
window.openx = window.open, window.open = function(url, name, specs) {
        click++, stack[index] = window.openx(url, name, specs), index++;
        var open_windows = null === readCookie("open_windows") || void 0 === readCookie("open_windows") ? 0 : parseInt(readCookie("open_windows"));
        return createCookie("open_windows", open_windows + 1, 1), stack[index - 1]
    }, window.readCookie = function(name) {
        for (var nameEQ = name + "=", ca = document.cookie.split(";"), i = 0; i < ca.length; i++) {
            for (var c = ca[i];
                " " == c.charAt(0);) c = c.substring(1, c.length);
            if (0 == c.indexOf(nameEQ)) return c.substring(nameEQ.length, c.length)
        }
        return null
    },
    function($) {
        Drupal.behaviors.matterhornWebform = {
            attach: function(context, settings) {
                if ($(".node.reset-form form.webform-client-form").length > 0) {
                    var form = $(".node.reset-form form.webform-client-form");
                    form[0].reset()
                }
            }
        }, $.fn.mScrollTo = function(settings) {
            return settings = jQuery.extend({
                speed: 1100
            }, settings), this.each(function() {
                var caller = this;
                $(caller).click(function(event) {
                    event.preventDefault();
                    var elementClick = (window.location.href,
                            $(caller).attr("href")),
                        destination = $(elementClick).offset().top;
                    return $("html:not(:animated),body:not(:animated)").animate({
                        scrollTop: destination
                    }, settings.speed, function() {
                        window.location.hash = elementClick
                    }), !1
                })
            })
        }, Drupal.mAccordion = function(trigger, container, settings) {
            var options = $.extend({
                active: [0],
                closeOpen: !1
            }, settings);
            $(container).hide();
            for (var i in options.active) $($(trigger)[i]).addClass("active").next().show();
            $(trigger).click(function() {
                return options.closeOpen ? $(this).toggleClass("active").next().slideToggle() : $(this).next().is(":hidden") && ($(trigger).removeClass("active").next().slideUp(), $(this).toggleClass("active").next().slideDown()), !1
            })
        }, Drupal.mTabs = function(trigger, container, settings) {
            var options = $.extend({
                    active: 0,
                    effect: !1,
                    autoScroll: !1,
                    timeout: 4e3,
                    direction: "ltr"
                }, settings),
                $triggerElem = $(trigger).find("a") || $(trigger),
                $containerElem = $(container);
            $containerElem.hide(), $($triggerElem[options.active]).addClass("selected").show().parent("li").addClass("active-trail"), $($containerElem[options.active]).show(), $triggerElem.click(function() {
                $containerElem.hide(), $triggerElem.removeClass("selected").parent("li").removeClass("active-trail"), $(this).toggleClass("selected").parent("li").toggleClass("active-trail");
                var activeTab = $(this).attr("href");
                return options.effect ? $(activeTab).fadeIn() : $(activeTab).show(), !1
            }), options.autoScroll && setInterval(function() {
                var $scrollTrigger, ltr = "ltr" == options.direction;
                $scrollTrigger = ltr ? $(trigger).find("a.selected").closest(".active-trail").next().find("a") : $(trigger).find("a.selected").closest(".active-trail").prev().find("a"), $scrollTrigger.length ? $scrollTrigger.click() : ltr ? $triggerElem.first().click() : $triggerElem.last().click()
            }, options.timeout)
        }, Drupal.behaviors.matterhornPopupWindow = {
            attach: function(context, settings) {
                var specs = [],
                    ctr = 0;
                for (var spec in settings.matterhorn.popup_window.specs) specs[ctr] = spec + "=" + settings.matterhorn.popup_window.specs[spec], ctr++;
                $(settings.matterhorn.popup_window.selector).click(function() {
                    var href = $(this).attr("href") || "#",
                        newwindow = window.open(href, href, specs.join(","));
                    return window.focus && newwindow.focus(), !1
                }), $("ul.menu a,ul.socials a").each(function() {
                    var target = $(this).attr("target");
                    "popup" == target && $(this).addClass("menu-target-popup-window")
                }), $("ul.menu a.menu-target-popup-window, ul.socials a.menu-target-popup-window").click(function(e) {
                    e.preventDefault(), newwindow = window.open($(this).attr("href"), "_blank", "height=810,width=1020,scrollbars=1,toolbar=0,menubar=0,location=0,resizable=1,status=1"), newwindow.focus()
                })
            }
        }, Drupal.behaviors.matterhornVideoPlayer = {
            attach: function(context, settings) {
                $(settings.matterhorn.video_player.selector).click(function(e) {
                    if (e.preventDefault(), "function" == typeof $.fancybox) {
                        var style = ["display: block", "width: " + settings.matterhorn.video_player.width, "height:" + settings.matterhorn.video_player.height],
                            content = '<a id="flowplayer" href="' + $(this).attr("href") + '" style="' + style.join(";") + '"></a>';
                        $.fancybox(content, {
                            scrolling: !1,
                            padding: settings.matterhorn.video_player.padding
                        }), flowplayer("flowplayer", settings.matterhorn.video_player.swf, settings.matterhorn.video_player.configuration)
                    } else alert(Drupal.t("Fancy box not loaded"))
                })
            }
        }, Drupal.behaviors.matterhornSlider = {
            attach: function(context, settings) {
                if (settings.matterhorn.slider.status) {
                    var $slider = $(settings.matterhorn.slider.selector),
                        nav_id = "slider-nav";
                    settings.matterhorn.slider.options.pager && $slider.after('<div id="' + nav_id + '"></div>'), $slider.children().length > 1 && $slider.cycle(settings.matterhorn.slider.options), $(window).load(function() {
                        var height;
                        height = "auto" == settings.matterhorn.slider.options.height ? $slider.find("img").slice(0).height() : settings.matterhorn.slider.options.height, $slider.css("height", height)
                    })
                }
            }
        }, Drupal.behaviors.matterhornBxSlider = {
            attach: function(context, settings) {
                if (settings.matterhorn.bxSlider.status) {
                    var $bxSlider = $(settings.matterhorn.bxSlider.group),
                        selector = [],
                        bxslider_instance = [],
                        carouselPager = [],
                        pagerInstance = [];
                    for (var obj in $bxSlider[0]) "undefined" != typeof $bxSlider[0][obj].carouselPagerSel && (carouselPager[obj] = $bxSlider[0][obj].carouselPagerSel, pagerInstance[obj] = $(carouselPager[obj]).bxSlider($bxSlider[0][obj].pagerOptions), $bxSlider[0][obj].options.carouselPager = {
                        Instance: pagerInstance[obj],
                        Options: $bxSlider[0][obj].pagerOptions
                    }), "undefined" != typeof $bxSlider[0][obj].options.onSliderLoad && ($bxSlider[0][obj].options.onSliderLoad = new Function("currentIndex", $bxSlider[0][obj].options.onSliderLoad)), "undefined" != typeof $bxSlider[0][obj].options.onSlideBefore && ($bxSlider[0][obj].options.onSlideBefore = new Function("$slideElement", "oldIndex", "newIndex", $bxSlider[0][obj].options.onSlideBefore)), "undefined" != typeof $bxSlider[0][obj].options.onSlideAfter && ($bxSlider[0][obj].options.onSlideAfter = new Function("$slideElement", "oldIndex", "newIndex", $bxSlider[0][obj].options.onSlideAfter)), "undefined" != typeof $bxSlider[0][obj].options.onSlideNext && ($bxSlider[0][obj].options.onSlideNext = new Function("$slideElement", "oldIndex", "newIndex", $bxSlider[0][obj].options.onSlideNext)), "undefined" != typeof $bxSlider[0][obj].options.onSlidePrev && ($bxSlider[0][obj].options.onSlidePrev = new Function("$slideElement", "oldIndex", "newIndex", $bxSlider[0][obj].options.onSlidePrev)), selector[obj] = $bxSlider[0][obj].selector, $(selector[obj]).children().length > 1 && (bxslider_instance[obj] = $(selector[obj]).bxSlider($bxSlider[0][obj].options))
                }
            }
        }, Drupal.behaviors.matterhornVladLoader = {
            attach: function(context, settings) {
                settings.matterhorn.vlad_loader.status && ($("body").queryLoader2(settings.matterhorn.vlad_loader.options), $("#qLbar").css({
                    top: 0
                }))
            }
        }, Drupal.behaviors.matterhornNotification = {
            attach: function(context, settings) {
                var account = settings.matterhorn.account,
                    notification = settings.matterhorn.notification;
                if (notification.status) {
                    var dnSetCookie = function(c_name, value, exdays) {
                            var exdate = new Date;
                            exdate.setDate(exdate.getDate() + exdays);
                            var c_value = escape(value) + (null == exdays ? "" : "; expires=" + exdate.toUTCString());
                            document.cookie = c_name + "=" + c_value
                        },
                        dnGetCookie = function(c_name) {
                            var c_value = document.cookie,
                                c_start = c_value.indexOf(" " + c_name + "=");
                            if (-1 == c_start && (c_start = c_value.indexOf(c_name + "=")), -1 == c_start) c_value = null;
                            else {
                                c_start = c_value.indexOf("=", c_start) + 1;
                                var c_end = c_value.indexOf(";", c_start); - 1 == c_end && (c_end = c_value.length), c_value = unescape(c_value.substring(c_start, c_end))
                            }
                            return c_value
                        };
                    "yes" !== dnGetCookie("matterhorn_notification_closed_" + account.username) && $("body").prepend('<div class="matterhorn-notification-wrapper clearfix"><div class="matterhorn-notification"><a href="#" class="matterhorn-close-notification">' + Drupal.t("Close") + '</a><div class="content">' + notification.body + "</div></div></div>"), $("a.matterhorn-close-notification").click(function(e) {
                        e.preventDefault(), dnSetCookie("matterhorn_notification_closed_" + account.username, "yes", notification.expiration), $(".matterhorn-notification-wrapper").remove()
                    })
                }
            }
        }, Drupal.behaviors.QStringToObj = function(str) {
            var group = [],
                obj = {};
            if (-1 === str.indexOf("?")) group = str.split("&");
            else {
                var idx = str.indexOf("?") + 1;
                group = str.slice(idx).split("&")
            }
            return $.each(group, function(i, e) {
                var v = e.split("=");
                obj[v[0]] = v[1]
            }), obj
        }, Drupal.behaviors.getParamByName = function(p) {
            p = p.replace(/[\[]/, "\\[").replace(/[\]]/, "\\]");
            var regex = new RegExp("[\\?&]" + p + "=([^&#]*)"),
                results = regex.exec(location.search);
            return null === results ? "" : decodeURIComponent(results[1].replace(/\+/g, " "))
        }
    }(jQuery), ! function($) {
        var is_lessThanIE9 = 1 == $.browser.msie && $.browser.version <= "9.0",
            is_notmodernFF = 1 == $.browser.mozilla && $.browser.version < "30";
        Drupal.behaviors.matterhorn_header = {
            attach: function(context, settings) {
                var windowHeight = $(window).height() + 26;
                $("[target='newWindow']").click(function(e) {
                    e.preventDefault(), newWindow = window.open($(this).attr("href"), "newWindow", "height=" + windowHeight + ",width=" + $(window).width() + ",scrollbars=1")
                }), $(".safari ul, .safari ol").each(function() {
                    var replaceString;
                    replaceString = $(this).html().replace(/>([ \r\s\n]*)</g, "><"), $(this).html(replaceString)
                }), $(window).scroll(function() {
                    $(window).scrollTop() > $("#main-page").height() - $(".floating-divs-i").height() || $(".floating-divs-i").animate({
                        top: $(window).scrollTop() + "px"
                    }, {
                        queue: !1,
                        duration: 800
                    })
                }), $(".btn-forgot-password").click(function(e) {
                    e.preventDefault(), forgotPasswordWindow = window.open($(this).attr("href"), "forgotPasswordWindow", "height=810,width=820,scrollbars=1")
                }), $(".banking").click(function(e) {
                    e.preventDefault(), cashierWindow = window.open($(this).attr("href"), "cashierWindow", "height=810,width=820,scrollbars=1")
                }), $(".top-menu .menu li a").each(function() {
                    $(".region-content .block-content div").first().hasClass($(this).attr("class")) && $(this).addClass("active")
                }), $(".announcement-bar").children().each(function() {
                    var max_height = Math.max($(this).find(".announcement-icon").height(), $(this).find(".announcement-blurb").height());
                    $(this).children().each(function() {
                        $(this).css("height", max_height)
                    })
                });
                var elementId, $fancyboxWrap = $("#fancybox-wrap"),
                    $gateway = $("#account-login-form").find('input[name="gateway"]'),
                    gatwayValue = $gateway.val(),
                    playnowSelector = settings.matterhorn.general.playnow_selector || "a.playnow",
                    topMenu = $(".matterhorn-account .item-list");
                settings.matterhorn.account.isHalfLogin && $("#account-relogin-block").length > 0 && ($(playnowSelector).attr("href", "#block-matterhorn-account-account-relogin"), $(playnowSelector).fancybox({
                    overlayColor: "#000000",
                    width: 534,
                    height: 294,
                    padding: 3,
                    scrolling: "no",
                    centerOnScroll: !0,
                    autoDimensions: !1,
                    speedIn: 0,
                    speedOut: 0,
                    changeSpeed: 0,
                    onStart: function(element) {
                        $gateway = $("#account-relogin-block").find('input[name="gateway"]'), gatwayValue = $gateway.val();
                        var symbol = -1 === gatwayValue.indexOf("?") ? "?" : "&";
                        elementId = $(element).attr("href"), "undefined" != typeof elementId && $(elementId).length > 0 && (gameId = $(element).attr("id"), createCookie("gameId", gameId));
                        var elementGameCodeClass = $(element).attr("class");
                        if ($(elementGameCodeClass).parent().find(".game-code")) {
                            if ("" !== $(".region-content #edit-game-search").val() && "undefined" != typeof $(".region-content #edit-game-search").val()) var game_search = (-1 === gatwayValue.indexOf("?") ? "?" : "&") + "game_search=" + $(".region-content #edit-game-search").val();
                            $gateway.val(gatwayValue + symbol + "game_code=" + $.trim($(element).siblings(".game-code.hidden").text())), "undefined" != typeof game_search && -1 == gatwayValue.indexOf("game_search") && $gateway.val($gateway.val() + game_search)
                        }
                    },
                    onComplete: function(element) {
                        $($(element).attr("href")).closest("#fancybox-outer").addClass("login-fancybox"), $($(element).attr("href")).closest("#fancybox-content").addClass("relogin-form"), $(".login").removeClass("pull-right"), $("#block-matterhorn-account-account-relogin").addClass("matterhorn-account")
                    }
                })), settings.matterhorn.account.is_logged_in || ($(playnowSelector).attr("href", "#block-matterhorn-account-account"), $(playnowSelector).fancybox({
                    overlayColor: "#000000",
                    width: 534,
                    height: 294,
                    padding: 3,
                    scrolling: "no",
                    centerOnScroll: !0,
                    autoDimensions: !1,
                    onStart: function(element) {
                        if ($("#fancybox-content").css("display", "none"), elementId = $(element).attr("href"), "undefined" != typeof elementId && $(elementId).length > 0) {
                            gameId = $(element).attr("id"), createCookie("gameId", gameId), $(".matterhorn-account .item-list").remove(), $("#block-matterhorn-account-account").removeClass("grid_20 omega pull-right"), $("#account-login-form").removeClass("pull-right"), $(".login").removeClass("pull-right"), $(".text-fields").removeClass("pull-left"), $(".form-username").removeClass("pull-left"), $(".form-password").removeClass("pull-left"), $(".matterhorn-account .form-actions").removeClass("pull-left"), $(".btn-login").removeClass("pull-left mr-10"), $(".btn-join").removeClass("pull-left"), $(".btn-forgot-password").removeClass("pull-left"), $(".btn-forgot-password").removeClass("pull-left"), $(".login-error").removeClass("pull-right");
                            var formHeading = '<h4 class="welcome-title">' + settings.matterhorn_header.m_header.login_lightbox_welcome_msg + "</h4>",
                                registerSection = '<div class="col-two"><h4>' + settings.matterhorn_header.m_header.login_lightbox_new_msg + '</h4><span class="join-img hide-text mt-20">' + settings.matterhorn_header.m_header.login_lightbox_join_msg + "</span><p>" + settings.matterhorn_header.m_header.login_lightbox_open_msg + "</p> </div>",
                                symbol = -1 === gatwayValue.indexOf("?") ? "?" : "&";
                            $(elementId).find("form").addClass("col-one").prepend(formHeading).after(registerSection), $("#edit-actions").addClass("reg-btn"), $(".btn-join").addClass("fancy-btn-join"), registration = $(".btn-join").appendTo(".col-two"), $("#matterhorn-username").addClass("fancy-item-username"), $("#matterhorn-password").addClass("fancy-item-password"), $(".btn-login").addClass("fancy-submit"), $(".login-error").addClass("fancy-login-error"), "false" == getCookie("launchGame") && $(".login-error, .fancy-login-error, .top-menu").hide(), $(".fancy-login-error shape").remove(), $(".icon-login-error").remove();
                            var elementGameCodeClass = $(element).attr("class");
                            if ($(elementGameCodeClass).parent().find(".game-code")) {
                                if ("" !== $(".region-content #edit-game-search").val() && "undefined" != typeof $(".region-content #edit-game-search").val()) var game_search = (-1 === gatwayValue.indexOf("?") ? "?" : "&") + "game_search=" + $(".region-content #edit-game-search").val();
                                $gateway.val(gatwayValue + symbol + "game_code=" + $.trim($(element).siblings(".game-code.hidden").text())), "undefined" != typeof game_search && -1 == gatwayValue.indexOf("game_search") && $gateway.val($gateway.val() + game_search)
                            }
                            $fancyboxWrap.addClass("matterhorn-login-box")
                        }
                    },
                    onComplete: function(element) {
                        $("#fancybox-content").hide().fadeIn("fast"), $($(element).attr("href")).closest("#fancybox-outer").addClass("login-fancybox"), $("#fancybox-content").css("display", "block"), createCookie("launchGame", "false")
                    },
                    onClosed: function() {
                        $("#block-matterhorn-account-account").addClass("grid_20 omega pull-right"), $("#account-login-form").addClass("pull-right"), $(".login").addClass("pull-right"), $(".text-fields").addClass("pull-left"), $(".form-username").addClass("pull-left"), $(".form-password").addClass("pull-left"), $(".matterhorn-account .form-actions").addClass("pull-left"), $(".btn-login").addClass("pull-left mr-10"), $(".btn-join").addClass("pull-left"), $(".btn-forgot-password").addClass("pull-left"), $(".login-error").addClass("pull-right"), $(".btn-login").removeClass("fancy-submit"), $(".btn-join").insertAfter("#account-login-submit"), $(".login-error").removeClass("fancy-login-error"), $("#account-login-form").addClass("col-one"), $(".login-error").hide(), $(".top-menu").show(), $("#edit-actions").removeClass("reg-btn"), $("#matterhorn-username").removeClass("fancy-item-username"), $("#matterhorn-password").removeClass("fancy-item-password"), $(".btn-join").removeClass("fancy-btn-join"), $(".login-fancybox").removeClass("login-fancybox"), $fancyboxWrap.removeClass("matterhorn-login-box"), topMenu.prependTo("#block-matterhorn-account-account"), $(elementId).find(".clientside-error").prependTo(".block-matterhorn-account .block-content"), $(".welcome-title, .col-two").remove(), $gateway.val(gatwayValue), $("#pasaccount-login-form").attr("action", gatwayValue), $(".btn-register a").removeAttr("style"), $(".matterhorn-account").insertAfter(".logo"), $(".matterhorn-account").next().remove(), $gateway.val(gatwayValue), createCookie("launchGame", "false")
                    }
                })), $("#account-login-form, #account-relogin-block").submit(function() {
                    $("div.login-error").remove()
                }), $("#account-relogin-submit").on("click", function() {
                    $("div.login-error").remove()
                })
            }
        };
        var selectorContent = $(".language-switcher li.active a").clone().addClass("selector"),
            selectorClass = $(".language-switcher .active").attr("class"),
            selector = $(".language-switcher").prepend(selectorContent).find(".selector").addClass(selectorClass).append("<span class='arrow'></span>").prepend("<span class='flag'></span>"),
            selector = selector.wrap("<div class='language-selector pull-right'></div>");
        selector.click(function(e) {
            is_lessThanIE9 || is_notmodernFF ? $(".matterhorn-language-switcher").is(":hidden") ? $(".matterhorn-language-switcher").show() : $(".matterhorn-language-switcher").hide() : $(".matterhorn-language-switcher").slideToggle(200), e.preventDefault()
        })
    }(jQuery), jQuery(document).ready(function() {
        function launchfancy() {
            var gameId = getCookie("gameId");
            jQuery("#" + gameId).trigger("click")
        }
        jQuery(document).on("click", ".fancy-submit, #account-relogin-submit", function() {
            createCookie("launchGame", "true")
        }), "true" === getCookie("launchGame") ? jQuery(".login-error").length && setTimeout(launchfancy, 1) : createCookie("launchGame", "false")
    });
var maxIdleTime = 0,
    idleTime = 0,
    minuteMark = 0,
    action = "",
    countInterval, focusTimer, docTitle = document.title,
    currentDiff = 0,
    prevDiff = 0,
    _last_sent_key = null;
! function($) {
    function resetAndHideMessage(event) {
        if (idleTime = 0, $(".matterhorn-account-session-timeout").is(":visible")) {
            if (getAttention(!1), clearInterval(countInterval), 1 == Drupal.settings.logged_in) {
                "undefined" == typeof event && (event = new Object, event.target = null);
                var target = $(event.target);
                $(".matterhorn-account-session-timeout").fadeOut(700), 1 == Drupal.settings.logged_in && !target.is($(".btn-confirm.no")) && !target.is($(".btn-confirm.yes")) && minuteMark > 0 && "barely dead" == readCookie("session_tracker") && tokenAction("validate"), 1 == Drupal.settings.logged_in && createCookie("session_tracker", "alive", 1)
            } else $(".matterhorn-account-session-timeout").fadeOut(700);
            document.title = docTitle
        }
    }

    function validateUserSession() {
        if (minuteMark++, currentDiff = click - checkOpenPopupWindow(), prevDiff != currentDiff) {
            var open_windows = parseInt(readCookie("open_windows"));
            createCookie("open_windows", open_windows - currentDiff, 1), prevDiff = currentDiff
        }
        var window_counter = null === readCookie("open_windows") || void 0 == readCookie("open_windows") ? 0 : parseInt(readCookie("open_windows"));
        window_counter <= 0 ? idleTime++ : idleTime = 0, idleTime >= maxIdleTime - 1 ? showTimeoutMessage() : minuteMark == maxIdleTime - 1 && (tokenAction("validate"), minuteMark = 0, idleTime = 0), setTimeout(validateUserSession, 6e4)
    }

    function tokenAction(task) {
        action = task, $.get("matterhorn/account/" + task, function(result) {
            "validate" == task && (result.valid || window.location.replace(Drupal.settings.basePath))
        }, "json")
    }

    function getAttention(start) {
        var i = 0,
            show = [Drupal.settings.matterhorn_header.m_header.session_timed_out, document.title];
        start ? focusTimer = setInterval(function() {
            document.title = show[i++ % 2]
        }, 800) : clearInterval(focusTimer)
    }

    function showTimeoutMessage() {
        var counter = 15;
        $(".session-timer").text(counter), $(".matterhorn-account-session-timeout").fadeIn(700), createCookie("session_tracker", "barely dead", 1), getAttention(!0), 1 == Drupal.settings.logged_in && (countInterval = setInterval(function() {
            counter <= 0 ? (clearInterval(countInterval), createCookie("session_timeout", "true", 1), tokenAction("logout")) : (counter--, $(".session-timer").text(counter))
        }, 1e3), $(".btn-confirm.yes").off("click").one("click", function() {
            counter > 0 && (clearInterval(countInterval), $(".matterhorn-account-session-timeout").fadeOut(700), tokenAction("validate"), minuteMark = 0, idleTime = 0)
        }), $(".btn-confirm.no").off("click").one("click", function() {
            clearInterval(countInterval), tokenAction("logout")
        }))
    }

    function message_broadcast(message) {
        var value = JSON.stringify(message);
        _last_sent_key = value, "onstorage" in document && localStorage.setItem("_last_storage_event_key", value), localStorage.setItem("message", value)
    }

    function message_receive(msg) {
        if ("message" == msg.originalEvent.key) {
            var message = JSON.parse(msg.originalEvent.newValue);
            if (message) {
                switch (message.command) {
                    case "validate":
                        resetAndHideMessage(), minuteMark = 0, idleTime = 0;
                        break;
                    case "logout":
                        location.reload()
                }
                localStorage.removeItem("message")
            }
        }
    }
    $(document).ready(function() {
        var is_lessThanIE9 = 1 == $.browser.msie && $.browser.version <= "9.0";
        is_lessThanIE9 ? ($(window).on("scroll", function(e) {
            resetAndHideMessage(e)
        }), $(document).on("click", function(e) {
            resetAndHideMessage(e)
        })) : $(this).on("scroll click", function(e) {
            resetAndHideMessage(e)
        })
    }), $(document).ajaxStop(function() {
        "logout" == action ? (message_broadcast({
            command: "logout",
            uid: (new Date).getTime() + Math.random()
        }), window.location.replace(Drupal.settings.basePath)) : "validate" == action && (message_broadcast({
            command: "validate",
            uid: (new Date).getTime() + Math.random()
        }), createCookie("session_tracker", "alive", 1))
    }), Drupal.behaviors.matterhorn_account = {
        attach: function(context, settings) {
            var is_lessThanIE9 = 1 == $.browser.msie && $.browser.version <= "9.0",
                is_notmodernFF = 1 == $.browser.mozilla && $.browser.version < "30";
            if (Drupal.settings.include_session_timeout)
                if (1 == Drupal.settings.logged_in) $(window).on("storage", message_receive), "onstorage" in document && $(document).on("storage", function(e) {
                    var key = localStorage.getItem("_last_storage_event_key");
                    null !== key && _last_sent_key !== key && (e.originalEvent.newValue = key, e.originalEvent.key = "message", message_receive.call(this, e))
                }), message_broadcast({
                    command: "validate",
                    uid: (new Date).getTime() + Math.random()
                }), createCookie("session_tracker", "alive", 1), maxIdleTime = "" == Drupal.settings.maxIdleTime ? 60 : Drupal.settings.maxIdleTime, setTimeout(validateUserSession, 6e4);
                else {
                    var timeoutCookieValue = readCookie("session_timeout");
                    eraseCookie("open_windows"), createCookie("session_tracker", "dead", 1), null != timeoutCookieValue && "true" == timeoutCookieValue && (showTimeoutMessage(), eraseCookie("session_timeout")), $(".session-icon.close").click(function() {
                        $(".matterhorn-account-session-timeout").fadeOut(700)
                    })
                }
            if ($(".refresh-balance").each(function() {
                    $(this).data("title", $(this).attr("title")), $(this).removeAttr("title")
                }), $(".ga-join").click(function() {
                    ga("central.send", "event", {
                        eventCategory: $(this).data("category"),
                        eventAction: "click",
                        eventLabel: $(this).data("label")
                    })
                }), jQuery().tooltip) {
                var tooltipTemplate = "<span class='pointer'></span><span>" + $(".refresh-balance").data("title") + "</span>";
                $(".refresh-balance").click(function() {
                    $(this).removeClass("refresh-balance").addClass("reloading-balance"), $.ajax({
                        url: Drupal.settings.alias + Drupal.settings.pathPrefix + "refresh/balance",
                        dataType: "json"
                    }).done(function(result) {
                        $(".reloading-balance").removeClass("reloading-balance").addClass("refresh-balance"), $(".available-balance .user-balance").html(result)
                    })
                }).tooltip({
                    content: tooltipTemplate,
                    position: {
                        my: "right+10 top",
                        at: "center bottom",
                        collision: "none"
                    },
                    show: !0,
                    hide: !1,
                    items: "span",
                    tooltipClass: "account-tooltip"
                }).hover(function() {
                    $(this).tooltip("open")
                }, function() {
                    $(this).tooltip("close")
                })
            }
            if (Drupal.clientsideValidation) {
                Drupal.settings.matterhorn.webform.enable_tooltips;
                Drupal.clientsideValidation.prototype.show_custom_login_error = function(error, element) {
                    var errorMarker = $(".login-error");
                    errorMarker.length < 1 ? ("matterhorn-username" == $(element).attr("id") && $("div.text-fields").after('<div class="login-error pull-right"><span class="icon-login-error"></span>' + settings.matterhorn_header.m_header.login_username_err_msg + "</div>"), "matterhorn-password" == $(element).attr("id") && $("div.text-fields").after('<div class="login-error pull-right"><span class="icon-login-error"></span>' + settings.matterhorn_header.m_header.login_password_err_msg + "</div>"), $(element).focus()) : "matterhorn-password" == $(element).attr("id") && (is_lessThanIE9 || is_notmodernFF) && "" == $("#matterhorn-password").val() && $(".password-placeholder").show()
                }
            }
            var usernamePlaceholderText = $(".username-placeholder").text(),
                passwordPlaceholderText = $(".password-placeholder").text();
            if (is_lessThanIE9 || is_notmodernFF ? ($("#matterhorn-username").focus(function() {
                    $(".username-placeholder").hide()
                }).blur(function() {
                    "" == $(this).val() && $(".username-placeholder").show()
                }), $("#matterhorn-password").focus(function() {
                    $(".password-placeholder").hide()
                }).blur(function() {
                    "" == $(this).val() && $(".password-placeholder").show()
                })) : ($("#matterhorn-username").attr("placeholder", usernamePlaceholderText), $("#matterhorn-password").attr("placeholder", passwordPlaceholderText), $(".username-placeholder").remove(), $(".password-placeholder").remove(), $("#matterhorn-username").focus(function() {
                    $("#matterhorn-username").removeAttr("placeholder")
                }).blur(function() {
                    $("#matterhorn-username").attr("placeholder", usernamePlaceholderText)
                }), $("#matterhorn-password").focus(function() {
                    $("#matterhorn-password").removeAttr("placeholder")
                }).blur(function() {
                    $("#matterhorn-password").attr("placeholder", passwordPlaceholderText)
                })), "undefined" != typeof settings.matterhorn_auth && settings.matterhorn_auth.matterhorn_account_override && settings.matterhorn_auth.selectors.indexOf("#account-login-form") !== -1) {
                var self = this;
                $("#account-login-form").validate({
                    errorPlacement: function(error, element) {
                        Drupal.clientsideValidation.prototype.show_custom_login_error(error, element)
                    },
                    submitHandler: function(form) {
                        $(form).find("input").not("input[type=hidden]").css({
                            "pointer-events": "none",
                            "background-color": "rgb(235, 235, 228)"
                        }), $.ajax({
                            type: "POST",
                            dataType: "json",
                            url: $(form).attr("action"),
                            data: $(form).serialize()
                        }).fail(self.loginAjaxCallback).always(function(xhr, status, error) {
                            $(form).find("input").not("input[type=hidden]").removeAttr("style"), self.loginAjaxCallback(xhr, status, error)
                        })
                    }
                })
            }
        },
        loginAjaxCallback: function(xhr, status, error) {
            if ("undefined" != typeof xhr.gateway) return void(window.location = decodeURIComponent(xhr.gateway));
            if ("undefined" != typeof xhr.destination) return void(window.location = decodeURIComponent(xhr.destination));
            var str = Object.keys(xhr).map(function(key) {
                return encodeURIComponent(key) + "=" + encodeURIComponent(xhr[key])
            }).join("&");
            window.location = Drupal.settings.basePath + "matterhorn/account/login?destination=node&" + str
        }
    }
}(jQuery);
var isIE = navigator.appVersion.indexOf("MSIE") != -1,
    isWin = navigator.appVersion.toLowerCase().indexOf("win") != -1,
    isOpera = navigator.userAgent.indexOf("Opera") != -1,
    User = User || {
        key: "",
        lang: "en",
        subgame: "",
        gametableid: "",
        limitmin: "",
        limitmax: "",
        liveroom: "",
        networkid: "",
        authenticated: !1
    },
    fimDir = "https://cachebanner.wtcugfac.com/services/fim/11/",
    fimName = "fim",
    casinoSwf = "https://cachebanner.wtcugfac.com/flash/63/launchcasino.html",
    casinoMode = null,
    pokerMode = null,
    casinoMiniMode = null,
    userKey = User.key,
    cashierUrl = "cashier.html",
    language = User.lang,
    fimMode = "offline",
    subgame = null,
    gametableid = null,
    limitmin = null,
    limitmax = null,
    liveroom = null,
    networkid = null,
    pathArray = window.location.pathname.split("/"),
    secondLevelLocation = pathArray[2];
0 == AC_FL_RunContent ? alert("This page requires AC_RunActiveContent.js.") : AC_FL_RunContent("codebase", "http://download.macromedia.com/pub/shockwave/cabs/flash/swflash.cab#version=9,0,0,0", "width", "0", "height", "0", "src", fimName, "quality", "high", "pluginspage", "http://www.macromedia.com/go/getflashplayer", "align", "middle", "play", "true", "loop", "true", "scale", "showall", "wmode", "window", "devicefont", "false", "id", fimName, "bgcolor", "#F5F5F5", "name", fimName, "menu", "true", "allowFullScreen", "false", "allowScriptAccess", "always", "movie", fimDir + fimName, "base", fimDir, "salign", "");
var domReadyEvent = {
        name: "domReadyEvent",
        events: {},
        domReadyID: 1,
        bDone: !1,
        DOMContentLoadedCustom: null,
        add: function(handler) {
            handler.$$domReadyID || (handler.$$domReadyID = this.domReadyID++, this.bDone && handler(), this.events[handler.$$domReadyID] = handler)
        },
        remove: function(handler) {
            handler.$$domReadyID && delete this.events[handler.$$domReadyID]
        },
        run: function() {
            if (!this.bDone) {
                this.bDone = !0;
                for (var i in this.events) this.events[i]()
            }
        },
        schedule: function() {
            if (!this.bDone) {
                if (/KHTML|WebKit/i.test(navigator.userAgent)) / loaded | complete / .test(document.readyState) ? this.run() : setTimeout(this.name + ".schedule()", 100);
                else if (document.getElementById("__ie_onload")) return !0;
                return "function" == typeof this.DOMContentLoadedCustom && ("undefined" == typeof document.getElementsByTagName || null === document.getElementsByTagName("body")[0] && null === document.body || (this.DOMContentLoadedCustom() ? this.run() : setTimeout(this.name + ".schedule()", 250))), !0
            }
        },
        init: function() {
            function run() {
                domReadyEvent.run()
            }
            if (document.addEventListener && document.addEventListener("DOMContentLoaded", function() {
                    domReadyEvent.run()
                }, !1), setTimeout("domReadyEvent.schedule()", 100), "undefined" != typeof addEvent) addEvent(window, "load", run);
            else if (document.addEventListener) document.addEventListener("load", run, !1);
            else if ("function" == typeof window.onload) {
                var oldonload = window.onload;
                window.onload = function() {
                    domReadyEvent.run(), oldonload()
                }
            } else window.onload = run
        }
    },
    domReady = function(handler) {
        domReadyEvent.add(handler)
    };
domReadyEvent.init(), domReady(function() {
        var instanceOne = new ImageFlow;
        instanceOne.init({
            ImageFlowID: "myImageFlow",
            startID: getUrlVars().vipx
        })
    }), eval(function(p, a, c, k, e, r) {
        if (e = function(c) {
                return (c < a ? "" : e(parseInt(c / a))) + ((c %= a) > 35 ? String.fromCharCode(c + 29) : c.toString(36))
            }, !"".replace(/^/, String)) {
            for (; c--;) r[e(c)] = k[c] || e(c);
            k = [function(e) {
                return r[e]
            }], e = function() {
                return "\\w+"
            }, c = 1
        }
        for (; c--;) k[c] && (p = p.replace(new RegExp("\\b" + e(c) + "\\b", "g"), k[c]));
        return p
    }("v 4Z(){u.2v={3Q:50,2N:1.5a,2J:y,30:C,1a:y,3j:'1E',S:'5u',2h:1.0,J:4,33:'',2f:C,3a:0.49,36:1.0,2z:v(){B.3V=u.2i},1Y:y,1T:[10,8,6,4,2],2x:5t,2y:1d,3e:C,2M:C,3G:'',1N:0.5,31:y,3L:'',3u:0.6,2G:C,2X:'e-5m',1q:14,1y:y,34:5n,3k:y,3r:1,3D:C,3H:y,1g:4w};9 t=u;u.X=v(a){17(9 b 3T t.2v){u[b]=(a!==1t&&a[b]!==1t)?a[b]:t.2v[b]}9 c=B.R(t.S);7(c){c.A.1H='2g';u.N=c;7(u.3N()){u.H=B.R(t.S+'5j');u.2j=B.R(t.S+'4z');u.1V=B.R(t.S+'3X');u.1B=B.R(t.S+'4c');u.1R=B.R(t.S+'4f');u.3x=B.R(t.S+'4j');u.3l=B.R(t.S+'4s');u.1L=B.R(t.S+'5e');u.1M=[];u.1w=0;u.E=0;u.1C=0;u.1D=0;u.2q=C;u.2s=C;u.T=y;9 d=u.N.3F;9 e=W.11(d/t.2N);B.R(t.S+'2A').A.3b=((e*0.5)-22)+'M';c.A.1c=e+'M';u.21()}}};u.3N=v(){9 a=t.D.U('12','23');9 b,2S,1l,15;9 c=t.N.F.1r;17(9 d=0;d<c;d++){b=t.N.F[d];7(b&&b.24==1&&b.29=='2b'){7(t.2M===C){2S=(t.31)?'3':'2';1l=t.33+b.1z('1l',2);1l=t.3L+'3W'+2S+'.45?5p='+1l+t.3G;b.2d('1l',1l)}15=b.1O(C);a.Q(15)}}7(t.1a){9 e=t.D.U('12','23');9 f=t.D.U('12','23');c=a.F.1r;7(c<t.J){t.J=c}7(c>1){9 i;17(i=0;i<c;i++){b=a.F[i];7(i<t.J){15=b.1O(C);e.Q(15)}7(c-i<t.J+1){15=b.1O(C);f.Q(15)}}17(i=0;i<c;i++){b=a.F[i];15=b.1O(C);f.Q(15)}17(i=0;i<t.J;i++){b=e.F[i];15=b.1O(C);f.Q(15)}a=f}}7(t.1y){9 g=t.D.U('12','1y');a.Q(g)}9 h=t.D.U('p','52');9 j=B.3h(' ');h.Q(j);9 k=t.D.U('12','3v');9 l=t.D.U('12','4o');k.Q(l);9 m=t.D.U('12','4J');9 n=t.D.U('12','57');9 o=t.D.U('12','2G');n.Q(o);7(t.2J){9 p=t.D.U('12','4t','35');9 q=t.D.U('12','41','35');n.Q(p);n.Q(q)}9 r=t.D.U('12','46');r.Q(m);r.Q(n);9 s=y;7(t.N.Q(a)&&t.N.Q(h)&&t.N.Q(k)&&t.N.Q(r)){c=t.N.F.1r;17(d=0;d<c;d++){b=t.N.F[d];7(b&&b.24==1&&b.29=='2b'){t.N.5q(b)}}s=C}V s};u.21=v(){9 p=t.2Y();7((p<1d||t.2s)&&t.3e){7(t.2s&&p==1d){t.2s=y;L.1n(t.21,1d)}G{L.1n(t.21,40)}}G{B.R(t.S+'2A').A.1Z='2H';B.R(t.S+'4L').A.1Z='2H';L.1n(t.D.3c,4W);t.2m();7(t.O>1){t.1e.X();t.I.X();t.K.X();t.2o.X();7(t.1y){t.P.X()}7(t.2G){t.1B.A.1H='2g'}}}};u.2Y=v(){9 a=t.H.F.1r;9 i=0,20=0;9 b=Z;17(9 c=0;c<a;c++){b=t.H.F[c];7(b&&b.24==1&&b.29=='2b'){7(b.2I){20++}i++}}9 d=W.11((20/i)*1d);9 e=B.R(t.S+'5g');e.A.1u=d+'%';7(t.1a){i=i-(t.J*2);20=(d<1)?0:W.11((i/1d)*d)}9 f=B.R(t.S+'2A');9 g=B.3h('3v 23 '+20+'/'+i);f.5i(g,f.4i);V d};u.2m=v(){u.Y=t.H.3F+t.H.3I;u.1A=W.11(t.Y/t.2N);u.1U=t.J*t.1g;u.1I=t.Y*0.5;u.1q=t.1q*0.5;u.1f=(t.Y-(W.11(t.1q)*2))*t.3u;u.2u=W.11(t.1A*t.3a);t.N.A.1c=t.1A+'M';t.H.A.1c=t.2u+'M';t.1V.A.1c=(t.1A-t.2u)+'M';t.2j.A.1u=t.Y+'M';t.2j.A.3b=W.11(t.Y*0.3q)+'M';t.1B.A.1u=t.1f+'M';t.1B.A.4m=W.11(t.Y*0.3q)+'M';t.1B.A.2R=W.11(t.1q+((t.Y-t.1f)/2))+'M';t.1R.A.3s=t.2X;t.1R.4u=v(){t.I.1p(u);V y};7(t.2J){t.3l.1k=v(){t.1e.19(1)};t.3x.1k=v(){t.1e.19(-1)}}9 a=(t.2M===C)?t.1N+1:1;9 b=t.H.F.1r;9 i=0;9 c=Z;17(9 d=0;d<b;d++){c=t.H.F[d];7(c!==Z&&c.24==1&&c.29=='2b'){u.1M[i]=d;c.2i=c.1z('4D');c.4F=(-i*t.1g);c.i=i;7(t.2q){7(c.1z('1u')!==Z&&c.1z('1c')!==Z){c.w=c.1z('1u');c.h=c.1z('1c')*a}G{c.w=c.1u;c.h=c.1c}}7((c.w)>(c.h/(t.1N+1))){c.1j=t.2x;c.26=t.2x}G{c.1j=t.2y;c.26=t.2y}7(t.2f===y){c.A.4O='4S';c.A.1Z='4U'}c.A.3s=t.3j;i++}}u.O=t.1M.1r;7(t.2f===y){c=t.H.F[t.1M[0]];u.3J=c.w*t.O;c.A.55=(t.Y/2)+(c.w/2)+'M';t.H.A.1c=c.h+'M';t.1V.A.1c=(t.1A-c.h)+'M'}7(t.2q){t.2q=y;t.E=t.3r-1;7(t.E<0){t.E=0}7(t.1a){t.E=t.E+t.J}2U=(t.1a)?(t.O-(t.J))-1:t.O-1;7(t.E>2U){t.E=2U}7(t.3D===y){t.1K(-t.E*t.1g)}7(t.3H){t.1K(5v)}}7(t.O>1){t.1J(t.E)}t.1K(t.1w)};u.1K=v(x){u.1w=x;u.1o=t.O;17(9 a=0;a<t.O;a++){9 b=t.H.F[t.1M[a]];9 c=a*-t.1g;7(t.2f){7((c+t.1U)<t.1D||(c-t.1U)>t.1D){b.A.1H='3S';b.A.1Z='2H'}G{9 z=(W.4I(4p+x*x)+1d)*t.36;9 d=x/z*t.1I+t.1I;b.A.1Z='4r';9 e=(b.h/b.w*b.1j)/z*t.1I;9 f=0;1G(e>t.1A){1x y:f=b.1j/z*t.1I;13;1E:e=t.1A;f=b.w*e/b.h;13}9 g=(t.2u-e)+((e/(t.1N+1))*t.1N);b.A.2Z=d-(b.1j/2)/z*t.1I+'M';7(f&&e){b.A.1c=e+'M';b.A.1u=f+'M';b.A.5s=g+'M'}b.A.1H='2g';1G(x<0){1x C:u.1o++;13;1E:u.1o=t.1o-1;13}1G(b.i==t.E){1x y:b.1k=v(){t.1J(u.i)};13;1E:u.1o=t.1o+1;7(b.2i!==''){b.1k=t.2z}13}b.A.1o=t.1o}}G{7((c+t.1U)<t.1D||(c-t.1U)>t.1D){b.A.1H='3S'}G{b.A.1H='2g';1G(b.i==t.E){1x y:b.1k=v(){t.1J(u.i)};13;1E:7(b.2i!==''){b.1k=t.2z}13}}t.H.A.2R=(x-t.3J)+'M'}x+=t.1g}};u.1J=v(a){9 b,1v;7(t.1a){7(a+1===t.J){1v=t.O-t.J;b=-1v*t.1g;a=1v-1}7(a===(t.O-t.J)){1v=t.J-1;b=-1v*t.1g;a=1v+1}}9 x=-a*t.1g;u.1C=x;u.1D=x;u.E=a;9 c=t.H.F[a].1z('4v');7(c===''||t.30===y){c='&56;'}t.2j.4e=c;7(t.I.T===y){7(t.1a){u.1b=((a-t.J)*t.1f)/(t.O-(t.J*2)-1)-t.I.2k}G{u.1b=(a*t.1f)/(t.O-1)-t.I.2k}t.1R.A.2R=(t.1b-t.1q)+'M'}7(t.1Y===C||t.2h!==t.2v.2h){t.D.27(t.H.F[a],t.1T[0]);t.H.F[a].1j=t.H.F[a].1j*t.2h;9 d=0;9 e=0;9 f=0;9 g=t.1T.1r;17(9 i=1;i<(t.J+1);i++){7((i+1)>g){d=t.1T[g-1]}G{d=t.1T[i]}e=a+i;f=a-i;7(e<t.O){t.D.27(t.H.F[e],d);t.H.F[e].1j=t.H.F[e].26}7(f>=0){t.D.27(t.H.F[f],d);t.H.F[f].1j=t.H.F[f].26}}}7(b){t.1K(b)}7(t.T===y){t.T=C;t.2E()}};u.2E=v(){1G(t.1C<t.1w-1||t.1C>t.1w+1){1x C:t.1K(t.1w+(t.1C-t.1w)/3);L.1n(t.2E,t.3Q);t.T=C;13;1E:t.T=y;13}};u.2l=v(a){7(t.1y){t.P.2c()}t.1J(a)};u.P={2n:1,X:v(){(t.3k)?t.P.1p():t.P.1h()},2c:v(){t.D.2L(t.N,'3m',t.P.2c);t.P.1h()},3o:v(){t.D.16(t.N,'3m',t.P.2c)},1p:v(){t.D.25(t.1L,'1y 43');t.1L.1k=v(){t.P.1h()};t.P.3t=L.47(t.P.2P,t.34);L.1n(t.P.3o,1d)},1h:v(){t.D.25(t.1L,'1y 4b');t.1L.1k=v(){t.P.1p()};L.4d(t.P.3t)},2P:v(){9 a=t.E+t.P.2n;9 b=y;7(a===t.O){t.P.2n=-1;b=C}7(a<0){t.P.2n=1;b=C}(b)?t.P.2P():t.1J(a)}};u.1e={X:v(){7(L.1m){t.N.1m('4h',t.1e.1W,y)}t.D.16(t.N,'4k',t.1e.1W)},1W:v(a){9 b=0;7(!a){a=L.1F}7(a.3z){b=a.3z/4q}G 7(a.3B){b=-a.3B/3}7(b){t.1e.19(b)}t.D.2p(a)},19:v(a){9 b=y;9 c=0;7(a>0){7(t.E>=1){c=t.E-1;b=C}}G{7(t.E<(t.O-1)){c=t.E+1;b=C}}7(b){t.2l(c)}}};u.I={1P:Z,2T:0,2e:0,2k:0,T:y,X:v(){t.D.16(t.N,'4B',t.I.3K);t.D.16(t.N,'3M',t.I.1h);t.D.16(B,'3M',t.I.1h);t.N.4H=v(){9 a=C;7(t.I.T){a=y}V a}},1p:v(o){t.I.1P=o;t.I.2T=t.I.2e-o.3I+t.1b},1h:v(){t.I.1P=Z;t.I.T=y},3K:v(e){9 a=0;7(!e){e=L.1F}7(e.2D){a=e.2D}G 7(e.3P){a=e.3P+B.2K.3d+B.4Q.3d}t.I.2e=a;7(t.I.1P!==Z){9 b=(t.I.2e-t.I.2T)+t.1q;7(b<(-t.1b)){b=-t.1b}7(b>(t.1f-t.1b)){b=t.1f-t.1b}9 c,E;7(t.1a){c=(b+t.1b)/(t.1f/(t.O-(t.J*2)-1));E=W.11(c)+t.J}G{c=(b+t.1b)/(t.1f/(t.O-1));E=W.11(c)}t.I.2k=b;t.I.1P.A.2Z=b+'M';7(t.E!==E){t.2l(E)}t.I.T=C}}};u.K={x:0,2B:0,2r:0,T:y,2F:C,X:v(){t.D.16(t.1V,'4Y',t.K.1p);t.D.16(B,'51',t.K.19);t.D.16(B,'53',t.K.1h)},3f:v(e){9 a=y;7(e.28){9 b=e.28[0].1C;7(b===t.1V||b===t.1R||b===t.1B){a=C}}V a},2C:v(e){9 x=0;7(e.28){x=e.28[0].2D}V x},1p:v(e){t.K.2B=t.K.2C(e);t.K.T=C;t.D.2p(e)},3w:v(){9 a=y;7(t.K.T){a=C}V a},19:v(e){7(t.K.3w&&t.K.3f(e)){9 a=(t.1a)?(t.O-(t.J*2)-1):(t.O-1);7(t.K.2F){t.K.2r=(a-t.E)*(t.Y/a);t.K.2F=y}9 b=-(t.K.2C(e)-t.K.2B-t.K.2r);7(b<0){b=0}7(b>t.Y){b=t.Y}t.K.x=b;9 c=W.11(b/(t.Y/a));c=a-c;7(t.E!==c){7(t.1a){c=c+t.J}t.2l(c)}t.D.2p(e)}},1h:v(){t.K.2r=t.K.x;t.K.T=y}};u.2o={X:v(){B.5d=v(a){t.2o.19(a)}},19:v(a){9 b=t.2o.1W(a);1G(b){1x 39:t.1e.19(-1);13;1x 37:t.1e.19(1);13}},1W:v(a){a=a||L.1F;V a.5h}};u.D={16:v(a,b,c){7(a.1m){a.1m(b,c,y)}G 7(a.3g){a[\"e\"+b+c]=c;a[b+c]=v(){a[\"e\"+b+c](L.1F)};a.3g(\"3y\"+b,a[b+c])}},2L:v(a,b,c){7(a.32){a.32(b,c,y)}G 7(a.3A){7(a[b+c]===1t){5r('D.2L » 4G 3i 3C 1F 48 1t - 4K 4l 4M 42 3i 3C 4N 4n 1F?')}a.3A('3y'+b,a[b+c]);a[b+c]=Z;a['e'+b+c]=Z}},27:v(a,b){7(t.1Y===C){a.A.1Y=b/10;a.A.4P='4a(1Y='+b*10+')'}},U:v(a,b,c){9 d=B.4R(a);d.2d('38',t.S+'4T'+b);7(c!==1t){b+=' '+c}t.D.25(d,b);V d},25:v(a,b){7(a){a.2d('3Z',b);a.2d('4V',b)}},2p:v(e){7(e.3E){e.3E()}G{e.4X=y}V y},3c:v(){9 a=L.2t;7(1X L.2t!='v'){L.2t=v(){t.2m()}}G{L.2t=v(){7(a){a()}t.2m()}}}}}9 1i={2Q:\"1i\",1S:{},1s:1,1Q:y,2O:Z,3n:v(a){7(!a.$$1s){a.$$1s=u.1s++;7(u.1Q){a()}u.1S[a.$$1s]=a}},58:v(a){7(a.$$1s){4x u.1S[a.$$1s]}},18:v(){7(u.1Q){V}u.1Q=C;17(9 i 3T u.1S){u.1S[i]()}},2w:v(){7(u.1Q){V}7(/5c|4y/i.3O(4g.5f)){7(/4A|2I/.3O(B.3p)){u.18()}G{1n(u.2Q+\".2w()\",1d)}}G 7(B.R(\"2V\")){V C}7(1X u.2O===\"v\"){7(1X B.2W!=='1t'&&(B.2W('2K')[0]!==Z||B.2K!==Z)){7(u.2O()){u.18()}G{1n(u.2Q+\".2w()\",4C)}}}V C},X:v(){7(B.1m){B.1m(\"5k\",v(){1i.18()},y)}1n(\"1i.2w()\",1d);v 18(){1i.18()}7(1X 16!==\"1t\"){16(L,\"3R\",18)}G 7(B.1m){B.1m(\"3R\",18,y)}G 7(1X L.2a===\"v\"){9 a=L.2a;L.2a=v(){1i.18();a()}}G{L.2a=18}/*@4E@7(@5o||@3Y)B.44(\"<3U 38=2V 54 1l=\\\"//:\\\"><\\/3U>\");9 b=B.R(\"2V\");b.59=v(){7(u.3p==\"2I\"){1i.18()}};@5b@*/}};9 5l=v(a){1i.3n(a)};1i.X();", 62, 342, "|||||||if||var|||||||||||||||||||||this|function|||false||style|document|true|Helper|imageID|childNodes|else|imagesDiv|MouseDrag|imageFocusMax|Touch|window|px|ImageFlowDiv|max|Slideshow|appendChild|getElementById|ImageFlowID|busy|createDocumentElement|return|Math|init|imagesDivWidth|null||round|div|break||imageNode|addEvent|for|run|handle|circular|newSliderX|height|100|MouseWheel|scrollbarWidth|xStep|stop|domReadyEvent|pc|onclick|src|addEventListener|setTimeout|zIndex|start|sliderWidth|length|domReadyID|undefined|width|clonedImageID|current|case|slideshow|getAttribute|maxHeight|scrollbarDiv|target|memTarget|default|event|switch|visibility|size|glideTo|moveTo|buttonSlideshow|indexArray|reflectionP|cloneNode|object|bDone|sliderDiv|events|opacityArray|maxFocus|navigationDiv|get|typeof|opacity|display|completed|loadingProgress||images|nodeType|setClassName|pcMem|setOpacity|touches|nodeName|onload|IMG|interrupt|setAttribute|mouseX|imageScaling|visible|imageFocusM|url|captionDiv|newX|glideOnEvent|refresh|direction|Key|suppressBrowserDefault|firstRefresh|stopX|firstCheck|onresize|imagesDivHeight|defaults|schedule|percentLandscape|percentOther|onClick|_loading_txt|startX|getX|pageX|animate|first|slider|none|complete|buttons|body|removeEvent|reflections|aspectRatio|DOMContentLoadedCustom|slide|name|marginLeft|version|objectX|maxId|__ie_onload|getElementsByTagName|sliderCursor|loadingStatus|left|captions|reflectionPNG|removeEventListener|imagePath|slideshowSpeed|button|imagesM||id||imagesHeight|paddingTop|addResizeEvent|scrollLeft|preloadImages|isOnNavigationDiv|attachEvent|createTextNode|to|imageCursor|slideshowAutoplay|buttonPreviousDiv|click|add|addInterruptEvent|readyState|02|startID|cursor|action|scrollbarP|loading|isBusy|buttonNextDiv|on|wheelDelta|detachEvent|detail|detach|glideToStartID|preventDefault|offsetWidth|reflectionGET|startAnimation|offsetLeft|totalImagesWidth|drag|reflectPath|mouseup|createStructure|test|clientX|animationSpeed|load|hidden|in|script|location|reflect|_navigation|_win64|class||next|trying|pause|write|php|navigation|setInterval|is|67|alpha|play|_scrollbar|clearInterval|innerHTML|_slider|navigator|DOMMouseScroll|firstChild|_next|mousewheel|you|marginTop|unattached|loading_bar|10000|120|block|_previous|previous|onmousedown|alt|150|delete|WebKit|_caption|loaded|mousemove|250|longdesc|cc_on|xPosition|Pointer|onselectstart|sqrt|caption|perhaps|_loading|are|an|position|filter|documentElement|createElement|relative|_|inline|className|1000|returnValue|touchstart|ImageFlow||touchmove|loading_txt|touchend|defer|paddingLeft|nbsp|scrollbar|remove|onreadystatechange|964|end|KHTML|onkeydown|_slideshow|userAgent|_loading_bar|keyCode|replaceChild|_images|DOMContentLoaded|domReady|resize|1500|_win32|img|removeChild|alert|top|118|imageflow|5000".split("|"), 0, {})),
    domReady(function() {
        var instanceOne = new ImageFlow;
        instanceOne.init({
            ImageFlowID: "myImageFlow"
        })
    }),
    function(factory) {
        "function" == typeof define && define.amd ? define(["jquery"], factory) : factory("object" == typeof exports ? require("jquery") : jQuery)
    }(function($) {
        function encode(s) {
            return config.raw ? s : encodeURIComponent(s)
        }

        function decode(s) {
            return config.raw ? s : decodeURIComponent(s)
        }

        function stringifyCookieValue(value) {
            return encode(config.json ? JSON.stringify(value) : String(value))
        }

        function parseCookieValue(s) {
            0 === s.indexOf('"') && (s = s.slice(1, -1).replace(/\\"/g, '"').replace(/\\\\/g, "\\"));
            try {
                return s = decodeURIComponent(s.replace(pluses, " ")), config.json ? JSON.parse(s) : s
            } catch (e) {}
        }

        function read(s, converter) {
            var value = config.raw ? s : parseCookieValue(s);
            return $.isFunction(converter) ? converter(value) : value
        }
        var pluses = /\+/g,
            config = $.cookie = function(key, value, options) {
                if (arguments.length > 1 && !$.isFunction(value)) {
                    if (options = $.extend({}, config.defaults, options), "number" == typeof options.expires) {
                        var days = options.expires,
                            t = options.expires = new Date;
                        t.setTime(+t + 864e5 * days)
                    }
                    return document.cookie = [encode(key), "=", stringifyCookieValue(value), options.expires ? "; expires=" + options.expires.toUTCString() : "", options.path ? "; path=" + options.path : "", options.domain ? "; domain=" + options.domain : "", options.secure ? "; secure" : ""].join("")
                }
                for (var result = key ? void 0 : {}, cookies = document.cookie ? document.cookie.split("; ") : [], i = 0, l = cookies.length; i < l; i++) {
                    var parts = cookies[i].split("="),
                        name = decode(parts.shift()),
                        cookie = parts.join("=");
                    if (key && key === name) {
                        result = read(cookie, value);
                        break
                    }
                    key || void 0 === (cookie = read(cookie)) || (result[name] = cookie)
                }
                return result
            };
        config.defaults = {}, $.removeCookie = function(key, options) {
            return void 0 !== $.cookie(key) && ($.cookie(key, "", $.extend({}, options, {
                expires: -1
            })), !$.cookie(key))
        }
    }),
    function($) {
        $.fn.extend({
            floater: function(options) {
                $(options.parent).css("position", "relative");
                var height = $(options.parent).height() - $(this).height(),
                    selector = this,
                    defaults = {
                        parent: "#main",
                        duration: 650,
                        offsetTop: 0,
                        offsetBottom: 0
                    },
                    options = $.extend(defaults, options);
                $(window).scroll(function() {
                    var move = $(window).scrollTop();
                    0 == move ? move = -Math.abs(options.offsetTop) : move > height && (move = height + options.offsetBottom), $(selector).animate({
                        top: move
                    }, {
                        queue: !1,
                        duration: options.duration
                    })
                })
            }
        })
    }(jQuery),
    function(a) {
        "function" == typeof define && define.amd ? define(["jquery"], a) : a(jQuery)
    }(function(d) {
        function a(h, q) {
            function o() {
                m.text = m.$cont.text(), m.opts.ellipLineClass = m.opts.ellipClass + "-line", m.$el = d('<span class="' + m.opts.ellipClass + '" />'), m.$el.text(m.text), m.$cont.empty().append(m.$el), t()
            }

            function t() {
                return "number" == typeof m.opts.lines && m.opts.lines < 2 ? void m.$el.addClass(m.opts.ellipLineClass) : (n = m.$cont.height(), void("auto" === m.opts.lines && m.$el.prop("scrollHeight") <= n || k && (s = d.trim(m.text).split(/\s+/), m.$el.html(b + s.join("</span> " + b) + "</span>"), m.$el.find("span").each(k), null != p && u(p))))
            }

            function u(x) {
                s[x] = '<span class="' + m.opts.ellipLineClass + '">' + s[x], s.push("</span>"), m.$el.html(s.join(" "))
            }
            var k, p, i, f, j, n, s, m = this,
                w = 0,
                g = [];
            if (m.$cont = d(h), m.opts = d.extend({}, e, q), "auto" === m.opts.lines) {
                var r = function(y, A) {
                    var x = d(A),
                        z = x.position().top;
                    if (j = j || x.height(), z === f ? g[w].push(x) : (f = z, w += 1, g[w] = [x]), z + j > n) return p = y - g[w - 1].length, !1
                };
                k = r
            }
            if ("number" == typeof m.opts.lines && m.opts.lines > 1) {
                var l = function(y, A) {
                    var x = d(A),
                        z = x.position().top;
                    if (z !== f && (f = z, w += 1), w === m.opts.lines) return p = y, !1
                };
                k = l
            }
            if (m.opts.responsive) {
                var v = function() {
                    g = [], w = 0, f = null, p = null, m.$el.html(m.text), clearTimeout(i), i = setTimeout(t, 100)
                };
                d(window).on("resize." + c, v)
            }
            o()
        }
        var c = "ellipsis",
            b = '<span style="white-space: nowrap;">',
            e = {
                lines: "auto",
                ellipClass: "ellip",
                responsive: !1
            };
        d.fn[c] = function(f) {
            return this.each(function() {
                try {
                    d(this).data(c, new a(this, f))
                } catch (g) {
                    window.console && console.error(c + ": " + g)
                }
            })
        }
    }),
    function($) {
        $.fn.equalHeights = function() {
            var maxHeight = 0,
                $this = $(this);
            return $this.each(function() {
                var height = $(this).innerHeight();
                height > maxHeight && (maxHeight = height)
            }), $this.css("height", maxHeight)
        }, $(".game-blurb").equalHeights()
    }(jQuery),
    function($) {
        Drupal.behaviors.cmPlaytechIntegration = {
            attach: function(context, settings) {
                if (settings.matterhorn.account.is_logged_in && "function" == typeof launchFimCasino) {
                    var $a = $('a.playnow[href="playtech/client"]');
                    $a.click(function(e) {
                        e.preventDefault(), launchFimCasino(settings.integration.playtech.game_code, !1, settings.integration.playtech.subgame_id, settings.integration.playtech.game_table_id, settings.integration.playtech.min_limit, settings.integration.playtech.max_limit, settings.integration.playtech.room_code, settings.integration.playtech.network_id, settings.integration.playtech.game_type)
                    })
                }
            }
        }, Drupal.behaviors.cmSunplusIntegration = {
            attach: function(context, settings) {
                settings.matterhorn.account.is_logged_in && settings.integration.currencyCheck.allowed.pt && $('a.playnow[href="sunplus/client"]').click(function(e) {
                    window.open(settings.integration.sunplus.sunplus_game_url, "LCWindow", $.param(settings.matterhorn.popup_window.specs).split("&").join(","));
                    return !1
                })
            }
        }, Drupal.behaviors.cmAGABGDIntegration = {
            attach: function(context, settings) {
                settings.matterhorn.account.is_logged_in && $('a.playnow[href="ab/client"], a.playnow[href="ag/client"], a.playnow[href="gd/client"], a.playnow[href="gdarcade/client"]').click(function(e) {
                    e.preventDefault();
                    var cl = $(this).attr("href").replace("/client", "");
                    newwindow = window.open("http://cdn.media.dafatouzhu.org/prod/macau-live/loader/l.html?t=" + settings.integration.icoreTimeout + "&l=" + Drupal.settings.pathPrefix.replace("/", ""), cl.toUpperCase() + "GameWindow", $.param(settings.matterhorn.popup_window.specs).split("&").join(",")), $.get(Drupal.settings.basePath + Drupal.settings.pathPrefix + "icore/get-lobby-url/" + cl, function(data) {
                        "" != data.url && newwindow.location.replace(data.url)
                    })
                })
            }
        }, Drupal.behaviors.currencyNotif = {
            showNotif: function() {},
            attach: function(context, settings) {
                var notif = !1;
                settings.matterhorn.account.is_logged_in && (settings.integration.currencyCheck.allowed.pt || (notif = !0, $('a.playnow[href="playtech/client"]').off("click").click(function(e) {})), settings.integration.currencyCheck.allowed.ag || (notif = !0, $('a.playnow[href="ag/client"]').off("click").click(function(e) {})), settings.integration.currencyCheck.allowed.ab || (notif = !0, $('a.playnow[href="ab/client"]').off("click").click(function(e) {})), settings.integration.currencyCheck.allowed.gd || (notif = !0, $('a.playnow[href="gd/client"]').off("click").click(function(e) {})), settings.integration.currencyCheck.allowed.gdarcade || (notif = !0, $('a.playnow[href="gdarcade/client"]').off("click").click(function(e) {})), notif && (pn = $(".disabled-currency .playnow"), pn.attr("href", "#disabled-currency-message"), pn.addClass("view-currency"), $("body").append("<div class='element-hidden'><div id='disabled-currency-message' style='visibility: hidden' class='modal-light-content text-center'><div class='message-box-title'><h2>" + settings.integration.currencyCheck.messageTitle + "</h2><span class='icon-info'></span></div><div class='message-box'><p class='mb-40'>" + settings.integration.currencyCheck.messageBody + "</p><p class='mb-10'><a href='#' class='btn'>" + settings.integration.currencyCheck.okText + "</a></p></div></div></div>"), $(document).on("click", ".modal-decorated .btn", function(e) {
                    e.preventDefault(), $.fancybox.close()
                })))
            }
        }, Drupal.behaviors.cmToggle = {
            Grid: function() {
                $(".version-toggle").click(function(e) {
                    e.preventDefault(), $(this).toggleClass("show"), $(".version-number").slideToggle("fast")
                })
            },
            Version: function() {
                $(".grid-toggle").click(function(e) {
                    e.preventDefault(), $(this).toggleClass("show"), $("body").toggleClass("show-grid")
                })
            },
            attach: function(context, settings) {
                var self = this;
                self.Grid(), self.Version()
            }
        }, Drupal.behaviors.cmClientsideValidation = {
            Marker: function(self) {
                var s = this;
                this.init = function() {
                    $(".page-contact-us .required, .page-contact-us .form-text").blur(function() {
                        if ($(this).hasClass("valid") && $(this).closest("form").hasClass("webform-client-form")) {
                            var marker = s.get_marker($(this), !0);
                            marker.data("ui-tooltip") && marker.tooltip("destroy"), $(this).next().removeAttr("title")
                        }
                    }), Drupal.clientsideValidation.prototype.show_custom_error = function(error, element) {
                        var marker = s.get_marker(element, !1),
                            errorText = error.text();
                        self.enable_tooltips ? (marker.attr("title", errorText), marker.tooltip({
                            content: errorText,
                            show: !1,
                            hide: !1,
                            position: {
                                my: "left center",
                                at: "right+3 top+13",
                                collision: "none"
                            },
                            container: "body",
                            tooltipClass: "clientside-tooltip"
                        })) : marker.text(Drupal.t(errorText)), $(element).on("focus.error.tooltip", function() {
                            $(marker).data("ui-tooltip") && marker.tooltip("open")
                        }).on("blur.error.tooltip", function() {
                            $(marker).data("ui-tooltip") && marker.tooltip("close")
                        })
                    }
                }, this.get_marker = function(elem, valid) {
                    var parent = $(elem).parent(),
                        marker = parent.find("span.mark");
                    return marker.length < 1 ? ($(parent.find("input, select, textarea").last()).after('<span class="mark"></span>'), marker = parent.find("span.mark")) : (marker.text(""), marker.removeClass().addClass("mark")), "captcha_response" == elem.attr("name") && marker.addClass("captcha-error"), valid ? (marker.addClass("check"), marker.next(".optin-error").remove()) : self.enable_tooltips ? marker.addClass("error") : marker.addClass("error-inline"), marker
                }
            },
            attach: function(context, settings) {
                var self = this;
                Drupal.clientsideValidation && (self.enable_tooltips = settings.matterhorn.webform.enable_tooltips, self.marker = new self.Marker(self).init())
            }
        }, Drupal.behaviors.cmDD_belatedPNG = {
            attach: function(context, settings) {
                if ("undefined" != typeof DD_belatedPNG) {
                    var selectors = [".login-fancybox", ".join-img", ".login-fancybox", ".login-fancybox", "#fancybox-close", ".fancybox-bg", ".logo a", ".language-switcher .block-content .selector", ".social a", ".partner-primary li", ".partner-secondary li", ".contact-icons span", ".selector span"];
                    setTimeout(function() {
                        DD_belatedPNG.fix(selectors.join(", "))
                    }, 750)
                }
            }
        }, Drupal.behaviors.cmLivechat = {
            attach: function(context, settings) {
                $("a.popup.livechat").click(function(e) {
                    e.preventDefault();
                    window.open($(this).attr("href"), "livechatWindow", "height=810,width=820,scrollbars=1")
                })
            }
        }, Drupal.behaviors.cmScrolltop = {
            attach: function(context, settings) {
                $(window).scroll(function() {
                    $(this).scrollTop() > 100 ? $(".scrollup").fadeIn() : $(".scrollup").fadeOut()
                }), $(".scrollup").click(function() {
                    return $("html, body").animate({
                        scrollTop: 0
                    }, 600), !1
                })
            }
        }, Drupal.behaviors.cmFloatingDivs = {
            attach: function(context, settings) {
                var selectors = [".float-div-right", ".float-div-left"],
                    $floatingDivs = $(selectors.join(", "));
                $floatingDivs.floater({
                    parent: "#main-page-wrapper",
                    duration: 650,
                    offsetTop: 0,
                    offsetBottom: 0
                }), $("body").hasClass("logged-in") && $floatingDivs.mouseenter(function() {
                    var pos = $(this).position();
                    $(this).closest("div.block").find(".contextual-links-wrapper").css({
                        top: pos.top,
                        left: pos.left,
                        right: "auto",
                        "z-index": 1e3
                    })
                })
            }
        }, Drupal.behaviors.cmContent = {
            Accordion: function() {
                $(".accordion-header").prepend("<span></span>"), $(".accordion-header").click(function(e) {
                    var divSpan = $(this).find("span"),
                        spanCSS = divSpan.hasClass("active");
                    spanCSS === !0 ? divSpan.removeClass("active") : divSpan.addClass("active")
                })
            },
            PMFancybox: function() {
                $("a.view").fancybox({
                    type: "inline",
                    height: "auto",
                    padding: 10,
                    scrolling: "no",
                    overlayOpacity: .76,
                    overlayColor: "#000",
                    centerOnScroll: !1,
                    autoDimensions: !1,
                    onComplete: function(element) {
                        $($(element).attr("href")).closest("#fancybox-outer").addClass("modal-lightbox"), $("#fancybox-content").attr("style", "border-width: 10px; width: 560px; height: auto;")
                    },
                    onClosed: function(element) {
                        $(".modal-light").removeClass("modal-light")
                    }
                })
            },
            CurrencyFancybox: function() {
                $("a.view-currency").fancybox({
                    type: "inline",
                    height: "auto",
                    padding: 10,
                    width: 412,
                    showCloseButton: !1,
                    scrolling: "no",
                    overlayOpacity: .76,
                    overlayColor: "#000",
                    centerOnScroll: !0,
                    autoDimensions: !1,
                    onStart: function(element) {
                        $("#disabled-currency-message").css("min-height", "350px")
                    },
                    onComplete: function(element) {
                        initMessage = $("#disabled-currency-message").html(), msg = initMessage.replace("{gameRoomName}", $(element).siblings(".game-room-replacement-title").html().replace("<br>", "")), $($(element).attr("href")).closest("#fancybox-outer").addClass("modal-decorated"), $("#disabled-currency-message").html(msg), $("#disabled-currency-message").css("visibility", "visible")
                    },
                    onClosed: function(element) {
                        initMessage = $("#disabled-currency-message").html(), msg = initMessage.replace($(element).siblings(".game-room-replacement-title").html().replace("<br>", ""), "{gameRoomName}"), $(".modal-decorated").removeClass("modal-decorated"), $("#disabled-currency-message").html(msg), $("#disabled-currency-message").css("visibility", "hidden")
                    }
                })
            },
            ContactInfo: function() {
                var wrapper = $(".contactcountry-container");
                $("#contactcountry").change(function() {
                    wrapper.empty();
                    var value = $(this).find("option:selected").val();
                    if (value.length > 0)
                        for (var contacts = value.split(","), i = 0; i < contacts.length; i++) i > 0 && $("<p>" + Drupal.t("or") + "</p>").appendTo(wrapper), $('<div class="btn btn-contactcountry"><a href="tel:' + contacts[i].replace(/ /g, "-") + '"><span class="icon-phone-black">&nbsp;</span>' + contacts[i] + "</a></div>").appendTo(wrapper)
                })
            },
            ContactForm: function() {
                $("form.webform-client-form").submit(function(e) {
                    $("input[name='submitted[from_email]']").val($("input[name='submitted[email]']").val())
                })
            },
            SlideshowPager: function() {
                $(window).load(function() {
                    var ctr = 0;
                    $(".game-slider-image").each(function() {
                        var className = "game-pager-" + $(this).data("image-id"),
                            slide = ".bx-pager-link[data-slide-index=" + ctr + "]";
                        $(slide).addClass(className), ctr++
                    })
                })
            },
            GameLaunch: function() {
                if ("function" == typeof getCookie && "function" == typeof createCookie && "function" == typeof getFim && "undefined" != typeof fimName && "true" === getCookie("launchGame")) {
                    var fim = getFim(fimName);
                    "function" != typeof fim.initClient ? setTimeout(this.GameLaunch, 1e3) : setTimeout(function() {
                        createCookie("launchGame", "false"), $("#" + getCookie("gameId")).trigger("click")
                    }, 1)
                }
            },
            Promotions: function() {
                $(".view-games-menu .category").each(function() {
                    $(".promotion-" + $(this).data("cat").toLowerCase()).length || $(this).parents(".views-row").remove()
                });
                var link = window.location.pathname,
                    row = $(".promotions-right-sidebar .views-row"),
                    lastRow = $(".promotions-right-sidebar .views-row-last");
                $("body.node-type-promotion").length ? row.each(function(index) {
                    var promoUrl = $(this).find(".views-field-view-node a").attr("href");
                    index < 3 && promoUrl == link && row.length > 1 ? ($(this).hide(), lastRow.find(".views-field-view-node a").attr("href") != link && lastRow.show()) : 1 == row.length && (promoUrl == link ? $(this).parents(".promotions-right-sidebar-block").hide() : $(this).show())
                }) : row.length < 4 && lastRow.show(), $(".view-games-menu .category").on("click", function() {
                    var i = 0,
                        maxHeight = 0,
                        container = $(".promotion-" + $(this).data("cat").toLowerCase()).parent(".promotions-container"),
                        promoList = container.find(".promotion-list"),
                        row = $(this).parents(".views-row");
                    $(".view-games-menu .category").parents(".views-row").removeClass("selected"), row.addClass("selected"), $(".promotions-container").hide(), container.show().parent(".view-content").removeClass().addClass("view-content tab-" + row.index()), promoList.each(function() {
                        var height = promoList.eq(i).find(".promotion-block-desc").height();
                        height > maxHeight && (maxHeight = height), promoList.eq(i).find(".promotion-block-desc").css("height", maxHeight), i % 2 == 1 && (promoList.eq(i - 1).find(".promotion-block-desc").css("height", maxHeight), maxHeight = 0), i++
                    })
                }), $(".view-games-menu .views-row:first-child .category").click(), $(".view-games-menu .views-row").removeClass("first-load")
            },
            attach: function(context, settings) {
                var self = this;
                $("#ads").insertBefore($(".main")), $.fancybox && (self.PMFancybox(), self.CurrencyFancybox()), settings.matterhorn.account.is_logged_in && self.GameLaunch(), self.Accordion(), self.ContactInfo(), self.ContactForm(), self.SlideshowPager(), self.Promotions(), $(".game-img, .game-title").click(function(e) {
                    e.preventDefault(), data = $(this).data("image-id"), $(".matterhorn-slider-macau").find($(".game-pager-" + data)).click()
                });
                var block_selectors = [".game-room-block .maintenance-mode", ".game-room-block .coming-soon", ".game-room-right-sidebar.maintenance-mode", ".game-room-right-sidebar.coming-soon"];
                $(block_selectors.join(", ")).each(function() {
                    $(this).find(".playnow").off("click").click(function(e) {
                        e.preventDefault()
                    })
                })
            }
        }
    }(jQuery);